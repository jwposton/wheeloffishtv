---
phase: 11-sync-rebuild-diagnostics
plan: 08
type: execute
wave: 2
depends_on: ["11-04"]
gap_closure: true
files_modified:
  - frontend/src/components/playlists/RebuildBanner.tsx
  - frontend/src/components/playlists/RebuildBanner.test.tsx
  - frontend/src/pages/PlaylistDetailPage.tsx
autonomous: true
requirements: [DIAG-04, DIAG-05]
must_haves:
  truths:
    - "RebuildBanner has no empty placeholder paragraph elements"
    - "open_series from diagnostics modal uses React Router navigate without full page reload on playlist detail"
  artifacts:
    - path: "frontend/src/components/playlists/RebuildBanner.tsx"
      provides: "Clean banner DOM + navigate in actionContext"
    - path: "frontend/src/pages/PlaylistDetailPage.tsx"
      provides: "Passes navigate to RebuildBanner"
  key_links:
    - from: "PlaylistDetailPage.tsx"
      to: "RebuildBanner"
      via: "navigate prop"
      pattern: "useNavigate"
---

<objective>
Close verification info gaps IN-01 and IN-02 from `11-VERIFICATION.md`: remove dead DOM nodes in `RebuildBanner` and wire SPA navigation for `open_series` actions from the diagnostics modal on playlist detail.

Purpose: Polish only — no API or trigger logic changes (DIAG-05 compact surface unchanged).

Output: Banner cleanup, navigate prop plumbing, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/11-sync-rebuild-diagnostics/11-UI-SPEC.md
@.planning/phases/11-sync-rebuild-diagnostics/11-VERIFICATION.md
</execution_context>

<context>
## Gaps

| ID | Issue | Fix |
|----|-------|-----|
| IN-01 | Empty `<p className="text-sm text-muted-foreground"></p>` at lines 57-58 and 85-86 | Delete both elements |
| IN-02 | `actionContext` omits `navigate` → full page reload on open_series | Pass `useNavigate()` from `PlaylistDetailPage` through `RebuildBanner` into `RebuildDiagnosticsDialog` actionContext |

Reference: `PlaylistMembersPanel` / `TwoPanePicker` navigate pattern; `rebuildDiagnostics.test.ts` already tests `ctx.navigate`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove empty banner placeholders (IN-01)</name>
  <files>frontend/src/components/playlists/RebuildBanner.tsx</files>
  <read_first>
    @frontend/src/components/playlists/RebuildBanner.tsx
    @frontend/src/components/playlists/RebuildBanner.test.tsx
  </read_first>
  <action>
    Remove the two empty `<p className="text-sm text-muted-foreground"></p>` elements under the "Last rebuild" and provider sync section headers. Keep slots-filled line and partial warning paragraph.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run RebuildBanner.test.tsx</automated>
  </verify>
  <done>IN-01 closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire navigate into diagnostics actionContext (IN-02)</name>
  <files>frontend/src/components/playlists/RebuildBanner.tsx, frontend/src/components/playlists/RebuildBanner.test.tsx, frontend/src/pages/PlaylistDetailPage.tsx</files>
  <read_first>
    @frontend/src/pages/PlaylistDetailPage.tsx
    @frontend/src/components/playlists/RebuildBanner.tsx
    @frontend/src/lib/rebuildDiagnostics.test.ts
  </read_first>
  <action>
    Add optional `navigate?: (to: string) => void` to `RebuildBannerProps`. Pass into `RebuildDiagnosticsDialog` `actionContext`. Wire `useNavigate()` from `PlaylistDetailPage`.

    Extend `RebuildBanner.test.tsx` to assert `navigate` is called for `open_series` and `window.location.assign` is not (mock assign).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run RebuildBanner.test.tsx rebuildDiagnostics.test.ts</automated>
  </verify>
  <done>IN-02 closed: SPA navigation for series links from modal.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition |
|-----------|----------|-----------|-------------|
| T-11-SC | Tampering | npm | accept — no new packages |
</threat_model>

<verification>
- `cd frontend && npm test -- --run RebuildBanner.test.tsx rebuildDiagnostics.test.ts`
</verification>

<success_criteria>
- IN-01 and IN-02 from verification report addressed
- No change to compact list cards or `shouldShowDiagnostics` rules (owned by 11-07)
</success_criteria>

<artifacts_this_phase_produces>
- `RebuildBanner` optional `navigate` prop
- `PlaylistDetailPage` navigate wiring
- Banner test for open_series navigate path
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-08-SUMMARY.md` when done.
</output>
