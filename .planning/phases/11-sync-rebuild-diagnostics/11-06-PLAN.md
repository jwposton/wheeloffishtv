---
phase: 11-sync-rebuild-diagnostics
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/src/wheeloffish/core/rebuild_diagnostics.py
  - backend/tests/unit/test_rebuild_diagnostics.py
autonomous: true
requirements: [DIAG-02]
must_haves:
  truths:
    - "Failed rebuild with null/empty error_message still populates diagnostics.rebuild_error"
    - "Writeback failed with no per-episode warnings still populates a structured episode_issues row from run.writeback_error"
    - "Unknown fetch_warning reason codes map to fetch_failure copy, not writeback_warning"
  artifacts:
    - path: "backend/src/wheeloffish/core/rebuild_diagnostics.py"
      provides: "Complete resolver for edge-case runs"
  key_links:
    - from: "backend/src/wheeloffish/core/rebuild_diagnostics.py"
      to: "build_rebuild_diagnostics"
      via: "status/writeback gates"
      pattern: "if run.status == \"failed\""
---

<objective>
Close verification gaps WR-01, WR-02, and WR-03: ensure the diagnostics resolver always produces structured rows when the operator can open the modal, so empty Rebuild/Episode sections no longer regress D-07 inline error removal.

Purpose: Completes DIAG-02 edge paths identified in `11-VERIFICATION.md` and `11-REVIEW.md` without changing API shape or frontend contracts.

Output: Updated resolver + unit tests (RED first where applicable).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-VERIFICATION.md
</execution_context>

<context>
## Gap sources

| ID | Truth | Fix |
|----|-------|-----|
| WR-01 | Failed + empty `error_message` → modal Rebuild section empty | Gate `rebuild_error` on `status == "failed"` only; `_resolve_rebuild_error` already uses catalog fallback |
| WR-02 | `writeback_status == "failed"` with empty `writeback_warnings` → modal empty | Append synthetic `episode_issues` row from `run.writeback_error` via `writeback_failed` catalog |
| WR-03 | Unknown fetch reason → `writeback_warning` under Shows skipped | Fallback `reason_code` to `fetch_failure` when `raw_reason` not in `REASON_CATALOG` |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 failed rebuild without error_message</name>
  <files>backend/src/wheeloffish/core/rebuild_diagnostics.py, backend/tests/unit/test_rebuild_diagnostics.py</files>
  <read_first>
    @backend/src/wheeloffish/core/rebuild_diagnostics.py (`_resolve_rebuild_error`, `build_rebuild_diagnostics`)
    @backend/tests/unit/test_rebuild_diagnostics.py
  </read_first>
  <action>
    In `build_rebuild_diagnostics`, change `rebuild_error` from `if run.status == "failed" and run.error_message:` to `if run.status == "failed":` and always call `_resolve_rebuild_error(run, ctx)`.

    Add `test_failed_rebuild_without_error_message_uses_catalog_fallback` with `status="failed"`, `error_message=None`, asserting `rebuild_error` is not None and `reason_text` uses catalog default.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py::test_failed_rebuild_without_error_message_uses_catalog_fallback tests/unit/test_rebuild_diagnostics.py::test_failed_rebuild_populates_rebuild_error -x</automated>
  </verify>
  <done>WR-01 closed: failed runs always expose rebuild_error row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 whole-playlist writeback failure row</name>
  <files>backend/src/wheeloffish/core/rebuild_diagnostics.py, backend/tests/unit/test_rebuild_diagnostics.py</files>
  <read_first>
    @backend/src/wheeloffish/core/rebuild_diagnostics.py (`REASON_CATALOG`, episode_issues loop)
    @backend/tests/unit/test_rebuild_diagnostics.py
  </read_first>
  <action>
    After building `episode_issues` from `writeback_warnings`, if `run.writeback_status == "failed"` and `episode_issues` is empty and `run.writeback_error` is truthy, append one row with `reason_code="writeback_failed"`, `label` "Provider sync failed", `reason_text=str(run.writeback_error)`, and catalog actions.

    Add `test_writeback_failed_without_episode_warnings`.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py::test_writeback_failed_without_episode_warnings tests/integration/test_playlists_api.py::test_playlist_detail_diagnostics -x</automated>
  </verify>
  <done>WR-02 closed: writeback-only failures surface in Episode sync section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-03 fetch-specific unknown reason fallback</name>
  <files>backend/src/wheeloffish/core/rebuild_diagnostics.py, backend/tests/unit/test_rebuild_diagnostics.py</files>
  <read_first>
    @backend/src/wheeloffish/core/rebuild_diagnostics.py (`_resolve_show_issue`)
    @backend/tests/unit/test_rebuild_diagnostics.py
  </read_first>
  <action>
    In `_resolve_show_issue`, when `raw_reason not in REASON_CATALOG`, set `reason_code = "fetch_failure"` instead of `"writeback_warning"`.

    Add `test_unknown_fetch_reason_uses_fetch_failure_catalog`.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -q</automated>
  </verify>
  <done>WR-03 closed: Shows skipped uses fetch-oriented copy for unknown reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run.writeback_error` → episode_issues row | Provider-sourced error string becomes operator-facing copy in modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure | `writeback_error` in synthetic row | mitigate | Same as existing writeback paths: display as plain text in React modal (Plan 04); no HTML |
| T-11-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
- `cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -q` — all tests GREEN
</verification>

<success_criteria>
- WR-01, WR-02, WR-03 from `11-VERIFICATION.md` marked closed by behavior + tests
- DIAG-02 verified_with_gaps items for empty-message failed rebuild and writeback-only failure are resolved
</success_criteria>

<artifacts_this_phase_produces>
- `build_rebuild_diagnostics` — updated gates for failed rebuild and writeback_failed synthetic row
- `_resolve_show_issue` — fetch_failure fallback for unknown fetch codes
- `test_failed_rebuild_without_error_message_uses_catalog_fallback`
- `test_writeback_failed_without_episode_warnings`
- `test_unknown_fetch_reason_uses_fetch_failure_catalog`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-06-SUMMARY.md` when done.
</output>
