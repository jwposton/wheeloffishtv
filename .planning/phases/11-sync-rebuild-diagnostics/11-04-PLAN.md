---
phase: 11-sync-rebuild-diagnostics
plan: 04
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - frontend/src/lib/rebuildDiagnostics.ts
  - frontend/src/lib/rebuildDiagnostics.test.ts
  - frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx
  - frontend/src/components/playlists/RebuildDiagnosticsDialog.test.tsx
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "shouldShowDiagnostics returns true only when rebuild or writeback is partial/failed"
    - "Modal renders Rebuild, Shows skipped, Episode sync, and Prune history sections, hiding empty ones"
    - "Each issue row shows label + reason text + muted remediation hint, with id fallback for unknown labels"
    - "Action buttons render from API actions[] metadata and dispatch via a single runner"
    - "Empty modal shows the no-diagnostics empty state with run timestamp"
  artifacts:
    - path: "frontend/src/lib/rebuildDiagnostics.ts"
      provides: "shouldShowDiagnostics + runDiagnosticAction helpers"
      exports: ["shouldShowDiagnostics", "runDiagnosticAction"]
    - path: "frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx"
      provides: "scrollable diagnostics modal"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx"
      to: "DiagnosticAction actions[]"
      via: "render action buttons + runDiagnosticAction"
      pattern: "runDiagnosticAction"
    - from: "frontend/src/lib/rebuildDiagnostics.ts"
      to: "seriesDetailRoute / window.open"
      via: "action dispatch"
      pattern: "open_series|open_provider|remove_row"
---

<objective>
Build the on-demand diagnostics surface: a `lib/rebuildDiagnostics.ts` with `shouldShowDiagnostics(last)` (D-02 trigger visibility) and `runDiagnosticAction(action, ctx)` (D-19 action dispatch), plus the `RebuildDiagnosticsDialog` scrollable modal rendering the four stacked sections with friendly rows, id fallbacks, remediation hints, API-driven action buttons, and the empty state (D-09 through D-14, D-20). The trigger wiring and inline-list removal live in Plan 05.

Purpose: Centralize trigger logic + action dispatch in a tested helper and render the backend-resolved rows from API metadata (never hardcoding reason copy — D-17/D-19).

Output: lib helpers + tests; modal component + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-UI-SPEC.md
@frontend/src/api/playlists.ts
</context>

<task type="auto" tdd="true">
  <name>Task 1: rebuildDiagnostics lib helpers + tests</name>
  <files>frontend/src/lib/rebuildDiagnostics.ts, frontend/src/lib/rebuildDiagnostics.test.ts</files>
  <read_first>
    @frontend/src/api/playlists.ts (RebuildRunSummary, RebuildDiagnostics, DiagnosticAction types from Plan 01)
    @frontend/src/lib/seriesId.ts (seriesDetailRoute helper)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pattern 3 action runner; Pattern 4 trigger visibility; note A2 on partial writeback)
  </read_first>
  <behavior>
    - `shouldShowDiagnostics(null)` → false.
    - rebuild `status` of `partial` or `failed` → true; `succeeded`/`queued`/`running` with non-warning writeback → false (D-02).
    - writeback_status `partial` or `failed` → true even when rebuild succeeded (D-02, RESEARCH note A2).
    - `runDiagnosticAction` dispatches `remove_row` → `ctx.onRemoveRow(series_id)` (only when series_id present); `open_series` → navigates to `seriesDetailRoute(series_id)`; `open_provider` → `window.open(url, "_blank", "noopener,noreferrer")` (only when url present); unknown/incomplete action is a no-op.
  </behavior>
  <action>
    Create `frontend/src/lib/rebuildDiagnostics.ts` exporting `shouldShowDiagnostics(last: RebuildRunSummary | null): boolean` per the behavior matrix and `runDiagnosticAction(action: DiagnosticAction, ctx: { onRemoveRow: (seriesId: string) => void; navigate?: (to: string) => void }): void` that switches on `action.type` and uses `seriesDetailRoute` for `open_series` (prefer `ctx.navigate` when provided, else `window.location.assign`) and `window.open(..., "noopener,noreferrer")` for `open_provider` (open-redirect safe per T-11-02). Write `rebuildDiagnostics.test.ts` covering every behavior bullet; stub `window.open` and the navigate callback with `vi.fn()`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/rebuildDiagnostics.test.ts</automated>
  </verify>
  <done>All lib tests pass; `shouldShowDiagnostics` matches D-02; `runDiagnosticAction` dispatches the three action types with safe defaults and no-ops on incomplete actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RebuildDiagnosticsDialog modal + tests</name>
  <files>frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx, frontend/src/components/playlists/RebuildDiagnosticsDialog.test.tsx</files>
  <read_first>
    @frontend/src/components/playlists/QuickCreatePlaylistDialog.tsx (Dialog/DialogContent/DialogHeader/DialogTitle usage)
    @frontend/src/components/playlists/StatusBadge.tsx (header status badge)
    @frontend/src/components/playlists/WritebackStatus.tsx (existing relative-time / label conventions)
    @frontend/src/api/playlists.ts (RebuildDiagnostics, DiagnosticIssueRow, DiagnosticAction, PruneEvent, RebuildRunSummary)
    @.planning/phases/11-sync-rebuild-diagnostics/11-UI-SPEC.md (modal layout, section order, copy contract, empty state)
  </read_first>
  <action>
    Create `RebuildDiagnosticsDialog.tsx` using the existing `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle` from `components/ui/dialog` (scrollable per UI-SPEC: content `max-h-[70vh] overflow-y-auto`, width `max-w-lg`/`max-w-xl`). Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `lastRebuild: RebuildRunSummary | null`, `pruneEvents: PruneEvent[]`, and an action context `{ onRemoveRow: (seriesId: string) => void }`. Header: title "Rebuild diagnostics", a `StatusBadge` for `lastRebuild.status`, and a relative "Finished {time}" using the existing relative-time formatting pattern (D-14). The modal reads the latest run only (`lastRebuild`) with no run picker or prior-run UI (D-13, D-16). Body renders, in order, four sections (D-09): Rebuild (from `diagnostics.rebuild_error`, single row when present), Shows skipped (`diagnostics.show_issues`), Episode sync (`diagnostics.episode_issues`), Prune history (`pruneEvents`). Hide any section with no rows (D-10). Each issue row: primary `label`; `reason_text` in the same block; `remediation_hint` as a muted line below (D-11); when the row label is the "Unknown show/episode" copy, render the `series_id`/`episode_id` in subdued monospace below (D-20). Render one inline button/link per entry in `row.actions` using `action.label` as the visible text and dispatching through `runDiagnosticAction` from Task 1 (D-19) — do not hardcode reason→action mapping. For the Prune history section, map each `PruneEvent` to a row using its `event_type`/`reason` for copy and an `open_series` affordance when `series_id` is present (discretion: client-side mapping per RESEARCH Open Question 1). Empty state (D-12): when `rebuild_error` is null and all three issue lists AND `pruneEvents` are empty, render the UI-SPEC empty-state heading + body with the run's relative finished time instead of sections (do not silently close). Use React text nodes only (no `dangerouslySetInnerHTML`) per T-11-03. Write `RebuildDiagnosticsDialog.test.tsx` covering: renders show + episode rows with labels/hints, hides empty sections, renders action buttons from `actions[]` and calls `onRemoveRow` on a `remove_row` click, shows id fallback for unknown labels, and shows the empty state when no rows exist.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/playlists/RebuildDiagnosticsDialog.test.tsx</automated>
  </verify>
  <done>Modal renders the four sections with correct ordering, hides empty sections (D-10), renders rows per D-11/D-20, dispatches actions from API metadata via `runDiagnosticAction` (D-19), shows the empty state (D-12), and uses `Dialog` (not AlertDialog); tests green.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → rendered modal | Backend-resolved rows (including normalized exception text) rendered to the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure (XSS) | reason_text / labels in modal | mitigate | Render via React text nodes only; no `dangerouslySetInnerHTML` anywhere in the dialog |
| T-11-02 | Tampering | `open_provider` navigation | mitigate | `runDiagnosticAction` opens provider URLs with `noopener,noreferrer`; URL comes only from API `action.url` (server-built) |
| T-11-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none); reuses existing Dialog/Button/Badge |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/lib/rebuildDiagnostics.test.ts src/components/playlists/RebuildDiagnosticsDialog.test.tsx` green.
- `cd frontend && npx tsc --noEmit` passes.
</verification>

<success_criteria>
- `shouldShowDiagnostics` matches D-02 trigger rules (DIAG-01 enabling).
- Modal renders the four sections from API metadata with hints, id fallback, and metadata-driven actions (DIAG-02, DIAG-03, DIAG-04); empty state per D-12.
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/lib/rebuildDiagnostics.ts` (+ test) — trigger visibility + action dispatch.
- `frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx` (+ test) — scrollable diagnostics modal.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-04-SUMMARY.md` when done.
</output>
