---
phase: 11-sync-rebuild-diagnostics
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - backend/src/wheeloffish/api/routes/playlists.py
  - backend/tests/integration/test_playlists_api.py
autonomous: true
requirements: [DIAG-02]
must_haves:
  truths:
    - "GET /playlists/{id} embeds resolved diagnostics on last_rebuild only"
    - "recent_runs carry no diagnostics (diagnostics stays None there)"
    - "Diagnostics are reachable only through the owner-gated detail endpoint"
    - "recent_prune_events remain embedded (Phase 10 regression intact)"
  artifacts:
    - path: "backend/src/wheeloffish/api/routes/playlists.py"
      provides: "diagnostics assembly wired into _playlist_to_detail last_rebuild"
      contains: "build_rebuild_diagnostics"
    - path: "backend/tests/integration/test_playlists_api.py"
      provides: "test_playlist_detail_diagnostics integration coverage"
      contains: "test_playlist_detail_diagnostics"
  key_links:
    - from: "backend/src/wheeloffish/api/routes/playlists.py"
      to: "wheeloffish.core.rebuild_diagnostics.build_rebuild_diagnostics"
      via: "import + call in _playlist_to_detail"
      pattern: "build_rebuild_diagnostics"
    - from: "_playlist_to_detail"
      to: "last_rebuild.diagnostics"
      via: "attach on summary"
      pattern: "diagnostics="
---

<objective>
Wire the Plan 02 resolver into the playlist detail GET: build a `DiagnosticsContext` from data already available in `_playlist_to_detail` (owner-scoped series titles, episode titles from the latest run's own snapshot, provider open URL) and attach `diagnostics` to the `last_rebuild` summary only (D-21, D-24). `recent_runs` stay summary-only. No new endpoint (D-21).

Purpose: Deliver DIAG-02 over the existing owner-gated surface without changing request inputs or adding routes.

Output: Modified `_playlist_to_detail` + integration test proving the embed and the D-24 scoping, plus the Phase 10 prune-events regression staying green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md
@backend/src/wheeloffish/core/rebuild_diagnostics.py
</context>

<task type="auto">
  <name>Task 1: Embed diagnostics on last_rebuild in _playlist_to_detail</name>
  <files>backend/src/wheeloffish/api/routes/playlists.py</files>
  <read_first>
    @backend/src/wheeloffish/api/routes/playlists.py (lines 108-253: _rebuild_run_to_summary, _series_title_map, _playlist_to_detail, _playlist_open_url)
    @backend/src/wheeloffish/core/rebuild_diagnostics.py (DiagnosticsContext, build_rebuild_diagnostics)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pattern 1 call-site; Pitfall 1 needs DB context; Pitfall 2 episode titles from the run's own snapshot)
  </read_first>
  <action>
    In `_playlist_to_detail`, after computing `latest_run` (the most-recent run used for `last_rebuild`, ~line 205), build diagnostics for that run only. Construct a `DiagnosticsContext`: `series_title_map` via the existing `_series_title_map(db, app_user_id, series_ids)` over the union of playlist row series ids and any series ids found in `latest_run.row_outcomes_json["fetch_warnings"]`; `episode_title_map` built from `latest_run.snapshot_json` (the run's OWN snapshot per Pitfall 2 — map `episode_id`→`title`, falling back to the playlist-level `current_snapshot` titles when the run has no snapshot); `provider_open_url` = the value already computed by `_playlist_open_url(db, playlist)` (reuse it; do not rebuild). When `latest_run` exists, set `last_rebuild` to `_rebuild_run_to_summary(latest_run)` and then assign `last_rebuild.diagnostics = build_rebuild_diagnostics(latest_run, ctx)` (per D-24 do NOT touch `_rebuild_run_to_summary` for `recent_runs` — keep that builder diagnostics-free so `recent_runs` emit `diagnostics: None`). Import `build_rebuild_diagnostics` and `DiagnosticsContext` from `wheeloffish.core.rebuild_diagnostics`. Keep all ownership gating unchanged (route still goes through `_get_owned_playlist`).
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/integration/test_playlists_api.py -q</automated>
  </verify>
  <done>Existing playlist API tests stay green; `last_rebuild.diagnostics` is populated when a latest run exists; `recent_runs[*].diagnostics` is `None`; `_rebuild_run_to_summary` is unchanged for `recent_runs`.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test for diagnostics embed + D-24 scoping</name>
  <files>backend/tests/integration/test_playlists_api.py</files>
  <read_first>
    @backend/tests/integration/test_playlists_api.py (fixtures _make_user/_set_user/base_client/_create_body; test_rebuild_owner_only_returns_run_summary line 238; test_prune_events_in_detail line 554 for RebuildRun + prune seeding patterns)
    @backend/src/wheeloffish/db/models/rebuild_run.py (fields to seed: status, error_message, row_outcomes_json, writeback_warnings, snapshot_json, started_at, finished_at)
  </read_first>
  <action>
    Add `test_playlist_detail_diagnostics`: create an owned playlist, seed a `RebuildRun` with `status="partial"`, `row_outcomes_json={"outcomes": [], "fetch_warnings": [{"series_id": <row series id>, "reason": "fetch_failure"}]}`, `writeback_warnings=[{"episode_id": "c::plex::e1", "reason": "not found (404)"}, {"episode_id": None, "reason": "info notice"}]`, and a `started_at`/`finished_at`. GET the detail and assert: `last_rebuild["diagnostics"]` is present; `show_issues` contains a row with `reason_code == "fetch_failure"` carrying `actions`; `episode_issues` contains exactly one row (the info notice with `episode_id None` is excluded) with normalized `reason_code == "episode_not_found"`; and assert D-24 by also seeding a second older run and confirming every entry in `recent_runs` has `diagnostics is None`. Add `test_failed_run_diagnostics_has_rebuild_error`: seed `status="failed"`, `error_message="boom"`, empty warnings, GET detail, assert `last_rebuild["diagnostics"]["rebuild_error"]` is non-null and `reason_code == "rebuild_failed"`. Reuse existing fixtures; do not add new endpoints or mocks beyond DB seeding.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/integration/test_playlists_api.py::test_playlist_detail_diagnostics tests/integration/test_playlists_api.py::test_failed_run_diagnostics_has_rebuild_error tests/integration/test_playlists_api.py::test_prune_events_in_detail -q</automated>
  </verify>
  <done>Both new tests pass; the Phase 10 `test_prune_events_in_detail` regression stays green; `recent_runs` diagnostics confirmed `None` (D-24).</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /playlists/{id} | Authenticated operator reads playlist detail; diagnostics ride on the response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure (IDOR) | diagnostics on detail GET | mitigate | Embed only inside `_playlist_to_detail`, reached via `_get_owned_playlist` (app_user_id-scoped); no new endpoint; integration test asserts owner-only path (existing `test_get_other_users_playlist_404`) |
| T-11-02 | Tampering | `open_provider` action URL | mitigate | URL passed to resolver is `_playlist_open_url(...)` built server-side from the connection's `base_url`; never from request input |
| T-11-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cd backend && python3 -m pytest tests/integration/test_playlists_api.py -q` green (includes prune regression).
- `cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -q` still green (resolver untouched).
</verification>

<success_criteria>
- `last_rebuild.diagnostics` populated on detail GET; `recent_runs` diagnostics `None` (DIAG-02, D-21, D-24).
- Owner-gating unchanged; prune events regression intact (PRUNE-03).
</success_criteria>

<artifacts_this_phase_produces>
- `backend/src/wheeloffish/api/routes/playlists.py` — `_playlist_to_detail` builds + attaches diagnostics to `last_rebuild`.
- `backend/tests/integration/test_playlists_api.py` — `test_playlist_detail_diagnostics` + `test_failed_run_diagnostics_has_rebuild_error`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-03-SUMMARY.md` when done.
</output>
