---
phase: 11-sync-rebuild-diagnostics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/wheeloffish/api/schemas/playlists.py
  - frontend/src/api/playlists.ts
  - backend/tests/unit/test_rebuild_diagnostics.py
autonomous: true
requirements: [DIAG-02, DIAG-05]
must_haves:
  truths:
    - "Backend exposes a typed diagnostics shape ({ rebuild_error?, show_issues[], episode_issues[] }) on the rebuild run summary"
    - "Frontend types mirror the diagnostics shape and the playlist detail carries recent_prune_events"
    - "A backend resolver unit test exists and fails RED against the not-yet-built rebuild_diagnostics module"
  artifacts:
    - path: "backend/src/wheeloffish/api/schemas/playlists.py"
      provides: "DiagnosticAction, DiagnosticIssueRow, RebuildDiagnostics models + diagnostics field on RebuildRunSummary"
      contains: "class RebuildDiagnostics"
    - path: "frontend/src/api/playlists.ts"
      provides: "RebuildDiagnostics/DiagnosticIssueRow/DiagnosticAction/PruneEvent TS types + recent_prune_events on PlaylistDetailResponse"
      contains: "RebuildDiagnostics"
    - path: "backend/tests/unit/test_rebuild_diagnostics.py"
      provides: "RED resolver contract test"
      contains: "build_rebuild_diagnostics"
  key_links:
    - from: "backend/src/wheeloffish/api/schemas/playlists.py"
      to: "RebuildRunSummary.diagnostics"
      via: "optional field"
      pattern: "diagnostics: RebuildDiagnostics \\| None"
    - from: "frontend/src/api/playlists.ts"
      to: "PlaylistDetailResponse.recent_prune_events"
      via: "interface field"
      pattern: "recent_prune_events"
---

<objective>
Establish the cross-cutting contracts every other Phase 11 plan builds against: the backend Pydantic diagnostics models, the mirrored frontend TypeScript types (including the currently-missing `recent_prune_events` on `PlaylistDetailResponse`), and a RED unit-test scaffold that pins the backend resolver's expected behavior (D-21, D-22).

Purpose: Interface-first ordering. Downstream resolver, route, and UI plans receive the diagnostics contract in the plan itself instead of re-deriving it, and the resolver plan inherits a failing test to turn GREEN.

Output: New Pydantic models + field on `RebuildRunSummary`; new TS interfaces + `recent_prune_events`; RED `test_rebuild_diagnostics.py`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend diagnostics Pydantic models</name>
  <files>backend/src/wheeloffish/api/schemas/playlists.py</files>
  <read_first>
    @backend/src/wheeloffish/api/schemas/playlists.py (existing RebuildRunSummary, PruneEventResponse, PlaylistDetailResponse)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Code Examples → "Pydantic models (proposed)")
  </read_first>
  <action>
    In `schemas/playlists.py`, add three new Pydantic v2 BaseModel classes implementing D-22/D-23 exactly:
    `DiagnosticAction` with fields `type: Literal["remove_row", "open_provider", "open_series"]`, `label: str`, `series_id: str | None = None`, `episode_id: str | None = None`, `url: str | None = None`;
    `DiagnosticIssueRow` with fields `label: str`, `reason_code: str`, `reason_text: str`, `remediation_hint: str`, `series_id: str | None = None`, `episode_id: str | None = None`, `actions: list[DiagnosticAction] = Field(default_factory=list)`;
    `RebuildDiagnostics` with fields `rebuild_error: DiagnosticIssueRow | None = None`, `show_issues: list[DiagnosticIssueRow] = Field(default_factory=list)`, `episode_issues: list[DiagnosticIssueRow] = Field(default_factory=list)`.
    Import `Literal` from `typing`. Add an optional field `diagnostics: RebuildDiagnostics | None = None` to the existing `RebuildRunSummary` class (per D-21 the field rides on the run summary; per D-24 only `last_rebuild` will be populated, so the default stays `None` and `recent_runs` keep emitting `None`). Do not change `PruneEventResponse` or any existing field. Define the new classes before `RebuildRunSummary` so the forward reference resolves without a string annotation.
  </action>
  <verify>
    <automated>cd backend && python3 -c "from wheeloffish.api.schemas.playlists import RebuildDiagnostics, DiagnosticIssueRow, DiagnosticAction, RebuildRunSummary; assert 'diagnostics' in RebuildRunSummary.model_fields; print('ok')"</automated>
  </verify>
  <done>Import succeeds; `RebuildRunSummary.model_fields` contains `diagnostics`; the three new models import cleanly; existing playlist API integration tests still pass (`python3 -m pytest tests/integration/test_playlists_api.py -q`).</done>
</task>

<task type="auto">
  <name>Task 2: Mirror diagnostics types in the frontend API layer</name>
  <files>frontend/src/api/playlists.ts</files>
  <read_first>
    @frontend/src/api/playlists.ts (RebuildRunSummary, PlaylistDetailResponse interfaces)
    @backend/src/wheeloffish/api/schemas/playlists.py (PruneEventResponse — for PruneEvent field parity)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pitfall 3: type drift)
  </read_first>
  <action>
    In `frontend/src/api/playlists.ts`, add exported interfaces mirroring the backend models from Task 1: `DiagnosticAction` (`type: "remove_row" | "open_provider" | "open_series"`, `label: string`, optional `series_id`, `episode_id`, `url`), `DiagnosticIssueRow` (`label`, `reason_code`, `reason_text`, `remediation_hint` strings; optional `series_id`, `episode_id`; `actions: DiagnosticAction[]`), and `RebuildDiagnostics` (`rebuild_error: DiagnosticIssueRow | null`, `show_issues: DiagnosticIssueRow[]`, `episode_issues: DiagnosticIssueRow[]`). Add optional `diagnostics?: RebuildDiagnostics | null` to the existing `RebuildRunSummary` interface. Add a `PruneEvent` interface (`id`, `series_id`, `event_type`, `reason` strings; `event_metadata?: Record<string, unknown> | null`; `timestamp: string`) matching `PruneEventResponse`, and add `recent_prune_events: PruneEvent[]` to `PlaylistDetailResponse` (this field is already sent by the backend today but missing from the type per Pitfall 3 — fixes DIAG-05 prune section consumption). Do not change runtime fetch/query logic.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>`tsc --noEmit` passes with the new interfaces; `RebuildRunSummary` has optional `diagnostics`; `PlaylistDetailResponse` has `recent_prune_events: PruneEvent[]`; no runtime code changed.</done>
</task>

<task type="auto">
  <name>Task 3: Create RED resolver unit-test scaffold</name>
  <files>backend/tests/unit/test_rebuild_diagnostics.py</files>
  <read_first>
    @backend/tests/unit/test_catalog_prune.py (existing unit-test style: plain functions, no DB needed for pure logic)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pattern 1 builder signature; Pattern 2 reason catalog table; Code Examples → fetch/writeback warning shapes)
    @backend/src/wheeloffish/core/orchestrator.py (lines 130-209 — fetch_warnings item shape: {"series_id", "reason"})
    @backend/src/wheeloffish/core/provider_writeback.py (warnings item shape: {"episode_id", "reason"}; ORPHAN_RECREATED_WARNING)
  </read_first>
  <action>
    Create `backend/tests/unit/test_rebuild_diagnostics.py` with failing (RED) tests that pin the resolver contract Plan 02 must satisfy. Import `build_rebuild_diagnostics` and a `DiagnosticsContext` from `wheeloffish.core.rebuild_diagnostics` (module does not exist yet — RED is expected). Construct lightweight `RebuildRun`-like inputs (a real `RebuildRun` ORM instance with `row_outcomes_json={"outcomes": [], "fetch_warnings": [{"series_id": "c::plex::s1", "reason": "fetch_failure"}]}`, `writeback_warnings=[{"episode_id": "c::plex::e1", "reason": "not found (404)"}]`, `status`, `error_message`). Write at minimum these named tests so Plan 02 can target them: `test_fetch_failure_maps_to_show_issue` (DIAG-02: a `fetch_failure` warning becomes a `show_issues` row with `reason_code == "fetch_failure"`), `test_unknown_series_label` (DIAG-03: when the series title map lacks the id, `label` falls back to the "Unknown show" copy and `series_id` is preserved on the row), `test_show_issue_actions` (DIAG-04: a show issue row carries non-empty `actions[]` including an `open_series` action), `test_writeback_404_normalizes_to_episode_not_found` (DIAG-02: a writeback reason containing 404/"not found" normalizes to a known `episode_not_found` reason_code rather than raw exception text), and `test_failed_rebuild_populates_rebuild_error` (a `failed` run with `error_message` yields a `rebuild_error` row). Do not implement the resolver here. Add a module docstring noting these are RED until Plan 02.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -q || echo "RED_EXPECTED"</automated>
  </verify>
  <done>File exists with the five named test functions; running it reports failure/collection error referencing the missing `wheeloffish.core.rebuild_diagnostics` module (RED, expected); Plan 02 will turn it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (response shape only) | This plan only defines types/contracts; no new request input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `RebuildRunSummary.diagnostics` field surface | mitigate | Field is optional and only ever populated on the owner-gated detail GET (enforced in Plan 03); no new endpoint introduced here |
| T-11-SC | Tampering | npm/pip installs | accept | Phase installs no new packages (RESEARCH Package Legitimacy Audit: none); no install task in this plan |
</threat_model>

<verification>
- `cd backend && python3 -c "import wheeloffish.api.schemas.playlists"` imports cleanly.
- `cd frontend && npx tsc --noEmit` passes.
- `cd backend && python3 -m pytest tests/integration/test_playlists_api.py -q` stays green (additive schema change is backward compatible).
</verification>

<success_criteria>
- Backend diagnostics models (`DiagnosticAction`, `DiagnosticIssueRow`, `RebuildDiagnostics`) exist and `RebuildRunSummary` carries an optional `diagnostics` field (D-21, D-22, D-23).
- Frontend TS types mirror the contract and `PlaylistDetailResponse.recent_prune_events` is typed (DIAG-05 enabling, Pitfall 3).
- RED resolver test scaffold exists for Plan 02 to satisfy.
</success_criteria>

<artifacts_this_phase_produces>
- `backend/src/wheeloffish/api/schemas/playlists.py` — extended with diagnostics models + `RebuildRunSummary.diagnostics`.
- `frontend/src/api/playlists.ts` — extended with diagnostics TS types + `recent_prune_events`.
- `backend/tests/unit/test_rebuild_diagnostics.py` — RED resolver contract test (handed to Plan 02).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-01-SUMMARY.md` when done.
</output>
