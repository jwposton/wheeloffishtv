---
phase: 11-sync-rebuild-diagnostics
plan: 02
type: tdd
wave: 1
depends_on: ["11-01"]
files_modified:
  - backend/src/wheeloffish/core/rebuild_diagnostics.py
  - backend/tests/unit/test_rebuild_diagnostics.py
autonomous: true
requirements: [DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "Raw fetch_warnings rows become resolved show issues with reason_code, reason_text, remediation hint, and actions"
    - "Heterogeneous writeback reasons (often raw exception strings) normalize to known reason codes with a generic fallback"
    - "Missing series/episode titles fall back to 'Unknown show/episode' while preserving raw identifiers"
    - "A failed run with error_message produces a rebuild_error row"
  artifacts:
    - path: "backend/src/wheeloffish/core/rebuild_diagnostics.py"
      provides: "REASON_CATALOG, DiagnosticsContext, build_rebuild_diagnostics resolver"
      exports: ["build_rebuild_diagnostics", "DiagnosticsContext", "REASON_CATALOG"]
      min_lines: 80
    - path: "backend/tests/unit/test_rebuild_diagnostics.py"
      provides: "GREEN resolver unit tests"
      contains: "test_show_issue_actions"
  key_links:
    - from: "backend/src/wheeloffish/core/rebuild_diagnostics.py"
      to: "wheeloffish.api.schemas.playlists.RebuildDiagnostics"
      via: "return type"
      pattern: "RebuildDiagnostics"
    - from: "backend/src/wheeloffish/core/rebuild_diagnostics.py"
      to: "REASON_CATALOG"
      via: "reason_code lookup"
      pattern: "REASON_CATALOG"
---

<objective>
Implement the backend diagnostics resolver `wheeloffish/core/rebuild_diagnostics.py`: a central reason catalog plus a pure `build_rebuild_diagnostics(run, ctx)` that turns raw `row_outcomes_json.fetch_warnings`, `writeback_warnings`, and `error_message` into fully-resolved `DiagnosticIssueRow`s with friendly labels, reason text, remediation hints, and structured `actions[]` (D-17, D-18, D-19, D-20). Turn the RED scaffold from Plan 01 GREEN.

Purpose: The hard part of Phase 11 is normalizing heterogeneous writeback reasons and owning operator-facing copy in one tested place — keeping reason maps out of route handlers and the SPA (D-17).

Output: `rebuild_diagnostics.py` resolver module + GREEN unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md
@backend/src/wheeloffish/api/schemas/playlists.py
</context>

<task type="auto" tdd="true">
  <name>Task 1: Extend RED tests to full resolver behavior coverage</name>
  <files>backend/tests/unit/test_rebuild_diagnostics.py</files>
  <read_first>
    @backend/tests/unit/test_rebuild_diagnostics.py (the RED scaffold from Plan 01)
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pattern 2 reason catalog table; Pitfall 2 + Pitfall 4)
    @backend/src/wheeloffish/db/models/rebuild_run.py (RebuildRun fields: row_outcomes_json, writeback_warnings, error_message, status, snapshot_json)
  </read_first>
  <behavior>
    - `fetch_failure` / `empty_snapshot` / `not_found` each map to a `show_issues` row whose `reason_code` matches and whose `reason_text` + `remediation_hint` come from the catalog (DIAG-02, DIAG-04).
    - Show issue rows include `actions` with at least `open_series` (series_id present) and `remove_row` per catalog mapping (DIAG-04).
    - Series title present in context → `label` is the friendly title; absent → `label` is the "Unknown show" copy and `series_id` is preserved (DIAG-03, D-20).
    - Writeback warning with `episode_id` and a reason string containing "404"/"not found" → `episode_issues` row with normalized `reason_code == "episode_not_found"`; an unrecognized exception-string reason → generic `writeback_warning` reason_code (D-18).
    - Writeback warning with `episode_id is None` (info notice such as ORPHAN_RECREATED_WARNING / missing_episode_id) is NOT added to `episode_issues` (info notices are not episode-level diagnostics).
    - `status == "failed"` with `error_message` → `rebuild_error` is a populated row (`reason_code == "rebuild_failed"`); non-failed run → `rebuild_error is None`.
    - Episode label resolution prefers the title map built from the run's own `snapshot_json` (Pitfall 2).
  </behavior>
  <action>
    Replace the RED scaffold body with the full behavior matrix above as discrete pytest functions (keep the five names introduced in Plan 01 plus add `test_empty_snapshot_and_not_found_codes`, `test_writeback_info_notice_excluded`, and `test_episode_label_from_run_snapshot`). Build a `DiagnosticsContext` (per the signature Task 2 implements) carrying `series_title_map: dict[str, str]`, `episode_title_map: dict[str, str]`, and `provider_open_url: str | None`. Use real `RebuildRun` ORM instances populated in-memory (no DB session required — pass attributes directly). Assert on resolved row fields, not on internal helpers.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py --collect-only -q || echo "RED_EXPECTED"</automated>
  </verify>
  <done>Test file defines the full behavior matrix; pytest reports import/collection failure for missing `wheeloffish.core.rebuild_diagnostics` (RED, expected) until Task 2 implements the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reason catalog + build_rebuild_diagnostics</name>
  <files>backend/src/wheeloffish/core/rebuild_diagnostics.py</files>
  <read_first>
    @.planning/phases/11-sync-rebuild-diagnostics/11-RESEARCH.md (Pattern 1 builder; Pattern 2 catalog table; Code Examples)
    @backend/src/wheeloffish/api/schemas/playlists.py (RebuildDiagnostics, DiagnosticIssueRow, DiagnosticAction from Plan 01)
    @backend/src/wheeloffish/core/orchestrator.py (fetch_warnings reasons: fetch_failure, empty_snapshot, not_found)
    @backend/src/wheeloffish/core/provider_writeback.py (writeback reason heterogeneity; ORPHAN_RECREATED_WARNING info notices)
  </read_first>
  <action>
    Create `backend/src/wheeloffish/core/rebuild_diagnostics.py`. Define a frozen `DiagnosticsContext` dataclass with `series_title_map: dict[str, str]`, `episode_title_map: dict[str, str]`, and `provider_open_url: str | None = None`. Define `REASON_CATALOG: dict[str, dict]` keyed by reason_code with `reason_text`, `remediation_hint`, and a list of default action templates, covering the known codes from D-18 and RESEARCH Pattern 2: `fetch_failure`, `empty_snapshot`, `not_found`, `rebuild_failed`, `writeback_failed`, `episode_not_found`, `missing_episode_id`, `writeback_warning` (generic fallback), and prune reasons `catalog_sync` / `operator`. Implement private resolvers `_resolve_show_issue(warning, ctx)`, `_resolve_episode_issue(warning, ctx)`, `_resolve_rebuild_error(run)`, and a `_normalize_writeback_reason(raw: str) -> str` that applies substring heuristics (contains "404" or "not found"/"not_found" → `episode_not_found`; "missing_episode_id" → `missing_episode_id`; otherwise `writeback_warning`) before catalog lookup; log unknown raw reasons with structlog. Build `DiagnosticAction` instances from the catalog templates, filling `series_id`/`episode_id`/`url` from the warning + context (only emit `open_provider` when `ctx.provider_open_url` is set; only emit `open_series` when a `series_id` is available). Label resolution: show label = `ctx.series_title_map.get(series_id)` else the "Unknown show" copy with `series_id` preserved on the row; episode label = `ctx.episode_title_map.get(episode_id)` else "Unknown episode" with `episode_id` preserved (D-20). Implement `build_rebuild_diagnostics(run, ctx) -> RebuildDiagnostics`: iterate `(run.row_outcomes_json or {}).get("fetch_warnings", [])` into `show_issues`; iterate `run.writeback_warnings or []` skipping items without `episode_id` into `episode_issues`; set `rebuild_error` only when `run.status == "failed" and run.error_message` (per D-07 the full text lives in the modal). Keep the function pure (no DB/network); never raise on unknown codes (D-18). Do NOT place fenced code in route handlers — this module owns all copy.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -x</automated>
  </verify>
  <done>All resolver unit tests pass GREEN; module exports `build_rebuild_diagnostics`, `DiagnosticsContext`, `REASON_CATALOG`; unknown reason codes resolve to the generic `writeback_warning` row without raising (D-18); no DB or network access in the resolver.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted run JSON → resolved rows | `row_outcomes_json` / `writeback_warnings` contain raw exception strings (provider-sourced) that become operator-facing copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | `open_provider` action URL | mitigate | Action URL is sourced only from server-built `provider_playlist_open_url` (via `DiagnosticsContext.provider_open_url`), never from warning/exception text or client input |
| T-11-03 | Information Disclosure | raw exception text in `reason_text` | mitigate | Resolver normalizes to catalog copy where known; unknown reasons fall back to generic `writeback_warning` text rather than echoing arbitrary exception internals into a featured field |
| T-11-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH audit: none); no install task |
</threat_model>

<verification>
- `cd backend && python3 -m pytest tests/unit/test_rebuild_diagnostics.py -q` is fully GREEN.
- `cd backend && python3 -c "from wheeloffish.core.rebuild_diagnostics import build_rebuild_diagnostics, DiagnosticsContext, REASON_CATALOG; print(sorted(REASON_CATALOG))"` lists the known reason codes.
</verification>

<success_criteria>
- Known fetch/writeback/prune/rebuild reason codes resolve to friendly text + hints + actions (DIAG-02, DIAG-04).
- Unknown/raw exception reasons normalize or fall back gracefully without raising (D-18).
- Missing titles fall back to "Unknown show/episode" with identifiers preserved (DIAG-03, D-20).
</success_criteria>

<artifacts_this_phase_produces>
- `backend/src/wheeloffish/core/rebuild_diagnostics.py` — NEW resolver + reason catalog.
- `backend/tests/unit/test_rebuild_diagnostics.py` — completed GREEN unit suite.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-02-SUMMARY.md` when done.
</output>
