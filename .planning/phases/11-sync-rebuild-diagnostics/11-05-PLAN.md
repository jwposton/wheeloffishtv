---
phase: 11-sync-rebuild-diagnostics
plan: 05
type: execute
wave: 2
depends_on: ["11-01", "11-04"]
files_modified:
  - frontend/src/components/playlists/RebuildBanner.tsx
  - frontend/src/components/playlists/RebuildBanner.test.tsx
  - frontend/src/components/playlists/WritebackStatus.tsx
  - frontend/src/components/playlists/WritebackStatus.test.tsx
  - frontend/src/pages/PlaylistDetailPage.tsx
autonomous: true
requirements: [DIAG-01, DIAG-05]
must_haves:
  truths:
    - "RebuildBanner shows a single 'View details' link at the panel bottom only when rebuild or writeback is partial/failed"
    - "Clicking View details opens the diagnostics modal"
    - "The failed-rebuild error_message paragraph no longer renders inline in the banner"
    - "WritebackStatus renders no episode/info bullet lists on detail (non-compact); compact stays unchanged"
    - "PlaylistDetailPage feeds prune events and a remove-row handler into the banner/modal"
  artifacts:
    - path: "frontend/src/components/playlists/RebuildBanner.tsx"
      provides: "View details trigger + modal host; inline error removed"
      contains: "shouldShowDiagnostics"
    - path: "frontend/src/components/playlists/WritebackStatus.tsx"
      provides: "detail lists stripped; compact preserved"
    - path: "frontend/src/pages/PlaylistDetailPage.tsx"
      provides: "prune events + onRemoveRow wired to banner"
  key_links:
    - from: "frontend/src/components/playlists/RebuildBanner.tsx"
      to: "RebuildDiagnosticsDialog"
      via: "modal state + render"
      pattern: "RebuildDiagnosticsDialog"
    - from: "frontend/src/pages/PlaylistDetailPage.tsx"
      to: "RebuildBanner pruneEvents/onRemoveRow"
      via: "props"
      pattern: "recent_prune_events"
---

<objective>
Complete the operator-facing surface: add the single panel-level "View details" `Button variant="link"` trigger to `RebuildBanner` with diagnostics modal state (D-01–D-04), remove the inline failed-rebuild error paragraph now that it lives in the modal (D-07), strip the inline writeback episode/info lists from `WritebackStatus` on detail while keeping `compact` (cards) untouched (D-05, D-06, D-08), and wire `PlaylistDetailPage` to pass `recent_prune_events` and a remove-row handler into the banner/modal.

Purpose: Delivers DIAG-01 (open structured diagnostics from warning/error states) and DIAG-05 (compact badges unchanged; granular detail is on-demand only).

Output: Updated banner + WritebackStatus + detail page, with component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md
@.planning/phases/11-sync-rebuild-diagnostics/11-UI-SPEC.md
@frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx
@frontend/src/lib/rebuildDiagnostics.ts
</context>

<task type="auto" tdd="true">
  <name>Task 1: RebuildBanner trigger + modal host; remove inline error</name>
  <files>frontend/src/components/playlists/RebuildBanner.tsx, frontend/src/components/playlists/RebuildBanner.test.tsx</files>
  <read_first>
    @frontend/src/components/playlists/RebuildBanner.tsx (current banner; lines 70-96 inline error + WritebackStatus usage)
    @frontend/src/components/playlists/RebuildDiagnosticsDialog.tsx (modal props from Plan 04)
    @frontend/src/lib/rebuildDiagnostics.ts (shouldShowDiagnostics from Plan 04)
    @frontend/src/api/playlists.ts (RebuildRunSummary, PruneEvent)
    @.planning/phases/11-sync-rebuild-diagnostics/11-UI-SPEC.md (Layout — RebuildBanner)
  </read_first>
  <behavior>
    - When `shouldShowDiagnostics(lastRebuild)` is false, no "View details" control renders.
    - When true, exactly one `Button variant="link"` labeled "View details" renders at the panel bottom (D-03/D-04), and clicking it opens `RebuildDiagnosticsDialog` (D-01).
    - The failed-rebuild `error_message` paragraph (current lines 70-74) is removed; the partial one-line summary copy remains (D-06, D-07).
  </behavior>
  <action>
    Extend `RebuildBannerProps` with `pruneEvents?: PruneEvent[]` and `onRemoveRow?: (seriesId: string) => void`. Add local `useState` modal open state. Remove the inline `status === "failed" && error_message` paragraph (D-07) — keep the partial-warnings amber one-liner and all badges/slot copy unchanged. After both sections, render a single `Button variant="link"` ("View details", `className="h-auto px-0 self-start"`) guarded by `shouldShowDiagnostics(lastRebuild)` (D-01–D-04). Render `RebuildDiagnosticsDialog` with `open`, `onOpenChange`, `lastRebuild`, `pruneEvents={pruneEvents ?? []}`, and the action context `{ onRemoveRow: onRemoveRow ?? (() => {}) }`. Keep passing existing props to `WritebackStatus` (the inline list removal happens inside that component in Task 2). Write `RebuildBanner.test.tsx`: trigger hidden on succeeded/no-warning run, trigger visible + opens modal on partial and on failed, and assert the raw `error_message` text is NOT present inline in the banner.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/playlists/RebuildBanner.test.tsx</automated>
  </verify>
  <done>Single "View details" link appears only on partial/failed rebuild or partial/failed writeback (D-01–D-04); clicking opens the modal; inline failed `error_message` paragraph removed (D-07); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip inline writeback lists on detail; keep compact</name>
  <files>frontend/src/components/playlists/WritebackStatus.tsx, frontend/src/components/playlists/WritebackStatus.test.tsx</files>
  <read_first>
    @frontend/src/components/playlists/WritebackStatus.tsx (current episodeWarnings/infoNotices lists, lines 104-137)
    @.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md (D-05, D-06, D-08)
  </read_first>
  <behavior>
    - Non-compact (detail) render: status badge + "Open in {provider}" link remain; NO episode warning `<ul>` and NO info-notice `<ul>` render (D-05/D-06).
    - Compact (cards) render: unchanged from today (D-08) — it already renders badge-only.
    - The failed-status one-line "Sync failed: {error}" summary may remain as a one-liner (banner summary), but the bulleted per-episode list is gone.
  </behavior>
  <action>
    Remove the two `<ul>` blocks (the `episodeWarnings` list and the `infoNotices` list) and the now-unused `warningLabel` helper / `episodeTitlesById` prop usage that only fed those lists (D-05, D-06). Keep the status badge, the provider "Open in" link, and the failed one-line summary. Do not change any `compact` branch behavior (D-08). Prune dead props/imports so `tsc` stays clean. Update `WritebackStatus.test.tsx` (create if absent): assert that on a non-compact partial/failed status with warnings, no list with role/aria-label "Sync warnings" or "Sync notes" is rendered, while the status badge still renders; and that compact mode still renders the badge only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/playlists/WritebackStatus.test.tsx</automated>
  </verify>
  <done>No inline episode/info bullet lists render on detail (D-05/D-06); compact card rendering unchanged (D-08); component + its consumers type-check; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire prune events + remove-row handler in PlaylistDetailPage</name>
  <files>frontend/src/pages/PlaylistDetailPage.tsx</files>
  <read_first>
    @frontend/src/pages/PlaylistDetailPage.tsx (RebuildBanner usage lines 115-120)
    @frontend/src/api/playlists.ts (useRemovePlaylistRow mutation; PlaylistDetailResponse.recent_prune_events from Plan 01)
    @.planning/phases/11-sync-rebuild-diagnostics/11-CONTEXT.md (D-15 prune playlist-scoped; Claude's Discretion: remove_row wiring)
  </read_first>
  <action>
    Pass `pruneEvents={playlist.recent_prune_events}` (D-15) to `RebuildBanner`. Provide an `onRemoveRow` handler that calls the existing `useRemovePlaylistRow()` mutation with `{ playlistId: id, seriesId }` and surfaces a `toast` on success/error (reuse the existing sonner toast pattern in this file); the mutation already invalidates the detail query so the modal/banner refresh after removal. Keep the discretion note light: directly invoke the remove mutation from the modal action (no extra confirm dialog required for Phase 11, since the action originates from an explicit diagnostics affordance) — surface a toast error if the row was already removed (stale run, RESEARCH note A3). No other page behavior changes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/pages</automated>
  </verify>
  <done>`PlaylistDetailPage` passes `recent_prune_events` and an `onRemoveRow` handler into the banner; `tsc --noEmit` passes; existing page tests (if any) stay green.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → banner/modal | Operator views resolved diagnostics and triggers a row-removal mutation from the modal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure (XSS) | error_message / reason text in UI | mitigate | Inline error paragraph removed from banner (D-07); all modal text via React text nodes (Plan 04); banner renders no raw HTML |
| T-11-01 | Information Disclosure (IDOR) | remove_row mutation from modal | mitigate | Uses existing owner-gated `removePlaylistRow` DELETE; no new client-trusted id path; stale-row DELETE surfaces a toast error (A3) |
| T-11-SC | Tampering | npm installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/playlists/RebuildBanner.test.tsx src/components/playlists/WritebackStatus.test.tsx` green.
- `cd frontend && npx tsc --noEmit` passes.
- Manual (UI-SPEC): open a playlist with a partial rebuild → "View details" appears at panel bottom; banner shows badges + one-liners only (no bullet lists, no inline error).
</verification>

<success_criteria>
- Single panel-level "View details" trigger gated by D-02 rules; opens the modal (DIAG-01).
- Inline granular lists + failed error paragraph removed from the banner; compact card badges unchanged (DIAG-05, D-05–D-08).
- Prune events + remove-row action wired from the detail page (D-15).
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/components/playlists/RebuildBanner.tsx` (+ test) — trigger + modal host; inline error removed.
- `frontend/src/components/playlists/WritebackStatus.tsx` (+ test) — detail lists stripped; compact preserved.
- `frontend/src/pages/PlaylistDetailPage.tsx` — prune events + onRemoveRow wired.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-sync-rebuild-diagnostics/11-05-SUMMARY.md` when done.
</output>
