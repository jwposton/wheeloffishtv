---
phase: 01-foundations-packaging
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/src/wheeloffish/core/config.py
  - backend/src/wheeloffish/core/logging.py
  - backend/src/wheeloffish/api/routes/health.py
  - backend/src/wheeloffish/main.py
  - backend/tests/test_config.py
  - backend/tests/test_health.py
  - .env.example
autonomous: true
requirements:
  - DEP-01
user_setup: []
must_haves:
  truths:
    - "Application fails fast at startup when WOF_SECRET_KEY is missing"
    - "GET /health returns JSON with status field"
    - "Log output is structured JSON in non-dev mode"
  artifacts:
    - path: backend/src/wheeloffish/core/config.py
      provides: "Settings with WOF_SECRET_KEY required"
      contains: "WOF_SECRET_KEY"
    - path: backend/src/wheeloffish/api/routes/health.py
      provides: "Health endpoint"
      contains: "/health"
    - path: .env.example
      provides: "Documented required env vars"
      contains: "WOF_SECRET_KEY"
  key_links:
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/core/config.py
      via: "settings loaded at startup"
      pattern: "get_settings|Settings"
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/api/routes/health.py
      via: "router include"
      pattern: "health"
---

<objective>
Deliver runnable API configuration, structured JSON logging, and a health endpoint — the first operator-visible backend capability.

Purpose: Satisfies ROADMAP success criterion #1 (health + structured JSON logs) and D-07/D-15 config injection.
Output: `/health` JSON endpoint, structlog JSON baseline, `.env.example`, config fail-fast tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@.planning/phases/01-foundations-packaging/01-RESEARCH.md
@backend/src/wheeloffish/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings with required WOF_SECRET_KEY and DATABASE_URL defaults</name>
  <files>backend/src/wheeloffish/core/config.py, .env.example, backend/tests/test_config.py</files>
  <read_first>backend/src/wheeloffish/main.py, .planning/phases/01-foundations-packaging/01-CONTEXT.md</read_first>
  <action>
Create `backend/src/wheeloffish/core/config.py` using pydantic-settings `BaseSettings`:
- `WOF_SECRET_KEY: str` — required, no default (D-15)
- `DATABASE_URL: str` — default `sqlite:////data/wheeloffish.db` for Docker; document override for Postgres
- `LOG_LEVEL: str` — default `INFO`
- `LOG_FORMAT: str` — default `json`; allow `console` for local dev
- `ENVIRONMENT: str` — default `production`
- `@lru_cache` function `get_settings() -> Settings`
Create `.env.example` at repo root with commented entries for all vars and note: `WOF_SECRET_KEY` generate via `openssl rand -hex 32`
Create `backend/tests/test_config.py`:
- test missing WOF_SECRET_KEY raises ValidationError when env cleared
- test valid hex key loads settings
  </action>
  <verify>cd backend && uv run pytest tests/test_config.py -q</verify>
  <acceptance_criteria>
    - `backend/src/wheeloffish/core/config.py` contains `WOF_SECRET_KEY`
    - `.env.example` contains `WOF_SECRET_KEY=`
    - `pytest tests/test_config.py -k secret_key` exits 0
  </acceptance_criteria>
  <done>Config fail-fast validated by tests</done>
</task>

<task type="auto">
  <name>Task 2: Configure structlog JSON logging and request context middleware</name>
  <files>backend/src/wheeloffish/core/logging.py, backend/src/wheeloffish/main.py</files>
  <read_first>backend/src/wheeloffish/core/config.py, .planning/phases/01-foundations-packaging/01-RESEARCH.md</read_first>
  <action>
Create `backend/src/wheeloffish/core/logging.py`:
- `configure_logging(settings: Settings)` using structlog
- When `LOG_FORMAT=json`: use `JSONRenderer` with ISO timestamp and log level
- When `LOG_FORMAT=console`: use ConsoleRenderer for local dev
- Export `get_logger(name: str)` wrapper
Update `main.py`:
- Call `configure_logging(get_settings())` on startup (lifespan or `@app.on_event("startup")`)
- Add middleware binding `request_id` (uuid4) per request via structlog contextvars; log one line per request: method, path, status_code, duration_ms
  </action>
  <verify>cd backend && WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef uv run python -c "from wheeloffish.core.logging import configure_logging; from wheeloffish.core.config import Settings; configure_logging(Settings(WOF_SECRET_KEY='0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef'))"</verify>
  <acceptance_criteria>
    - `backend/src/wheeloffish/core/logging.py` contains `JSONRenderer`
    - `backend/src/wheeloffish/main.py` calls `configure_logging`
    - `backend/src/wheeloffish/main.py` contains `request_id`
  </acceptance_criteria>
  <done>Structured JSON logging configured</done>
</task>

<task type="auto">
  <name>Task 3: Add /health route and wire router into main app</name>
  <files>backend/src/wheeloffish/api/routes/health.py, backend/src/wheeloffish/main.py, backend/tests/test_health.py</files>
  <read_first>backend/src/wheeloffish/main.py, backend/tests/conftest.py</read_first>
  <action>
Create `backend/src/wheeloffish/api/routes/health.py`:
- `APIRouter` with prefix="" (health at root `/health` per RESEARCH open question resolution)
- `GET /health` returns JSON: `{"status": "ok", "service": "wheeloffish", "version": "<from __init__>", "checks": {"api": "ok"}}`
- HTTP 200 always when process alive (DB check added in plan 01-04)
Update `main.py` to include health router and load settings before serving
Create `backend/tests/test_health.py` using `httpx.AsyncClient` or `TestClient`:
- with WOF_SECRET_KEY set in env/monkeypatch, GET `/health` returns 200 and body contains `"status": "ok"`
Update `conftest.py` with `settings` fixture providing valid 64-char hex WOF_SECRET_KEY
  </action>
  <verify>cd backend && uv run pytest tests/test_health.py -q</verify>
  <acceptance_criteria>
    - `backend/src/wheeloffish/api/routes/health.py` contains `@router.get("/health"`
    - `pytest tests/test_health.py` exits 0
    - Response JSON includes keys `status`, `service`, `checks`
  </acceptance_criteria>
  <done>Health endpoint testable without Docker</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/test_config.py tests/test_health.py -q` exits 0
- [ ] `.env.example` documents all required vars
- [ ] Logs emit JSON when LOG_FORMAT=json
</verification>

<success_criteria>
- Health endpoint returns structured JSON
- WOF_SECRET_KEY fail-fast proven by test
- Structured logging baseline in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-packaging/01-02-SUMMARY.md`
</output>
