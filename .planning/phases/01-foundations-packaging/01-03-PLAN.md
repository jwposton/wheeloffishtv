---
phase: 01-foundations-packaging
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/src/wheeloffish/db/session.py
  - backend/src/wheeloffish/db/models/base.py
  - backend/src/wheeloffish/db/models/app_metadata.py
  - backend/src/wheeloffish/db/models/secret.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/001_foundation.py
  - backend/tests/test_db.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - DEP-01
must_haves:
  truths:
    - "Alembic upgrade head creates app_metadata and secrets tables"
    - "SQLite WAL mode is enabled on connection"
    - "A write/read round-trip to app_metadata succeeds in tests"
  artifacts:
    - path: backend/alembic/versions/001_foundation.py
      provides: "First migration with foundation tables"
      contains: "app_metadata"
    - path: backend/src/wheeloffish/db/models/secret.py
      provides: "Secret ORM model"
      contains: "class Secret"
  key_links:
    - from: backend/alembic/env.py
      to: backend/src/wheeloffish/db/models/
      via: "target_metadata import"
      pattern: "target_metadata"
    - from: backend/src/wheeloffish/db/session.py
      to: SQLite file path
      via: "DATABASE_URL from settings"
      pattern: "create_engine|sessionmaker"
---

<objective>
Implement SQLAlchemy database layer, Alembic migrations, and foundation schema proving DB round-trip.

Purpose: ROADMAP success criterion #2 and D-01/D-03/D-04.
Output: `app_metadata` + `secrets` tables, WAL-enabled SQLite session, migration revision `001_foundation`, DB tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@.planning/phases/01-foundations-packaging/01-RESEARCH.md
@backend/src/wheeloffish/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQLAlchemy models and session factory with WAL pragma</name>
  <files>backend/src/wheeloffish/db/session.py, backend/src/wheeloffish/db/models/base.py, backend/src/wheeloffish/db/models/app_metadata.py, backend/src/wheeloffish/db/models/secret.py</files>
  <read_first>backend/src/wheeloffish/core/config.py, .planning/phases/01-foundations-packaging/01-CONTEXT.md</read_first>
  <action>
Create SQLAlchemy 2.0 declarative base in `db/models/base.py`.
Model `AppMetadata` in `app_metadata.py`:
- columns: `id` (PK int), `schema_version` (str), `install_id` (str UUID), `created_at` (datetime)
Model `Secret` in `secret.py`:
- columns: `id` (PK int), `namespace` (str indexed), `key` (str), `ciphertext` (bytes/text), `created_at`, `updated_at`
- unique constraint on (`namespace`, `key`)
Create `db/session.py`:
- `get_engine(settings)` using `create_engine(settings.DATABASE_URL, connect_args for sqlite check_same_thread=False)`
- `@event.listens_for(Engine, "connect")` for SQLite: execute `PRAGMA journal_mode=WAL`
- `SessionLocal = sessionmaker(...)` and `get_db()` generator for FastAPI Depends
  </action>
  <verify>cd backend && WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef uv run python -c "from wheeloffish.db.models.base import Base; from wheeloffish.db.models import app_metadata, secret; print(list(Base.metadata.tables.keys()))"</verify>
  <acceptance_criteria>
    - `app_metadata` and `secrets` table names in Base.metadata
    - `db/session.py` contains `journal_mode=WAL`
    - `Secret` model has `namespace` and `ciphertext` columns
  </acceptance_criteria>
  <done>ORM models and session factory defined</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Alembic and create 001_foundation migration</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/001_foundation.py</files>
  <read_first>backend/src/wheeloffish/db/models/base.py, .planning/phases/01-foundations-packaging/01-RESEARCH.md</read_first>
  <action>
Initialize Alembic under `backend/alembic/`:
- `alembic.ini` with `script_location = alembic`, sqlalchemy.url from env override in env.py (read DATABASE_URL from settings, not hardcoded)
- `env.py` imports `Base.metadata` as `target_metadata`
Create revision `001_foundation.py`:
- `upgrade()`: create `app_metadata` and `secrets` tables matching models
- `downgrade()`: drop both tables
- Insert seed row into `app_metadata` with generated `install_id` and `schema_version='001'`
Use portable column types (String, DateTime, LargeBinary or Text for ciphertext) — no SQLite-only constructs that break Postgres
  </action>
  <verify>cd backend && WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef DATABASE_URL=sqlite:///./test.db uv run alembic upgrade head</verify>
  <acceptance_criteria>
    - `backend/alembic/versions/001_foundation.py` exists
    - `alembic upgrade head` exits 0 against temp sqlite file
    - Tables `app_metadata` and `secrets` exist after upgrade (verify via sqlite3 or SQLAlchemy inspect)
  </acceptance_criteria>
  <done>First migration applies cleanly</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 3: [BLOCKING] Verify migration round-trip and add DB integration tests</name>
  <files>backend/tests/test_db.py, backend/tests/conftest.py</files>
  <read_first>backend/alembic/versions/001_foundation.py, backend/src/wheeloffish/db/session.py</read_first>
  <action>
Update `conftest.py`:
- `db_engine` fixture: temp SQLite file URL, run `alembic upgrade head` before tests
- `db_session` fixture: yield SessionLocal, rollback after test
Create `tests/test_db.py`:
- `test_wal_mode_enabled`: connect and query `PRAGMA journal_mode` equals `wal`
- `test_app_metadata_round_trip`: insert/read AppMetadata row
- `test_migration_creates_tables`: inspect has both tables
Run full migration upgrade as blocking verification step before marking plan complete.
  </action>
  <verify>cd backend && uv run pytest tests/test_db.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_db.py` exits 0
    - `test_wal_mode_enabled` passes
    - `test_app_metadata_round_trip` passes
  </acceptance_criteria>
  <done>DB round-trip proven by automated tests</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run alembic upgrade head` (with test DATABASE_URL) exits 0
- [ ] `cd backend && uv run pytest tests/test_db.py -q` exits 0
- [ ] Migration uses portable SQLAlchemy types for Postgres CI compatibility
</verification>

<success_criteria>
- Foundation tables exist after migration
- WAL mode enabled
- DB tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-packaging/01-03-SUMMARY.md`
</output>
