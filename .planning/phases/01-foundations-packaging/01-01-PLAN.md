---
phase: 01-foundations-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/src/wheeloffish/__init__.py
  - backend/src/wheeloffish/main.py
  - backend/tests/conftest.py
  - frontend/README.md
  - README.md
autonomous: true
requirements:
  - DEP-01
user_setup:
  - service: local-dev
    why: "WOF_SECRET_KEY must be generated once per install"
    env_vars:
      - name: WOF_SECRET_KEY
        source: "Run: openssl rand -hex 32 — copy into .env"
must_haves:
  truths:
    - "Monorepo layout exists with backend/ and frontend/ placeholder"
    - "uv sync installs dependencies from lockfile without error"
    - "Ruff and pytest are configured and runnable"
  artifacts:
    - path: backend/pyproject.toml
      provides: "Python project definition with wheeloffish package"
      contains: "wheeloffish"
    - path: backend/src/wheeloffish/main.py
      provides: "FastAPI app entry stub"
      contains: "FastAPI"
    - path: frontend/README.md
      provides: "Phase 3 placeholder"
      contains: "Phase 3"
  key_links:
    - from: backend/pyproject.toml
      to: backend/src/wheeloffish/
      via: "package discovery"
      pattern: "wheeloffish"
---

<objective>
Establish the monorepo skeleton, uv-based Python packaging, and test/lint tooling baseline.

Purpose: Every subsequent plan inherits directory layout and tooling from this foundation (D-09, D-10, D-11).
Output: Runnable `uv sync`, empty FastAPI stub, pytest/ruff config, frontend placeholder, root README stub.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@.planning/phases/01-foundations-packaging/01-RESEARCH.md
@.planning/phases/01-foundations-packaging/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo directory layout and frontend placeholder</name>
  <files>frontend/README.md, README.md, backend/src/wheeloffish/__init__.py</files>
  <read_first>.planning/phases/01-foundations-packaging/01-CONTEXT.md, .planning/phases/01-foundations-packaging/01-RESEARCH.md</read_first>
  <action>
Create directory structure per D-09/D-11:
- `backend/src/wheeloffish/` with subpackages: `api/`, `core/`, `db/`, `integrations/` (each with `__init__.py`)
- `backend/tests/`
- `frontend/README.md` stating "Frontend SPA deferred to Phase 3 — React/Vite stack"
- Root `README.md` with project title "Wheel of Fish TV", one-line description, and placeholder sections: `## Quickstart`, `## Configuration`, `## Backup` (content filled in plan 01-05)
- `backend/src/wheeloffish/__init__.py` with `__version__ = "0.1.0"`
  </action>
  <verify>test -d backend/src/wheeloffish/api && test -f frontend/README.md</verify>
  <acceptance_criteria>
    - `backend/src/wheeloffish/api/__init__.py` exists
    - `backend/src/wheeloffish/core/__init__.py` exists
    - `backend/src/wheeloffish/db/__init__.py` exists
    - `backend/src/wheeloffish/integrations/__init__.py` exists
    - `frontend/README.md` contains "Phase 3"
    - Root `README.md` contains "## Quickstart"
  </acceptance_criteria>
  <done>Monorepo layout matches CONTEXT.md D-09 and D-11</done>
</task>

<task type="auto">
  <name>Task 2: Configure uv pyproject.toml with dependencies and tooling</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>.planning/phases/01-foundations-packaging/01-RESEARCH.md, backend/src/wheeloffish/__init__.py</read_first>
  <action>
Create `backend/pyproject.toml`:
- `[project]` name=`wheeloffish`, version=`0.1.0`, requires-python=`>=3.12`
- Dependencies: fastapi, uvicorn[standard], pydantic-settings, sqlalchemy, alembic, structlog, cryptography, httpx
- `[project.scripts]` or document uvicorn entry via module path `wheeloffish.main:app`
- `[build-system]` hatchling or setuptools with src layout pointing to `src/`
- `[tool.uv]` dev-dependencies: pytest, pytest-asyncio, ruff
- `[tool.ruff]` line-length 100, select E,W,F,I,UP
- `[tool.ruff.format]` quote-style double
- `[tool.pytest.ini_options]` testpaths=`["tests"]`, asyncio_mode=`auto`
Run `cd backend && uv lock && uv sync` to generate `uv.lock`.
  </action>
  <verify>cd backend && uv run python -c "import wheeloffish; print(wheeloffish.__version__)"</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `wheeloffish` and `fastapi`
    - `backend/uv.lock` exists
    - `cd backend && uv run ruff --version` exits 0
    - `cd backend && uv run pytest --version` exits 0
  </acceptance_criteria>
  <done>uv sync succeeds; package importable</done>
</task>

<task type="auto">
  <name>Task 3: Add FastAPI stub and pytest conftest scaffold</name>
  <files>backend/src/wheeloffish/main.py, backend/tests/conftest.py, backend/tests/test_smoke.py</files>
  <read_first>backend/pyproject.toml, backend/src/wheeloffish/__init__.py</read_first>
  <action>
- `backend/src/wheeloffish/main.py`: create `app = FastAPI(title="Wheel of Fish TV")` with root route returning `{"service": "wheeloffish", "status": "ok"}`
- `backend/tests/conftest.py`: empty fixture scaffold with comment placeholders for settings/db fixtures (filled in plans 02-03)
- `backend/tests/test_smoke.py`: single test importing `wheeloffish.main.app` and asserting `app.title == "Wheel of Fish TV"`
  </action>
  <verify>cd backend && uv run pytest tests/test_smoke.py -q</verify>
  <acceptance_criteria>
    - `backend/src/wheeloffish/main.py` contains `app = FastAPI`
    - `cd backend && uv run pytest tests/test_smoke.py` exits 0
    - `cd backend && uv run ruff check .` exits 0
  </acceptance_criteria>
  <done>Minimal app importable; smoke test green; ruff clean</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv sync` exits 0
- [ ] `cd backend && uv run ruff check .` exits 0
- [ ] `cd backend && uv run pytest -q` exits 0
- [ ] Monorepo directories match RESEARCH.md structure
</verification>

<success_criteria>
- All tasks completed
- uv lockfile committed
- Package layout ready for plans 02-05
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-packaging/01-01-SUMMARY.md`
</output>
