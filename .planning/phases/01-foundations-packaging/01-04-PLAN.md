---
phase: 01-foundations-packaging
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - backend/src/wheeloffish/core/secrets.py
  - backend/src/wheeloffish/core/namespaces.py
  - backend/src/wheeloffish/api/routes/health.py
  - backend/src/wheeloffish/integrations/plex.py
  - backend/src/wheeloffish/integrations/jellyfin.py
  - backend/tests/test_secrets.py
autonomous: true
requirements:
  - INT-01
must_haves:
  truths:
    - "Vault get/set/delete round-trips a secret without storing plaintext in DB"
    - "store_media_token(connection_id, token) persists retrievable encrypted value"
    - "Health endpoint reports database check status"
    - "Plex and Jellyfin stub modules exist with documented Phase 2 interfaces"
  artifacts:
    - path: backend/src/wheeloffish/core/secrets.py
      provides: "Encrypted vault CRUD"
      contains: "def get_secret"
    - path: backend/src/wheeloffish/core/namespaces.py
      provides: "Namespace constants"
      contains: "media_server"
    - path: backend/src/wheeloffish/integrations/plex.py
      provides: "Plex client stub"
      contains: "Phase 2"
  key_links:
    - from: backend/src/wheeloffish/core/secrets.py
      to: backend/src/wheeloffish/db/models/secret.py
      via: "SQLAlchemy session write/read"
      pattern: "Secret\\("
    - from: backend/src/wheeloffish/api/routes/health.py
      to: backend/src/wheeloffish/db/session.py
      via: "DB ping in checks.database"
      pattern: "checks.*database"
---

<objective>
Implement encrypted secrets vault with namespaced CRUD, typed media-token helpers, DB-aware health check, and integration module stubs.

Purpose: ROADMAP success criterion #3 and INT-01 structural surfaces (D-13 through D-16).
Output: Fernet-backed vault, no HTTP secret routes, Plex/Jellyfin placeholders, vault + health tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@.planning/phases/01-foundations-packaging/01-RESEARCH.md
@backend/src/wheeloffish/db/models/secret.py
@backend/src/wheeloffish/core/config.py
@backend/src/wheeloffish/api/routes/health.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Fernet vault service with namespaced CRUD</name>
  <files>backend/src/wheeloffish/core/secrets.py, backend/src/wheeloffish/core/namespaces.py</files>
  <read_first>backend/src/wheeloffish/db/models/secret.py, backend/src/wheeloffish/core/config.py, .planning/phases/01-foundations-packaging/01-RESEARCH.md</read_first>
  <action>
Create `core/namespaces.py`:
- `MEDIA_SERVER_NS = "media_server"`
- helper `media_server_token_key(connection_id: str) -> str` returning `f"media_server/{connection_id}/token"`
Create `core/secrets.py` class `SecretsVault`:
- init with Settings (derive Fernet from WOF_SECRET_KEY hex via base64.urlsafe_b64encode pattern from RESEARCH.md)
- `get_secret(namespace: str, key: str) -> str | None` — decrypt ciphertext
- `set_secret(namespace: str, key: str, value: str) -> None` — encrypt then upsert
- `delete_secret(namespace: str, key: str) -> bool`
- Typed helpers: `store_media_token(connection_id, token)`, `get_media_token(connection_id)`, `delete_media_token(connection_id)`
Assert stored `ciphertext` in DB is NOT equal to plaintext value (encrypt before persist, D-14)
No FastAPI routes for secrets (D-16)
  </action>
  <verify>cd backend && uv run pytest tests/test_secrets.py -q 2>/dev/null || echo "tests written in task 2"</verify>
  <acceptance_criteria>
    - `core/secrets.py` contains `get_secret`, `set_secret`, `delete_secret`
    - `core/secrets.py` contains `store_media_token`
    - `core/namespaces.py` contains `media_server`
    - Fernet import from `cryptography.fernet`
  </acceptance_criteria>
  <done>Vault API implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add vault tests proving encryption and CRUD round-trip</name>
  <files>backend/tests/test_secrets.py</files>
  <read_first>backend/src/wheeloffish/core/secrets.py, backend/tests/conftest.py</read_first>
  <action>
Create `tests/test_secrets.py`:
- `test_set_get_delete_round_trip`: store value, retrieve, delete, assert None after delete
- `test_ciphertext_not_plaintext`: after set_secret, query DB row directly — `ciphertext` must not contain plaintext token string
- `test_store_media_token_helper`: uses connection_id `test-conn-1`, token `plex-token-xyz`, retrieves via get_media_token
- `test_wrong_key_returns_none`: get missing key returns None
Use db_session fixture from conftest; valid 64-char hex WOF_SECRET_KEY
  </action>
  <verify>cd backend && uv run pytest tests/test_secrets.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_secrets.py` exits 0
    - `test_ciphertext_not_plaintext` passes
    - `test_store_media_token_helper` passes
  </acceptance_criteria>
  <done>INT-01 vault behavior verified by tests</done>
</task>

<task type="auto">
  <name>Task 3: Extend health check with DB probe and add integration stubs</name>
  <files>backend/src/wheeloffish/api/routes/health.py, backend/src/wheeloffish/integrations/plex.py, backend/src/wheeloffish/integrations/jellyfin.py, backend/tests/test_health.py</files>
  <read_first>backend/src/wheeloffish/db/session.py, backend/src/wheeloffish/api/routes/health.py</read_first>
  <action>
Update `/health` response checks to include:
- `"database": "ok"` when `SELECT 1` succeeds against session
- `"database": "error"` with HTTP 503 if DB unreachable
- include `schema_version` from app_metadata if row exists
Create `integrations/plex.py`:
- module docstring: "Phase 2 — Plex API client stub"
- class `PlexClient` with methods `connect()`, `list_libraries()`, `list_shows()` raising `NotImplementedError` with message referencing Phase 2
Create `integrations/jellyfin.py` with parallel `JellyfinClient` stub
Update `test_health.py` to assert `checks.database` equals `ok` when DB fixture active
  </action>
  <verify>cd backend && uv run pytest tests/test_health.py tests/test_secrets.py -q</verify>
  <acceptance_criteria>
    - `health.py` response includes `checks.database`
    - `integrations/plex.py` contains `NotImplementedError` and `Phase 2`
    - `integrations/jellyfin.py` contains `JellyfinClient`
    - `pytest tests/test_health.py` exits 0
  </acceptance_criteria>
  <done>Health reflects DB; integration stubs in place</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/test_secrets.py tests/test_health.py -q` exits 0
- [ ] No HTTP routes expose vault operations (grep `/secret` in api/routes returns nothing)
- [ ] Ciphertext not stored as plaintext
</verification>

<success_criteria>
- Vault CRUD with encryption at rest
- Typed media token helpers available for Phase 2
- Integration stubs documented
- Health includes DB status
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-packaging/01-04-SUMMARY.md`
</output>
