---
phase: 01-foundations-packaging
plan: 05
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - backend/Dockerfile
  - compose.yml
  - compose.override.yml.example
  - backend/docker-entrypoint.sh
  - .github/workflows/ci.yml
  - README.md
  - .gitignore
autonomous: true
requirements:
  - DEP-01
user_setup:
  - service: docker
    why: "Operator must copy .env.example to .env and set WOF_SECRET_KEY before compose up"
    env_vars:
      - name: WOF_SECRET_KEY
        source: "openssl rand -hex 32"
must_haves:
  truths:
    - "docker compose up --wait exits 0 with healthy app container"
    - "No host ports published in default compose.yml (healthcheck-only per D-06)"
    - "CI runs Ruff, pytest, Docker build; main branch runs Postgres profile smoke"
    - "README documents quickstart, WOF_SECRET_KEY generation, and SQLite backup"
  artifacts:
    - path: compose.yml
      provides: "API-only Compose stack"
      contains: "healthcheck"
    - path: backend/Dockerfile
      provides: "Multi-stage uv-based image"
      contains: "uv sync"
    - path: .github/workflows/ci.yml
      provides: "CI pipeline"
      contains: "ruff"
    - path: README.md
      provides: "Operator quickstart"
      contains: "openssl rand -hex 32"
  key_links:
    - from: compose.yml
      to: backend/Dockerfile
      via: "build context"
      pattern: "build:"
    - from: backend/docker-entrypoint.sh
      to: alembic
      via: "alembic upgrade head before uvicorn"
      pattern: "alembic upgrade head"
---

<objective>
Containerize the API, ship Compose stack with healthcheck-only exposure, CI smoke pipeline, and operator README.

Purpose: DEP-01 delivery, D-04/D-05/D-06/D-07/D-08/D-12 completion.
Output: Dockerfile, compose.yml (+ override example), entrypoint running migrations, GitHub Actions CI, complete README.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@.planning/phases/01-foundations-packaging/01-RESEARCH.md
@.planning/phases/01-foundations-packaging/SKELETON.md
@backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-stage Dockerfile and entrypoint with Alembic upgrade</name>
  <files>backend/Dockerfile, backend/docker-entrypoint.sh, .gitignore</files>
  <read_first>backend/pyproject.toml, .planning/phases/01-foundations-packaging/01-RESEARCH.md</read_first>
  <action>
Create `backend/Dockerfile` multi-stage:
- builder: copy from `ghcr.io/astral-sh/uv`, run `uv sync --frozen --no-dev`
- runtime: python:3.12-slim, non-root user `app` (uid 1001), copy `.venv` and source
- ENV: PYTHONUNBUFFERED=1, PATH includes venv
- EXPOSE 8000 (internal; no compose ports publish)
- HEALTHCHECK CMD curling localhost:8000/health (urllib or curl)
Create `backend/docker-entrypoint.sh`:
- `set -e`
- `alembic upgrade head`
- `exec uvicorn wheeloffish.main:app --host 0.0.0.0 --port 8000`
Update `.gitignore`: `.env`, `backend/.venv`, `backend/test.db`, `data/`, `__pycache__`
  </action>
  <verify>docker build -t wheeloffish:test backend</verify>
  <acceptance_criteria>
    - `backend/Dockerfile` contains `uv sync`
    - `backend/docker-entrypoint.sh` contains `alembic upgrade head`
    - `docker build -t wheeloffish:test backend` exits 0
  </acceptance_criteria>
  <done>Docker image builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Author compose.yml with SQLite volume, env defaults, and postgres profile</name>
  <files>compose.yml, compose.override.yml.example</files>
  <read_first>.planning/phases/01-foundations-packaging/01-CONTEXT.md, backend/Dockerfile</read_first>
  <action>
Create root `compose.yml`:
- service `app`: build `backend/`, no `ports:` section (D-06 internal-only)
- `environment:` defaults: LOG_FORMAT=json, DATABASE_URL=sqlite:////data/wheeloffish.db, ENVIRONMENT=production
- `env_file: .env` for WOF_SECRET_KEY override (D-07)
- volume `wof_data:/data` mounted at `/data`
- healthcheck matching RESEARCH.md pattern (urllib to 127.0.0.1:8000/health)
- profiles.postgres: add `postgres` service (postgres:16-alpine), set app DATABASE_URL to postgresql://wof:wof@postgres:5432/wof — for CI smoke only (D-08)
Create `compose.override.yml.example`:
- bind mount `./data:/data` for host backup (D-04)
- comment explaining copy to `compose.override.yml`
  </action>
  <verify>WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef docker compose up --wait --build 2>&1; docker compose down -v</verify>
  <acceptance_criteria>
    - `compose.yml` contains `healthcheck`
    - `compose.yml` does NOT contain `ports:` under app service
    - `compose.yml` contains `profiles:` with `postgres`
    - `compose.override.yml.example` contains `./data:/data`
    - `docker compose up --wait` exits 0 when WOF_SECRET_KEY provided
  </acceptance_criteria>
  <done>Compose stack healthy without host port publish</done>
</task>

<task type="auto">
  <name>Task 3: Add GitHub Actions CI and complete README quickstart</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>.planning/ROADMAP.md, .env.example, compose.yml</read_first>
  <action>
Create `.github/workflows/ci.yml`:
- triggers: push/PR to main
- job `lint-test`: checkout, install uv, `cd backend && uv sync && uv run ruff check . && uv run pytest`
- job `docker-smoke`: build image, copy `.env.example` to `.env`, inject test WOF_SECRET_KEY, `docker compose up --wait`, `docker compose down -v`
- job `postgres-smoke`: `if: github.ref == 'refs/heads/main'`, run `docker compose --profile postgres up --wait`, verify health, tear down (D-02)
Expand root `README.md`:
- Prerequisites: Docker, Docker Compose
- Quickstart: copy `.env.example` → `.env`, generate key `openssl rand -hex 32`, `docker compose up --wait`, verify with `docker compose ps` showing healthy
- Configuration table for env vars
- Backup section: stop container, copy volume or bind-mounted `./data/wheeloffish.db` (D-04)
- PostgreSQL upgrade path note (change DATABASE_URL, run migrations)
- Explicit note: no bundled reverse proxy; external proxy upstream in Phase 3+
  </action>
  <verify>cd backend && uv run ruff check . && uv run pytest -q</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `ruff check`
    - `.github/workflows/ci.yml` contains `postgres-smoke` or `--profile postgres`
    - `README.md` contains `openssl rand -hex 32`
    - `README.md` contains `docker compose up`
    - `README.md` contains `Backup`
  </acceptance_criteria>
  <done>CI workflow and operator docs complete</done>
</task>

</tasks>

<verification>
- [ ] `docker build -t wheeloffish:test backend` exits 0
- [ ] `docker compose up --wait` exits 0 with valid `.env`
- [ ] `cd backend && uv run ruff check . && uv run pytest` exits 0
- [ ] README quickstart steps are accurate
</verification>

<success_criteria>
- DEP-01 fully satisfied: Docker artifacts runnable with documented defaults
- CI baseline green locally
- Operator can deploy without reading source code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-packaging/01-05-SUMMARY.md`
</output>
