---
phase: 07-provider-playlist-writeback
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - backend/src/wheeloffish/integrations/jellyfin/playlists.py
  - backend/src/wheeloffish/core/provider_writeback.py
  - backend/tests/unit/test_jellyfin_playlist_client.py
  - backend/tests/unit/test_provider_writeback_jellyfin.py
autonomous: true
requirements:
  - EXP-01
tags:
  - phase-7
  - jellyfin
  - writeback
must_haves:
  truths:
    - "JellyfinProvider playlist create/replace/rename/delete mirrors Plex semantics (D-19)"
    - "Jellyfin episode composite native IDs used directly as media item ids for POST /Playlists/{id}/Items"
    - "push_snapshot no longer skips Jellyfin connections — same partial/succeeded/failed rules as Plex (D-13, D-14)"
    - "provider_kind=jellyfin stored on first successful writeback"
  artifacts:
    - path: backend/src/wheeloffish/integrations/jellyfin/playlists.py
      provides: "Jellyfin playlist CRUD + replace_items"
      contains: "async def replace_playlist_items"
    - path: backend/tests/unit/test_jellyfin_playlist_client.py
      provides: "Mocked Jellyfin playlist tests"
      contains: "test_replace_playlist_items"
  key_links:
    - from: backend/src/wheeloffish/core/provider_writeback.py
      to: backend/src/wheeloffish/integrations/jellyfin/playlists.py
      via: "provider_kind dispatch"
      pattern: "JellyfinProvider"
---

## Phase Goal

**As a** Jellyfin operator, **I want** the same automatic playlist writeback as Plex users, **so that** v0.1.0 works regardless of configured provider (EXP-01, D-03).

<objective>
Wave 2 — Jellyfin playlist client + wire into existing `push_snapshot`. Ship before v0.1.0 tag if API straightforward; otherwise document as blocker in SUMMARY.

Output: jellyfin/playlists.py, provider_writeback Jellyfin branch, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-provider-playlist-writeback/07-CONTEXT.md
@.planning/phases/07-provider-playlist-writeback/07-RESEARCH.md
@backend/src/wheeloffish/core/provider_writeback.py
@backend/src/wheeloffish/integrations/jellyfin/client.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-02-01 | Tampering | Jellyfin entryIds vs media ids | mitigate | Use GET Items response entry ids for DELETE; media ids for POST add |
| T-07-02-02 | Elevation of Privilege | userId param | mitigate | Always pass linked Jellyfin user_id from provider instance |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Jellyfin playlist client module (D-19)</name>
  <files>backend/src/wheeloffish/integrations/jellyfin/playlists.py, backend/tests/unit/test_jellyfin_playlist_client.py</files>
  <read_first>backend/src/wheeloffish/integrations/jellyfin/client.py, .planning/phases/07-provider-playlist-writeback/07-RESEARCH.md</read_first>
  <action>
Create `integrations/jellyfin/playlists.py`:

- Reuse `provider_playlist_display_name()` from plex module or move to shared `integrations/playlist_names.py` if cleaner
- `async def create_playlist(provider: JellyfinProvider, title: str, media_item_ids: list[str]) -> str` → playlist Id
- `async def list_playlist_entry_ids(provider, playlist_id) -> list[str]`
- `async def clear_playlist_items(provider, playlist_id) -> None` — DELETE with entryIds
- `async def add_playlist_items(provider, playlist_id, media_item_ids: list[str]) -> None`
- `async def replace_playlist_items(provider, playlist_id, media_item_ids: list[str]) -> None`
- `async def rename_playlist(provider, playlist_id, title: str) -> None` via UpdatePlaylistDto
- `async def delete_playlist(provider, playlist_id) -> None`
- `def episode_native_id(episode_composite_id: str) -> str` — parse_composite_id native part

Mock tests for create + replace flow (clear uses entry ids from fixture GET response).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_jellyfin_playlist_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - replace_playlist_items clears then adds in order
    - Tests pass without live Jellyfin server
  </acceptance_criteria>
  <done>Jellyfin playlist client ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Jellyfin into push_snapshot</name>
  <files>backend/src/wheeloffish/core/provider_writeback.py, backend/tests/unit/test_provider_writeback_jellyfin.py</files>
  <read_first>backend/src/wheeloffish/core/provider_writeback.py, backend/src/wheeloffish/integrations/jellyfin/playlists.py</read_first>
  <action>
Refactor `push_snapshot` to dispatch on connection provider type:

- `plex` → existing 07-01 path
- `jellyfin` → Jellyfin replace/create with episode native ids
- Unknown → `skipped` with error reason

Add unit tests mirroring Plex partial/succeeded/failed cases for Jellyfin.

Remove any temporary "skip jellyfin" branch from 07-01 if present.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_provider_writeback_jellyfin.py tests/unit/test_provider_writeback.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Jellyfin connection no longer returns writeback_status skipped by default
    - provider_kind=jellyfin persisted on create
  </acceptance_criteria>
  <done>Both providers supported for rebuild writeback.</done>
</task>

</tasks>

## Wave verification

```bash
cd backend && uv run pytest tests/unit/test_jellyfin_playlist_client.py tests/unit/test_provider_writeback_jellyfin.py -q
```

## Plan completion checklist

- [ ] Jellyfin replace_playlist_items tested
- [ ] push_snapshot handles both providers
- [ ] No regressions in Plex writeback tests
