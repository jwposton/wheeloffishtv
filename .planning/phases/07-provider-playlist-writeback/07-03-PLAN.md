---
phase: 07-provider-playlist-writeback
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - backend/src/wheeloffish/core/provider_writeback.py
  - backend/src/wheeloffish/core/provider_playlist_urls.py
  - backend/src/wheeloffish/api/routes/playlists.py
  - backend/src/wheeloffish/api/schemas/playlists.py
  - backend/tests/integration/test_playlists_api.py
  - backend/tests/integration/test_playlist_writeback_lifecycle.py
  - frontend/src/api/playlists.ts
  - frontend/src/api/types.ts
  - frontend/src/components/playlists/WritebackStatus.tsx
  - frontend/src/components/playlists/PlaylistCard.tsx
  - frontend/src/components/playlists/RebuildBanner.tsx
  - frontend/src/pages/PlaylistDetailPage.tsx
  - frontend/src/pages/PlaylistsPage.test.tsx
  - frontend/src/pages/PlaylistDetailPage.test.tsx
  - .planning/phases/07-provider-playlist-writeback/07-VALIDATION.md
  - .planning/phases/07-provider-playlist-writeback/07-UAT.md
autonomous: true
requirements:
  - EXP-01
  - WEB-01
tags:
  - phase-7
  - lifecycle
  - ui
  - uat
must_haves:
  truths:
    - "PATCH playlist name renames linked provider playlist to '{new_name} [wof]' (D-09)"
    - "DELETE playlist removes linked provider playlist best-effort (D-11)"
    - "List + detail API expose provider_playlist_open_url when linked (D-17)"
    - "PlaylistCard and PlaylistDetailPage show writeback status separate from rebuild status (D-16)"
    - "Open in Plex/Jellyfin link renders when provider_playlist_open_url present (D-17)"
  artifacts:
    - path: backend/src/wheeloffish/core/provider_playlist_urls.py
      provides: "Deep link URL builders"
      contains: "def provider_playlist_open_url"
    - path: frontend/src/components/playlists/WritebackStatus.tsx
      provides: "Writeback badge + external link"
      contains: "export function WritebackStatus"
    - path: .planning/phases/07-provider-playlist-writeback/07-UAT.md
      provides: "Manual UAT checklist for v0.1.0 gate"
      contains: "Sync to Plex"
  key_links:
    - from: backend/src/wheeloffish/api/routes/playlists.py
      to: backend/src/wheeloffish/core/provider_writeback.py
      via: "rename_linked / delete_linked on PATCH/DELETE"
      pattern: "rename_linked"
    - from: frontend/src/components/playlists/RebuildBanner.tsx
      to: frontend/src/components/playlists/WritebackStatus.tsx
      via: "renders below rebuild status"
      pattern: "WritebackStatus"
---

## Phase Goal

**As a** self-host operator, **I want** writeback status visible in the UI and provider playlists kept in sync when I rename or delete, **so that** I trust v0.1.0 for daily use (D-09, D-11, D-16, D-17).

<objective>
Wave 3 — lifecycle hooks, open URLs, SPA status/link components, validation docs. Completes EXP-01 + WEB-01 writeback slice for v0.1.0.

Output: rename/delete sync, WritebackStatus UI, UAT checklist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-provider-playlist-writeback/07-CONTEXT.md
@.planning/phases/07-provider-playlist-writeback/07-RESEARCH.md
@backend/src/wheeloffish/api/routes/playlists.py
@frontend/src/components/playlists/RebuildBanner.tsx
@frontend/src/pages/PlaylistDetailPage.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-03-01 | Tampering | open URL | mitigate | Build URLs server-side from known connection.base_url only |
| T-07-03-02 | Denial of Service | delete blocking | mitigate | Best-effort provider delete; log failure; still delete local row |
| T-07-03-03 | XSS | writeback_error in UI | mitigate | Render as plain text only (same as RebuildBanner error_message) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle sync — rename + delete (D-09, D-11)</name>
  <files>backend/src/wheeloffish/core/provider_writeback.py, backend/src/wheeloffish/api/routes/playlists.py, backend/tests/integration/test_playlist_writeback_lifecycle.py</files>
  <read_first>backend/src/wheeloffish/api/routes/playlists.py, backend/src/wheeloffish/core/connections.py</read_first>
  <action>
Add to `provider_writeback.py`:

- `async def rename_linked(db, playlist, provider) -> None` — if provider_playlist_id set, rename to `{playlist.name} [wof]`
- `async def delete_linked(db, playlist, provider) -> None` — delete provider playlist; swallow/log errors

Hook routes:
- `update_playlist`: after name change + commit, if name changed and provider_playlist_id present, build provider and call rename_linked (async — use asyncio.run or existing async pattern in routes; follow project convention from rebuild endpoint)
- `delete_playlist`: before local delete, call delete_linked if linked

Integration tests with mocked provider or dependency override:
- Rename updates provider (mock asserts rename called)
- Delete calls provider delete
- Delete still succeeds if provider delete raises (best-effort)
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_playlist_writeback_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - PATCH name triggers provider rename when linked
    - DELETE removes provider playlist when linked
  </acceptance_criteria>
  <done>Provider playlist lifecycle synced with WheelOfFish CRUD.</done>
</task>

<task type="auto">
  <name>Task 2: Open URL builder + API fields (D-17)</name>
  <files>backend/src/wheeloffish/core/provider_playlist_urls.py, backend/src/wheeloffish/api/schemas/playlists.py, backend/src/wheeloffish/api/routes/playlists.py</files>
  <read_first>backend/src/wheeloffish/db/models/connection.py, .planning/phases/07-provider-playlist-writeback/07-RESEARCH.md</read_first>
  <action>
Create `provider_playlist_urls.py`:

```python
def provider_playlist_open_url(
    *, base_url: str, provider_kind: str, provider_playlist_id: str
) -> str | None
```

- Plex: `{base_url}/web/index.html#!/playlist?key=/playlists/{id}`
- Jellyfin: `{base_url}/web/index.html#!/details?id={id}`

Add `provider_playlist_open_url: str | None` to `PlaylistDetailResponse` and `PlaylistListItem`.

Resolve connection from first playlist row's series_id (same as orchestrator) or join via cached series → connection. Return null when not linked.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_playlists_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Detail response includes open URL when provider_playlist_id set
    - URL uses connection base_url without token query params
  </acceptance_criteria>
  <done>Deep links available to SPA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SPA WritebackStatus + types (D-16, D-17)</name>
  <files>frontend/src/api/playlists.ts, frontend/src/api/types.ts, frontend/src/components/playlists/WritebackStatus.tsx, frontend/src/components/playlists/PlaylistCard.tsx, frontend/src/components/playlists/RebuildBanner.tsx, frontend/src/pages/PlaylistDetailPage.tsx, frontend/src/pages/PlaylistsPage.test.tsx, frontend/src/pages/PlaylistDetailPage.test.tsx</files>
  <read_first>frontend/src/components/playlists/StatusBadge.tsx, frontend/src/components/playlists/RebuildBanner.tsx, frontend/src/api/playlists.ts</read_first>
  <action>
Extend TS types:
- `RebuildRunSummary`: writeback_status, writeback_error, writeback_at, writeback_warnings
- `PlaylistListItem` + detail: provider_playlist_id, provider_kind, provider_playlist_open_url

Create `WritebackStatus.tsx`:
- Badge for writeback_status (succeeded/partial/failed/skipped/null)
- External link button "Open in Plex" / "Open in Jellyfin" when `provider_playlist_open_url` set (target=_blank rel=noopener)
- Show writeback_error as muted text when failed

Integrate:
- `RebuildBanner` — add WritebackStatus row below rebuild line on detail page
- `PlaylistCard` — compact writeback indicator (icon or second badge) when last run has writeback_status

Vitest: render succeeded + link, failed + error, no link when url null.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/pages/PlaylistsPage.test.tsx src/pages/PlaylistDetailPage.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function WritebackStatus' frontend/src/components/playlists/WritebackStatus.tsx` equals 1
    - Frontend tests pass
  </acceptance_criteria>
  <done>Operator sees writeback status and can open provider playlist.</done>
</task>

<task type="manual">
  <name>Task 4: Validation + UAT docs (v0.1.0 gate)</name>
  <files>.planning/phases/07-provider-playlist-writeback/07-VALIDATION.md, .planning/phases/07-provider-playlist-writeback/07-UAT.md</files>
  <action>
Create `07-VALIDATION.md` (per-phase validation contract — pytest/vitest map for 07-01–03).

Create `07-UAT.md` manual checklist:
1. Create playlist + add shows → Rebuild now → playlist appears in Plex as `{name} [wof]` with correct episode order
2. Nightly rebuild updates Plex playlist items
3. Rename WheelOfFish playlist → Plex title updates
4. Delete WheelOfFish playlist → Plex playlist removed
5. Partial writeback shows warning in UI when episode unmapped (simulate if needed)
6. Repeat smoke on Jellyfin if 07-02 shipped

Mark v0.1.0 release gate criteria at top.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-provider-playlist-writeback/07-UAT.md && test -f .planning/phases/07-provider-playlist-writeback/07-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Both validation docs exist with EXP-01 traceability
  </acceptance_criteria>
  <done>Phase 7 ready for /gsd-verify-work and v0.1.0 tag.</done>
</task>

</tasks>

## Wave verification

```bash
cd backend && uv run pytest tests/integration/test_playlist_writeback_lifecycle.py -q && cd ../frontend && npm run test -- --run
```

## Plan completion checklist

- [ ] Rename/delete lifecycle tested
- [ ] WritebackStatus in UI with external link
- [ ] 07-UAT.md ready for manual pass
- [ ] v0.1.0 gate documented
