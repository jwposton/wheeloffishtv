---
phase: 10-safe-catalog-prune
plan: 02
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - backend/tests/unit/test_catalog_prune.py
  - backend/src/wheeloffish/core/catalog_prune.py
autonomous: true
requirements: [PRUNE-01, PRUNE-02, PRUNE-03]
must_haves:
  truths:
    - "absence_count below threshold (1 or 2) never deletes a row"
    - "absence_count reaching 3 deletes the row and writes one auto_pruned audit event"
    - "Failed/partial/stalled catalog sync resets all candidate counters to 0 for the connection"
    - "Series present again in CachedSeries has its counter cleared to 0"
    - "write_prune_event retains at most 50 events per playlist"
    - "record_catalog_sync_absence increments only rows whose series is absent from CachedSeries and sets first_absence_at on first hit"
  artifacts:
    - path: "backend/src/wheeloffish/core/catalog_prune.py"
      provides: "Prune evidence + reset + auto-prune + audit service"
      exports: ["record_catalog_sync_absence", "record_rebuild_row_absence", "clear_prune_state_for_recovered", "reset_absence_counters_for_connection", "execute_auto_prune", "write_prune_event", "PRUNE_THRESHOLD", "MAX_AUDIT_EVENTS_PER_PLAYLIST"]
      min_lines: 80
    - path: "backend/tests/unit/test_catalog_prune.py"
      provides: "Unit coverage for PRUNE-01/02/03 service behavior"
      contains: "def test_auto_prune_at_threshold"
  key_links:
    - from: "backend/src/wheeloffish/core/catalog_prune.py"
      to: "playlist_series_rows.absence_count"
      via: "SQLAlchemy query/update on PlaylistSeriesRow"
      pattern: "absence_count"
    - from: "backend/src/wheeloffish/core/catalog_prune.py"
      to: "PlaylistPruneEvent"
      via: "audit event insert + retention"
      pattern: "PlaylistPruneEvent"
---

<objective>
Build the centralized prune service (`core/catalog_prune.py`) that owns all prune-state mutations: evidence accumulation (D-02/D-03), counter reset (D-04), recovery clearing (D-11), threshold auto-prune (D-01/D-06), and audit writes with 50-event retention (D-17/D-18). Sync and rebuild integrations (plans 10-03/10-04/10-05/10-06) call into this module so state logic lives in one place.

Purpose: Single source of truth for the 3-event safety policy (D-01) — keeps PRUNE-01 (no first-miss deletion), PRUNE-02 (policy-gated removal), and PRUNE-03 (auditable material events) correct and testable in isolation.
Output: `core/catalog_prune.py` service module + `tests/unit/test_catalog_prune.py` covering threshold, reset, recovery, retention, and absence detection.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-safe-catalog-prune/10-CONTEXT.md
@.planning/phases/10-safe-catalog-prune/10-RESEARCH.md
@.planning/phases/10-safe-catalog-prune/10-01-SUMMARY.md
@backend/src/wheeloffish/core/catalog_sync.py
@backend/src/wheeloffish/db/models/cached_series.py
@backend/src/wheeloffish/db/models/playlist.py
@backend/tests/conftest.py
@backend/tests/unit/test_orchestrator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for the prune service (RED)</name>
  <files>backend/tests/unit/test_catalog_prune.py</files>
  <read_first>
    - backend/tests/unit/test_orchestrator.py (lines 1-129: TEST_APP_USER_ID, _seed_app_user, _seed_connection, _seed_playlist, _series_id, format_composite_id helpers — copy/import these seeding patterns)
    - backend/tests/conftest.py (db_session fixture, seed_cached_series helper, APP_USER_ID)
    - backend/src/wheeloffish/db/models/cached_series.py (CachedSeries.id is composite id; absence = row.series_id not in CachedSeries ids for connection/user)
  </read_first>
  <behavior>
    - test_sub_threshold_no_prune: rows with absence_count 1 and 2 → execute_auto_prune returns []; rows still exist (PRUNE-01).
    - test_auto_prune_at_threshold: row with absence_count == PRUNE_THRESHOLD (3) → execute_auto_prune (playlist scope) deletes it, returns [series_id], and exactly one PlaylistPruneEvent(event_type="auto_pruned") exists for that playlist/series (PRUNE-02, PRUNE-03).
    - test_reset_on_failed_sync: two rows at counts 2 and 3 → reset_absence_counters_for_connection sets both absence_count to 0 (D-04, PRUNE-02).
    - test_clear_on_recovery: row at count 2 whose series IS present in CachedSeries → clear_prune_state_for_recovered sets absence_count 0 and first_absence_at None (D-11, PRUNE-02).
    - test_audit_retention_50: call write_prune_event 55 times for one playlist → query count == MAX_AUDIT_EVENTS_PER_PLAYLIST (50), newest retained (D-18, PRUNE-03).
    - test_record_catalog_sync_absence: playlist row whose series_id is absent from CachedSeries (for connection/user) increments to 1 and sets first_absence_at; a second call increments to 2; a row whose series IS cached is untouched (D-02/D-03).
  </behavior>
  <action>
    Create `tests/unit/test_catalog_prune.py`. Reuse the seeding style from `test_orchestrator.py` (define or import `_seed_app_user`, `_seed_connection`, `_seed_playlist`, `_series_id`, `TEST_APP_USER_ID`, `TEST_CONNECTION_ID`). Use the `db_session` fixture. For "series present in cache" seed `CachedSeries` rows with `id == series_id`, matching `app_user_id` and `connection_id`. Import service symbols from `wheeloffish.core.catalog_prune`. Tests must currently FAIL (module/functions do not exist yet). Do not weaken assertions to make them pass.
    Set absence_count directly on seeded rows via the ORM (e.g. `row.absence_count = 2; db.flush()`) to drive threshold/reset/recovery cases deterministically.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_catalog_prune.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - File defines all six tests named exactly as in the behavior block.
    - Running the file fails with import/attribute errors or assertion failures (RED) — proves the contract precedes implementation.
  </acceptance_criteria>
  <done>test_catalog_prune.py exists with six failing tests describing the service contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core/catalog_prune.py service (GREEN)</name>
  <files>backend/src/wheeloffish/core/catalog_prune.py</files>
  <read_first>
    - backend/tests/unit/test_catalog_prune.py (the contract just written)
    - backend/src/wheeloffish/core/orchestrator.py (prune_rebuild_history — Python-side retention pattern to mirror for write_prune_event)
    - backend/src/wheeloffish/domain/ids.py (parse_composite_id to derive connection_id from series_id)
  </read_first>
  <action>
    Implement the module with `logger = structlog.get_logger(__name__)`, `PRUNE_THRESHOLD = 3` (D-01), `MAX_AUDIT_EVENTS_PER_PLAYLIST = 50` (D-18). All functions take a `Session` and operate in-session (flush, let caller commit) unless noted.
    Functions:
      - `record_catalog_sync_absence(db, connection_id, app_user_id) -> int`: select PlaylistSeriesRow joined to Playlist where `Playlist.app_user_id == app_user_id` and the row's series belongs to `connection_id` (filter by `parse_composite_id(series_id)[0] == connection_id`) and `series_id NOT IN` the set of `CachedSeries.id` for (connection_id, app_user_id). For each, `absence_count += 1`, set `last_absence_at = now`, `last_evidence_source = "catalog_sync"`, and set `first_absence_at = now` only when it was None (D-03). Return count incremented. Flush.
      - `record_rebuild_row_absence(db, row, source="rebuild") -> None`: increment the single passed PlaylistSeriesRow (caller guarantees provider reachable + not_found/empty_snapshot per D-02); set timestamps + `last_evidence_source=source`; set first_absence_at if None. Flush.
      - `clear_prune_state_for_recovered(db, connection_id, app_user_id) -> None`: for rows on this connection (owned by app_user_id) with `absence_count > 0` whose series IS now in CachedSeries, set absence_count=0, first_absence_at=None, last_absence_at=None, last_evidence_source=None (D-11). Flush. (No evidence_cleared event required — optional only.)
      - `reset_absence_counters_for_connection(db, connection_id, app_user_id) -> None`: set absence_count=0 (+ clear timestamps) for ALL rows on this connection owned by app_user_id (D-04). Flush.
      - `execute_auto_prune(db, *, app_user_id, trigger, playlist_id=None, connection_id=None) -> list[str]`: select rows with `absence_count >= PRUNE_THRESHOLD`. Scope: if `playlist_id` given → only that playlist's rows (rebuild scope); elif `connection_id` given → all rows on that connection across playlists owned by app_user_id (sync scope). For each row: capture series_id + playlist_id, call `write_prune_event(... event_type="auto_pruned", reason=trigger, metadata={"absence_count": row.absence_count, "trigger": trigger})`, then `db.delete(row)`. Return deleted series_ids. Flush. MUST filter by `Playlist.app_user_id == app_user_id` so a shared-connection sync never prunes another user's rows (Pitfall 6).
      - `write_prune_event(db, playlist_id, series_id, event_type, reason, metadata=None) -> None`: insert a PlaylistPruneEvent (event_metadata=metadata), then enforce retention — if count for playlist > MAX_AUDIT_EVENTS_PER_PLAYLIST, delete oldest by timestamp until 50 remain (mirror `prune_rebuild_history` Python-side approach, D-17/D-18). Flush. Also emit a structlog `prune_event` line for operators tailing Docker logs.
    Use `datetime.now(UTC)` for timestamps. Do not log every +1 tick (D-17) — only material events get audit rows.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/unit/test_catalog_prune.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All six tests in test_catalog_prune.py pass (GREEN).
    - `execute_auto_prune` writes exactly one auto_pruned event per pruned row and deletes only `>=3` rows.
    - `write_prune_event` caps stored events at 50 per playlist.
    - No prune occurs below threshold; failed-sync reset zeroes counters.
  </acceptance_criteria>
  <done>catalog_prune.py implements the full prune state machine; test_catalog_prune.py is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync/rebuild caller → prune service | Caller-supplied connection_id/app_user_id/playlist scope determines which rows mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | `execute_auto_prune` cross-user prune | mitigate | Filter all scoped queries by `Playlist.app_user_id == app_user_id` (Pitfall 6); never prune rows outside caller's ownership |
| T-10-03 | Denial of Service | audit table flooding | mitigate | `write_prune_event` enforces 50-event-per-playlist retention (D-18); only 3 material event types logged (D-17) |
| T-10-06 | Tampering | transient-failure-driven prune | mitigate | Threshold 3 (D-01) + `reset_absence_counters_for_connection` on any failed sync (D-04) prevents single-miss deletion |
| T-10-SC | Tampering | dependency installs | accept | No new packages (RESEARCH: existing SQLAlchemy/structlog only) |
</threat_model>

<verification>
- `cd backend && python3 -m pytest tests/unit/test_catalog_prune.py -x` green.
- `cd backend && python3 -m pytest tests/unit/ -x` (no regressions in existing unit suite).
</verification>

<success_criteria>
- Six service unit tests pass.
- Threshold (3), reset, recovery, retention (50), and absence detection behaviors all verified.
- Service is import-clean and used by no other module yet (wiring lands in 10-03/04/05/06).
</success_criteria>

## Artifacts this phase produces (Plan 10-02)

| Artifact | Type | Purpose |
|----------|------|---------|
| `backend/src/wheeloffish/core/catalog_prune.py` | service module | Prune evidence, reset, recovery, auto-prune, audit |
| `backend/tests/unit/test_catalog_prune.py` | tests | PRUNE-01/02/03 service-level coverage |

<output>
Create `.planning/phases/10-safe-catalog-prune/10-02-SUMMARY.md` when done.
</output>
