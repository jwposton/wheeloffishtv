---
phase: 06-library-playlist-assignment
plan: 07
type: execute
wave: 2
depends_on:
  - "06-06"
files_modified:
  - backend/src/wheeloffish/api/schemas/playlists.py
  - backend/src/wheeloffish/api/routes/playlists.py
  - backend/tests/integration/test_playlists_api.py
  - frontend/src/api/playlists.ts
  - frontend/src/components/playlists/PlaylistForm.tsx
  - frontend/src/pages/PlaylistFormPage.tsx
  - frontend/src/components/playlists/TwoPanePicker.tsx
  - frontend/src/components/playlists/TwoPanePicker.test.tsx
  - frontend/src/components/playlists/AddToPlaylistMenu.tsx
  - frontend/src/components/playlists/AddToPlaylistMenu.test.tsx
  - frontend/src/components/playlists/PlaylistRowMenuItems.tsx
  - frontend/src/components/playlists/PlaylistRowMenuItems.test.tsx
autonomous: true
gap_closure: true
requirements:
  - PLT-03
  - WEB-01
tags:
  - phase-6
  - gap-closure
  - uat-posters
  - uat-advanced-menu
  - uat-row-context-menu
  - uat-sticky-actions
must_haves:
  truths:
    - "In playlist pane shows poster art for each row member even when series is not in loaded catalog pages (UAT gap: missing posters)"
    - "Add-to-playlist menu (⋯ and long-press) shows playlist list, Create new, and Advanced… link (UAT gap: Advanced missing)"
    - "In-pane row settings accessible via ⋯ or long-press context menu with playback mode, completion policy, remove (UAT gap: sheet UX)"
    - "Playlist edit Save/Cancel actions remain visible while scrolling the two-pane picker (UAT gap: sticky actions)"
  artifacts:
    - path: backend/src/wheeloffish/api/schemas/playlists.py
      provides: "thumb_url on PlaylistSeriesRowResponse"
      contains: "thumb_url"
    - path: frontend/src/components/playlists/PlaylistRowMenuItems.tsx
      provides: "Shared In-pane row context/dropdown menu items"
      contains: "Remove from playlist"
    - path: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      provides: "Advanced… top-level menu item per D-08"
      contains: "Advanced"
    - path: frontend/src/components/playlists/PlaylistForm.tsx
      provides: "Sticky Save/Cancel footer bar"
      contains: "sticky"
  key_links:
    - from: frontend/src/components/playlists/PlaylistForm.tsx
      to: backend playlist detail rows
      via: "thumb_url from PlaylistSeriesRowResponse on edit load"
      pattern: "thumb_url"
    - from: frontend/src/components/playlists/TwoPanePicker.tsx
      to: frontend/src/components/playlists/PlaylistRowMenuItems.tsx
      via: "MemberTile ContextMenu + ⋯ trigger"
      pattern: "PlaylistRowMenuItems"
    - from: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      to: "/playlists/new?seriesId="
      via: "Advanced… Link after Create new separator (D-08)"
      pattern: "playlists/new\\?seriesId="
---

<objective>
Close remaining Phase 6 UAT gaps: In-pane missing posters, Advanced… missing from add menus, In-pane row context menu (replacing bottom sheet), and sticky playlist form Save/Cancel.

Purpose: Delivers the Library-parity UX the operator validated in UAT — visible posters, discoverable Advanced path, tile-level row controls, and always-reachable form actions.
Output: Backend row thumb_url, PlaylistRowMenuItems, updated TwoPanePicker/AddToPlaylistMenu/PlaylistForm, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-UAT.md
@.planning/phases/06-library-playlist-assignment/06-UI-SPEC.md
@.planning/phases/06-library-playlist-assignment/06-CONTEXT.md
@frontend/src/components/browse/SeriesCard.tsx
@frontend/src/components/playlists/TwoPanePicker.tsx
@frontend/src/components/playlists/RowSettingsSheet.tsx
@frontend/src/components/playlists/AddToPlaylistMenu.tsx
@frontend/src/components/playlists/PlaylistForm.tsx
@backend/src/wheeloffish/api/routes/playlists.py
@backend/src/wheeloffish/core/catalog_sync.py
</context>

<gap_reference>
Gaps closed by this plan:
1. In-pane missing posters — API thumb_url + form init
2. Advanced… missing — top-level menu item in dropdown + context menu (D-08)
3. In-pane context menu — replace RowSettingsSheet primary UX with Library-style menu (D-04, D-15, D-16)
4. Sticky Save/Cancel — fixed/sticky footer on PlaylistForm
</gap_reference>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-07-01 | Spoofing | row thumb_url from CachedSeries | mitigate | Reuse owner-scoped CachedSeries filter (app_user_id) in metadata lookup; same gate as _series_title_map |
| T-06-07-02 | Tampering | row menu PATCH/remove | mitigate | Reuse existing optimistic revert + owner-scoped API from 06-04; depends on 06-06 URL encoding |
| T-06-07-SC | Tampering | npm/pytest deps | accept | No new packages |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return thumb_url on playlist row detail (poster gap)</name>
  <files>backend/src/wheeloffish/api/schemas/playlists.py, backend/src/wheeloffish/api/routes/playlists.py, backend/tests/integration/test_playlists_api.py, frontend/src/api/playlists.ts, frontend/src/components/playlists/PlaylistForm.tsx, frontend/src/pages/PlaylistFormPage.tsx</files>
  <behavior>
    - Backend: GET playlist detail rows include thumb_url (same-origin artwork URL) when CachedSeries exists for row series_id
    - Frontend: PlaylistForm edit load maps r.thumb_url instead of null
    - Integration test: append row with cached series returns non-null thumb_url in detail response
  </behavior>
  <action>
Backend (follow cached_series_to_dto pattern in catalog_sync.py):

Add optional `thumb_url: str | None = None` to `PlaylistSeriesRowResponse` in schemas/playlists.py.

Replace `_series_title_map` with `_series_row_metadata_map(db, app_user_id, series_ids) -> dict[str, tuple[str | None, str | None]]` returning `(title, thumb_url)` keyed by series id. Query CachedSeries for id, title, connection_id where id in series_ids and app_user_id matches owner. For each row, set thumb_url via `series_artwork_url(row.connection_id, row.id)` from wheeloffish.core.media_artwork (import alongside catalog_sync usage).

Update `_playlist_to_detail` to populate series_title and thumb_url from the map.

Add integration test in test_playlists_api.py: seed CachedSeries for SERIES_ID_A with connection_id, append row, GET detail asserts rows[0].thumb_url is non-null and contains `/artwork`.

Frontend:

Add `thumb_url: string | null` to `PlaylistSeriesRowResponse` in playlists.ts.

In PlaylistForm edit initializer, set `thumb_url: r.thumb_url ?? null` (not hardcoded null).

In PlaylistFormPage initialRows from ?seriesId=, leave thumb_url null (catalog enrichment on add still works).

TwoPanePicker displayRows enrichment from catalog infinite query remains as fallback when thumb_url still null.
  </action>
  <verify>
    <automated>cd backend && pytest tests/integration/test_playlists_api.py::test_playlist_detail_row_includes_thumb_url -x 2>&1 | tail -15</automated>
  </verify>
  <done>Playlist detail API returns thumb_url per row; edit form initializes posters without requiring catalog page load.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Advanced… menu item + In-pane row context menu (D-08, D-04, D-16)</name>
  <files>frontend/src/components/playlists/AddToPlaylistMenu.tsx, frontend/src/components/playlists/AddToPlaylistMenu.test.tsx, frontend/src/components/playlists/PlaylistRowMenuItems.tsx, frontend/src/components/playlists/PlaylistRowMenuItems.test.tsx, frontend/src/components/playlists/TwoPanePicker.tsx, frontend/src/components/playlists/TwoPanePicker.test.tsx</files>
  <behavior>
    - AddToPlaylistMenu and AddToPlaylistContextMenuItems render "Advanced…" after "Create new playlist…" separator; navigates to `/playlists/new?seriesId={encodeURIComponent(seriesId)}` (D-08)
    - In-pane MemberTile shows visible ⋯ button (MoreVertical, absolute top-2 right-2) matching SeriesCard
    - Right-click and long-press on In-pane tile open same menu as ⋯
    - Menu offers playback mode (Ordered/Random), completion policy (remove/restart/disordered), and Remove from playlist (destructive with confirm)
    - Selecting mode/policy PATCHes immediately via existing handleSave path; remove uses handleRemove
    - RowSettingsSheet no longer opened on tile click (remove settingsRow state and sheet render from TwoPanePicker; keep RowSettingsSheet.tsx file unless fully unused — do not delete unless imports reach zero)
  </behavior>
  <action>
Advanced menu (UAT gap 3, D-08):

In AddToPlaylistMenu.tsx, after "Create new playlist…" item, add separator then "Advanced…" item linking to `/playlists/new?seriesId=${encodeURIComponent(seriesId)}`. Use react-router Link with onClick stopPropagation. Mirror in AddToPlaylistContextMenuItems using ContextMenuItem render Link pattern (match QuickCreatePlaylistDialog advancedHref).

Update AddToPlaylistMenu.test.tsx: assert Advanced… visible in opened dropdown and context menu variant; href contains encoded seriesId.

In-pane row menu (UAT gap 4):

Create PlaylistRowMenuItems.tsx exporting shared menu content for a SeriesRow:
- Props: row, onModeChange, onPolicyChange, onRemove (async callbacks)
- Playback mode submenu or radio group: Ordered / Random (labels match RowSettingsSheet)
- Completion policy submenu: Remove when done / Restart / Switch to random
- ContextMenuSeparator then destructive "Remove from playlist" → AlertDialog confirm (reuse copy from 06-UI-SPEC: "Remove {title} from this playlist?")
- On mode/policy select, call onModeChange/onPolicyChange with updated row; parent invokes handleSave

Refactor TwoPanePicker MemberTile to mirror SeriesCard.tsx:
- Wrap tile in ContextMenu + ContextMenuTrigger
- Add ⋯ DropdownMenu (or reuse menu items in both ContextMenuContent and DropdownMenuContent — extract PlaylistRowMenuItems to accept variant prop or render function)
- Tile body click does NOT open sheet; only menu actions mutate row
- stopPropagation on ⋯ and menu handlers

Remove RowSettingsSheet usage from TwoPanePicker (delete settingsRow state, sheet JSX block).

Tests: PlaylistRowMenuItems.test.tsx — renders mode/policy/remove items; TwoPanePicker.test.tsx — In-pane tile has aria-label Series actions or MoreVertical button; no RowSettingsSheet import in TwoPanePicker.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/playlists/AddToPlaylistMenu.test.tsx src/components/playlists/PlaylistRowMenuItems.test.tsx src/components/playlists/TwoPanePicker.test.tsx -x</automated>
  </verify>
  <done>Advanced… visible in add-to-playlist menus; In-pane tiles use Library-style ⋯ + context menu for row settings; bottom sheet no longer primary UX.</done>
</task>

<task type="auto">
  <name>Task 3: Sticky Save/Cancel footer on playlist form</name>
  <files>frontend/src/components/playlists/PlaylistForm.tsx</files>
  <action>
UAT gap 5 — sticky form actions per 06-UI-SPEC operator feedback:

Move Save/Cancel button group out of document flow into a sticky footer bar at bottom of viewport:
- Wrapper: `sticky bottom-0 z-10 -mx-4 mt-4 border-t bg-background/95 px-4 py-3 backdrop-blur supports-[backdrop-filter]:bg-background/80` (adjust horizontal margin to match form container max-w-6xl page padding)
- Keep existing Button types, disabled={isPending}, labels "Save playlist" / "Cancel"
- Add `pb-20` or equivalent bottom padding on the form element so last Series section content is not obscured by the sticky bar when scrolled to end
- Footer remains visible while scrolling long two-pane picker content

Do not change submit handler or validation logic.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Save/Cancel bar stays visible at bottom of viewport during playlist form scroll.</done>
</task>

</tasks>

<verification>
Re-run Phase 6 UAT scenarios:
- Test 1: Advanced… visible on ⋯ and long-press menus
- Test 2: In-pane posters render for existing playlist members on edit load
- Test 4: Save/Cancel remain visible while scrolling
- Test 5: Remove works; row settings via context menu not sheet
</verification>

<success_criteria>
- All four non-blocker UAT gaps marked closed in 06-UAT.md truths
- Backend integration test confirms thumb_url on playlist rows
- AddToPlaylistMenu tests cover Advanced… item
- TwoPanePicker uses PlaylistRowMenuItems; RowSettingsSheet not mounted from picker
- PlaylistForm sticky footer renders with sticky/fixed positioning classes
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-07-SUMMARY.md` when done
</output>
