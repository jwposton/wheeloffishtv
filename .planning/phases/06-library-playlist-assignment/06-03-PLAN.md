---
phase: 06-library-playlist-assignment
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - frontend/src/components/playlists/AddToPlaylistMenu.tsx
  - frontend/src/components/playlists/QuickCreatePlaylistDialog.tsx
  - frontend/src/components/browse/SeriesCard.tsx
  - frontend/src/components/browse/SeriesGrid.tsx
  - frontend/src/pages/BrowsePage.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/api/playlists.ts
  - frontend/src/components/playlists/AddToPlaylistMenu.test.tsx
  - frontend/src/components/browse/SeriesCard.test.tsx
autonomous: true
requirements:
  - PLT-03
  - WEB-01
tags:
  - phase-6
  - library
  - add-to-playlist
must_haves:
  truths:
    - "AppShell nav label reads Library (D-03); page heading reads Library"
    - "Each grid tile shows visible ⋯ control; right-click and long-press open same menu (D-04)"
    - "Menu actions stop propagation — tile click still navigates to detail (D-06)"
    - "Add to existing playlist and Create new playlist actions work from Library tiles (D-05, D-08)"
    - "Quick create: inline name → create + append series; Advanced link navigates to /playlists/new?seriesId= (D-08)"
    - "Shared AddToPlaylistMenu used from Library tiles (D-19)"
  artifacts:
    - path: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      provides: "Shared add-to-playlist dropdown/context menu"
      contains: "export function AddToPlaylistMenu"
    - path: frontend/src/components/browse/SeriesCard.tsx
      provides: "Tile with ⋯ + context menu integration"
      contains: "MoreVertical"
    - path: frontend/src/api/playlists.ts
      provides: "appendPlaylistRow, useAppendPlaylistRow hooks"
      contains: "appendPlaylistRow"
  key_links:
    - from: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      to: frontend/src/api/playlists.ts
      via: "useAppendPlaylistRow mutation calls POST /playlists/{id}/rows"
      pattern: "appendPlaylistRow"
    - from: frontend/src/components/browse/SeriesCard.tsx
      to: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      via: "⋯ button renders AddToPlaylistMenu with seriesId prop"
      pattern: "AddToPlaylistMenu"
---

## Phase Goal

**As a** self-host operator, **I want to** add shows to playlists directly from Library tiles, **so that** playlist assignment feels like browsing my catalog (D-03–D-08, D-19, PLT-03).

<objective>
Wave 3 frontend slice — rename Browse → Library, add visible ⋯ on tiles with Radix ContextMenu (right-click + long-press), shared AddToPlaylistMenu with quick-create dialog, and frontend hooks for row append API from Plan 06-02.

Output: AddToPlaylistMenu, QuickCreatePlaylistDialog, updated SeriesCard/SeriesGrid/BrowsePage/AppShell, append hook in playlists.ts, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-CONTEXT.md
@.planning/phases/06-library-playlist-assignment/06-UI-SPEC.md
@.planning/phases/06-library-playlist-assignment/06-RESEARCH.md
@frontend/src/components/browse/SeriesCard.tsx
@frontend/src/pages/BrowsePage.tsx
@frontend/src/components/layout/AppShell.tsx
@frontend/src/api/playlists.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-03-01 | Tampering | quick-create name input | mitigate | Trim whitespace; reject empty name client-side; backend min_length=1 on create |
| T-06-03-02 | Spoofing | append to foreign playlist | mitigate | Backend owner-scoped 404; frontend only lists user's playlists from usePlaylists |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Frontend API hooks for row append + quick create (D-20)</name>
  <files>frontend/src/api/playlists.ts</files>
  <read_first>frontend/src/api/playlists.ts, backend/src/wheeloffish/api/routes/playlists.py, backend/src/wheeloffish/api/schemas/playlists.py</read_first>
  <action>
Add to `playlists.ts`:
- `appendPlaylistRow(playlistId, { series_id, mode?, completion_policy? })` → POST `/api/v1/playlists/${playlistId}/rows`
- `useAppendPlaylistRow()` mutation hook: on success invalidate `["playlists", playlistId]` and `["playlists"]`
- `createPlaylistWithSeries(name, seriesId)` helper: POST create with defaults (episode_count=20, refresh_cadence=daily) and single row — used by quick-create dialog (D-08)

Match existing fetchJson + error handling patterns from createPlaylist/updatePlaylist.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function appendPlaylistRow' frontend/src/api/playlists.ts` equals 1
    - `grep -c 'export function useAppendPlaylistRow' frontend/src/api/playlists.ts` equals 1
    - `grep -c 'export async function createPlaylistWithSeries' frontend/src/api/playlists.ts` equals 1
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Frontend hooks for append + quick create ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AddToPlaylistMenu + QuickCreatePlaylistDialog (D-05, D-08, D-19)</name>
  <files>frontend/src/components/playlists/AddToPlaylistMenu.tsx, frontend/src/components/playlists/QuickCreatePlaylistDialog.tsx, frontend/src/components/playlists/AddToPlaylistMenu.test.tsx</files>
  <read_first>frontend/src/components/playlists/AddToPlaylistMenu.tsx, .planning/phases/06-library-playlist-assignment/06-UI-SPEC.md, frontend/src/api/playlists.ts, frontend/src/components/ui/dropdown-menu.tsx</read_first>
  <action>
Create `AddToPlaylistMenu.tsx`:
- Props: `seriesId: string`, `trigger: ReactNode` (or built-in ⋯ button variant)
- Uses `usePlaylists()` to list user playlists
- Menu items: each playlist name → calls `useAppendPlaylistRow`; separator; "Create new playlist…" → opens QuickCreatePlaylistDialog
- Toast on success: "Added to {name}" per UI-SPEC copywriting

Create `QuickCreatePlaylistDialog.tsx`:
- Dialog with name Input (autofocus), primary button "Create and add", link "Advanced…" → `navigate('/playlists/new?seriesId=' + encodeURIComponent(seriesId))`
- On submit: `createPlaylistWithSeries(name, seriesId)` then toast + close

Add `AddToPlaylistMenu.test.tsx`: mock hooks; assert menu renders playlist names; assert Advanced link href contains seriesId.

Install shadcn ContextMenu if not present: `npx shadcn@latest add context-menu` (only if missing from components/ui).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/playlists/AddToPlaylistMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function AddToPlaylistMenu' frontend/src/components/playlists/AddToPlaylistMenu.tsx` equals 1
    - `grep -c 'Create and add' frontend/src/components/playlists/QuickCreatePlaylistDialog.tsx` equals 1
    - `grep -c 'Advanced' frontend/src/components/playlists/QuickCreatePlaylistDialog.tsx` equals 1
    - `cd frontend && npm run test -- --run src/components/playlists/AddToPlaylistMenu.test.tsx` exits 0
  </acceptance_criteria>
  <done>Shared AddToPlaylistMenu component with quick-create dialog.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Library nav rename + SeriesCard ⋯ integration (D-03, D-04, D-06, D-07)</name>
  <files>frontend/src/components/browse/SeriesCard.tsx, frontend/src/components/browse/SeriesGrid.tsx, frontend/src/pages/BrowsePage.tsx, frontend/src/components/layout/AppShell.tsx, frontend/src/components/browse/SeriesCard.test.tsx</files>
  <read_first>frontend/src/components/browse/SeriesCard.tsx, frontend/src/pages/BrowsePage.tsx, frontend/src/components/layout/AppShell.tsx, .planning/phases/06-library-playlist-assignment/06-UI-SPEC.md</read_first>
  <action>
**AppShell.tsx:** Change nav label "Browse" → "Library" (path stays `/browse` per research discretion).

**BrowsePage.tsx:** Change page heading "Browse" → "Library"; keep debounced name search + infinite scroll unchanged (D-07).

**SeriesCard.tsx (grid variant):**
- Wrap tile in relative container; add visible ⋯ button (`MoreVertical` icon) at `absolute top-2 right-2` with `stopPropagation` on click
- Wrap tile in Radix `ContextMenu` so right-click and long-press open same menu as ⋯
- ContextMenu content renders `AddToPlaylistMenu` actions inline (or trigger submenu)
- Tile body click still navigates to detail; ⋯ and menu handlers call `e.stopPropagation()` (D-06)

**SeriesGrid.tsx:** Pass through any props needed (no structural change unless required).

**SeriesCard.test.tsx:** Test that clicking ⋯ does not call navigate; test context menu trigger exists.

List variant: add ⋯ on trailing edge with same menu pattern.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/browse/SeriesCard.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Library' frontend/src/components/layout/AppShell.tsx` is at least 1
    - `grep -c 'Library' frontend/src/pages/BrowsePage.tsx` is at least 1
    - `grep -c 'MoreVertical' frontend/src/components/browse/SeriesCard.tsx` equals 1
    - `grep -c 'stopPropagation' frontend/src/components/browse/SeriesCard.tsx` is at least 2
    - `grep -c 'ContextMenu' frontend/src/components/browse/SeriesCard.tsx` is at least 1
    - `cd frontend && npm run test -- --run src/components/browse/SeriesCard.test.tsx` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Library nav + tile ⋯ menu with propagation guard shipped.</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run src/components/playlists/AddToPlaylistMenu.test.tsx src/components/browse/SeriesCard.test.tsx && npm run build
</verification>

<success_criteria>
- D-03: Nav and page read "Library"
- D-04–D-06: Visible ⋯, shared context menu, stopPropagation
- D-05, D-08, D-19: AddToPlaylistMenu with quick-create + Advanced link
- D-07: Name search only (unchanged)
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-03-SUMMARY.md` when done.
</output>
