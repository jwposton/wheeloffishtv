---
phase: 06-library-playlist-assignment
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/src/wheeloffish/api/schemas/playlists.py
  - backend/src/wheeloffish/api/routes/playlists.py
  - backend/tests/integration/test_playlists_api.py
autonomous: true
requirements:
  - PLT-03
tags:
  - phase-6
  - api
  - row-ops
must_haves:
  truths:
    - "POST /api/v1/playlists/{id}/rows appends one series row without full PUT (D-20)"
    - "DELETE /api/v1/playlists/{id}/rows/{series_id} removes one row without full PUT (D-20)"
    - "PATCH /api/v1/playlists/{id}/rows/{series_id} updates mode/completion_policy on one row (D-16 backend support)"
    - "Duplicate append returns 409; cross-user access returns 404 (owner-scoped per Phase 5 D-18)"
    - "Append defaults mode=ordered, completion_policy=remove, completion_event=series_complete (match create defaults)"
  artifacts:
    - path: backend/src/wheeloffish/api/routes/playlists.py
      provides: "Row append/remove/patch endpoints"
      contains: "def append_playlist_row"
    - path: backend/src/wheeloffish/api/schemas/playlists.py
      provides: "AppendRowRequest, PatchRowRequest schemas"
      contains: "class AppendRowRequest"
    - path: backend/tests/integration/test_playlists_api.py
      provides: "Row operation integration tests"
      contains: "test_append_row"
  key_links:
    - from: backend/src/wheeloffish/api/routes/playlists.py
      to: backend/src/wheeloffish/db/models/playlist_series_row.py
      via: "UniqueConstraint(playlist_id, series_id) enforces duplicate detection"
      pattern: "PlaylistSeriesRowOrm"
---

## Phase Goal

**As a** self-host operator, **I want to** add or remove a single show from a playlist without re-submitting the entire form, **so that** Library quick-add and the two-pane editor stay fast (D-20, PLT-03).

<objective>
Wave 2 backend — add owner-scoped row append, remove, and patch endpoints to `playlists.py`. Enables Library context-menu quick-add and two-pane picker incremental mutations without full PUT row replacement.

Output: Three new endpoints, Pydantic request schemas, integration tests covering happy path, 409 duplicate, and cross-user 404.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-CONTEXT.md
@.planning/phases/06-library-playlist-assignment/06-RESEARCH.md
@backend/src/wheeloffish/api/routes/playlists.py
@backend/src/wheeloffish/api/schemas/playlists.py
@backend/tests/integration/test_playlists_api.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-02-01 | Elevation of Privilege | append/remove/patch routes | mitigate | `_get_owned_playlist()` before any mutation; 404 for foreign playlist_id |
| T-06-02-02 | Tampering | series_id in append body | mitigate | Validate series_id is non-empty string; optional: verify series exists in user's CachedSeries scope |
| T-06-02-03 | Tampering | duplicate row append | mitigate | Check existing (playlist_id, series_id) before insert; return 409 Conflict |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing integration tests for row ops (D-20)</name>
  <files>backend/tests/integration/test_playlists_api.py</files>
  <read_first>backend/tests/integration/test_playlists_api.py, backend/src/wheeloffish/api/routes/playlists.py, backend/src/wheeloffish/api/schemas/playlists.py</read_first>
  <action>
Append integration tests to existing `test_playlists_api.py` (reuse existing auth fixtures and playlist create helper):

1. `test_append_row`: create playlist with 0 rows; POST `/api/v1/playlists/{id}/rows` body `{"series_id": "<known_series_id>"}`; assert 200/201; GET detail shows 1 row with mode `ordered`, completion_policy `remove`
2. `test_append_row_duplicate_409`: append same series_id twice; second call returns 409
3. `test_remove_row`: append row then DELETE `/api/v1/playlists/{id}/rows/{series_id}`; assert 204; GET detail shows 0 rows
4. `test_patch_row_mode`: append row; PATCH body `{"mode": "disordered"}`; GET detail shows mode `disordered`
5. `test_append_row_cross_user_404`: user B attempts append on user A playlist; assert 404

Use a series_id that exists in test fixtures (grep existing tests for cached series setup). Tests MUST fail (404 route not found) before Task 2.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_playlists_api.py -k "append_row or remove_row or patch_row" -x 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_append_row' backend/tests/integration/test_playlists_api.py` equals 1
    - `grep -c 'def test_remove_row' backend/tests/integration/test_playlists_api.py` equals 1
    - `grep -c 'def test_patch_row_mode' backend/tests/integration/test_playlists_api.py` equals 1
    - pytest collects at least 5 new row-op tests without import errors
    - Tests fail with 404 (route missing) not collection errors — RED state
  </acceptance_criteria>
  <done>Failing row-op integration tests committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement append, remove, patch row endpoints (D-20)</name>
  <files>backend/src/wheeloffish/api/schemas/playlists.py, backend/src/wheeloffish/api/routes/playlists.py</files>
  <read_first>backend/src/wheeloffish/api/routes/playlists.py, backend/src/wheeloffish/api/schemas/playlists.py, backend/tests/integration/test_playlists_api.py</read_first>
  <action>
Add schemas to `playlists.py`:
- `AppendRowRequest`: `series_id: str` (required), optional `mode`, `completion_policy`, `completion_event` defaulting to ordered/remove/series_complete
- `PatchRowRequest`: optional `mode`, `completion_policy`, `completion_event` (at least one required via validator)

Add routes to `playlists.py`:
- `POST /{playlist_id}/rows` → `append_playlist_row`: ownership via `_get_owned_playlist`; check duplicate → 409; set `sort_order = max(existing)+1 or 0`; return `_playlist_to_detail`
- `DELETE /{playlist_id}/rows/{series_id}` → `remove_playlist_row`: delete matching row or 404 if row not found
- `PATCH /{playlist_id}/rows/{series_id}` → `patch_playlist_row`: update fields on matching row or 404

All three update `playlist.updated_at`. Run Task 1 tests — must pass GREEN.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_playlists_api.py -k "append_row or remove_row or patch_row" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def append_playlist_row' backend/src/wheeloffish/api/routes/playlists.py` equals 1
    - `grep -c 'def remove_playlist_row' backend/src/wheeloffish/api/routes/playlists.py` equals 1
    - `grep -c 'def patch_playlist_row' backend/src/wheeloffish/api/routes/playlists.py` equals 1
    - `grep -c 'class AppendRowRequest' backend/src/wheeloffish/api/schemas/playlists.py` equals 1
    - `cd backend && uv run pytest tests/integration/test_playlists_api.py -q` exits 0 (all playlist tests green)
    - `cd backend && uv run ruff check src/wheeloffish/api/` exits 0
  </acceptance_criteria>
  <done>Row append/remove/patch API live and tested.</done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/integration/test_playlists_api.py -q && uv run ruff check src/wheeloffish/api/
</verification>

<success_criteria>
- D-20: Library quick-add and two-pane picker can mutate rows without full PUT
- Owner-scoped 404 and duplicate 409 enforced
- Existing CRUD + rebuild tests remain green
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-02-SUMMARY.md` when done.
</output>
