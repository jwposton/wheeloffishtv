---
phase: 06-library-playlist-assignment
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/playlists.ts
  - frontend/src/api/playlists.test.ts
autonomous: true
gap_closure: true
requirements:
  - PLT-03
  - WEB-01
tags:
  - phase-6
  - gap-closure
  - cr-01
must_haves:
  truths:
    - "Remove from playlist succeeds for composite Plex series IDs in edit mode (UAT gap: remove broken)"
    - "PATCH row mode/completion_policy succeeds for composite Plex series IDs"
  artifacts:
    - path: frontend/src/api/playlists.ts
      provides: "Encoded row DELETE/PATCH URL paths"
      contains: "encodeURIComponent(seriesId)"
    - path: frontend/src/api/playlists.test.ts
      provides: "Regression tests for Plex composite ID URL encoding"
      contains: "encodeURIComponent"
  key_links:
    - from: frontend/src/components/playlists/TwoPanePicker.tsx
      to: frontend/src/api/playlists.ts
      via: "handleRemove/handleSave call removePlaylistRow/patchPlaylistRow with composite series_id"
      pattern: "useRemovePlaylistRow|usePatchPlaylistRow"
---

<objective>
Close UAT blocker: row remove and patch fail for real Plex composite series IDs because DELETE/PATCH URLs omit path encoding (CR-01 from 06-REVIEW.md).

Purpose: Unblocks remove-from-playlist in edit mode and row settings PATCH; required before In-pane context-menu remove can be verified.
Output: Encoded row mutation URLs + unit tests with Plex-style composite ID fixture.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-UAT.md
@.planning/phases/06-library-playlist-assignment/06-REVIEW.md
@frontend/src/api/playlists.ts
@frontend/src/lib/seriesId.ts
@frontend/src/lib/seriesId.test.ts
</context>

<gap_reference>
truth: "Remove from playlist works in edit mode via row settings; save row settings persists mode/completion_policy"
test: 5
root_cause: removePlaylistRow and patchPlaylistRow interpolate seriesId without encodeURIComponent — breaks DELETE and PATCH for Plex composite IDs (add via POST body works; settings save fails after add)
missing: encodeURIComponent(seriesId) in removePlaylistRow and patchPlaylistRow URLs
</gap_reference>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-06-01 | Tampering | row DELETE/PATCH path | mitigate | encodeURIComponent on seriesId path segment; match seriesApiPath single-encoding convention |
| T-06-06-SC | Tampering | npm test deps | accept | No new packages; vitest already in frontend devDependencies |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode seriesId in row DELETE/PATCH URLs (CR-01)</name>
  <files>frontend/src/api/playlists.ts, frontend/src/api/playlists.test.ts</files>
  <behavior>
    - Test 1: removePlaylistRow builds URL `/playlists/{playlistId}/rows/{encodeURIComponent(seriesId)}` for Plex composite ID containing `%3A%2F%2F`
    - Test 2: patchPlaylistRow builds same encoded path segment
    - Test 3: Plain series IDs (no special chars) still encode without breaking the request path
  </behavior>
  <action>
In `frontend/src/api/playlists.test.ts`, mock `fetchJson` from `@/api/client` and assert the URL passed to removePlaylistRow and patchPlaylistRow.

Use the Plex composite fixture from `seriesId.test.ts`:
`5d176728-53f0-4ce5-bfb1-69f593a63209:plex:plex%3A%2F%2Fshow%2F643cbf9ec1962a0897b7f6dd`

RED: tests fail because current URLs interpolate raw seriesId.

In `removePlaylistRow` and `patchPlaylistRow`, wrap seriesId with encodeURIComponent in the path:
`/playlists/${playlistId}/rows/${encodeURIComponent(seriesId)}`

Do NOT decode seriesId before encoding (seriesApiPath comment: single encoding only). Do NOT change appendPlaylistRow (series_id is JSON body). Do NOT alter playlistId encoding unless it already uses encodeURIComponent elsewhere (leave as-is).

GREEN: all playlists.test.ts cases pass.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/api/playlists.test.ts -x</automated>
  </verify>
  <done>removePlaylistRow and patchPlaylistRow request URLs encode composite Plex series IDs; unit tests lock the behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm build and existing playlist tests still pass</name>
  <files>frontend/src/api/playlists.ts</files>
  <action>
Run full frontend unit suite for playlist-related components to ensure no regressions from URL change. No additional code changes unless a test mocks hard-coded unencoded URLs — update those mocks to expect encoded paths.

If TwoPanePicker or integration-style tests stub fetch URLs, align stubs with encodeURIComponent output.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/playlists/ src/api/playlists.test.ts -x 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend playlist test suite green; encoding fix does not break adjacent components.</done>
</task>

</tasks>

<verification>
- UAT Test 5 remove action succeeds on playlist edit with real Plex composite series IDs
- CR-01 from 06-REVIEW.md resolved
</verification>

<success_criteria>
- grep confirms encodeURIComponent in removePlaylistRow and patchPlaylistRow URL templates
- playlists.test.ts passes with Plex composite ID fixture
- No unencoded `/rows/${seriesId}` interpolation remains in playlists.ts row mutations
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-06-SUMMARY.md` when done
</output>
