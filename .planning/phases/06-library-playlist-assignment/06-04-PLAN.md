---
phase: 06-library-playlist-assignment
plan: 04
type: execute
wave: 4
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - frontend/src/components/playlists/TwoPanePicker.tsx
  - frontend/src/components/playlists/RowSettingsSheet.tsx
  - frontend/src/components/playlists/PlaylistForm.tsx
  - frontend/src/pages/PlaylistFormPage.tsx
  - frontend/src/api/playlists.ts
  - frontend/src/components/playlists/TwoPanePicker.test.tsx
  - frontend/src/components/playlists/RowSettingsSheet.test.tsx
autonomous: true
requirements:
  - PLT-03
  - WEB-01
tags:
  - phase-6
  - two-pane
  - playlist-editor
must_haves:
  truths:
    - "PlaylistForm section 3 uses TwoPanePicker instead of SeriesPicker search list (D-13, D-21)"
    - "In playlist pane shows member tiles; Available pane shows catalog tiles with name search (D-14)"
    - "Click Available tile adds row via append API; click In tile opens RowSettingsSheet (D-15, D-16)"
    - "RowSettingsSheet: ordered/random toggle, completion policy select, remove action (D-16)"
    - "md+ side-by-side columns; <md tabbed In playlist (N) | Add shows (D-17)"
    - "Basics + Schedule sections remain above picker (D-18)"
    - "Pre-selected series from ?seriesId= query param appears in In pane on create (D-08 Advanced path)"
  artifacts:
    - path: frontend/src/components/playlists/TwoPanePicker.tsx
      provides: "Two-pane tile picker component"
      contains: "export function TwoPanePicker"
    - path: frontend/src/components/playlists/RowSettingsSheet.tsx
      provides: "Bottom sheet for row settings"
      contains: "export function RowSettingsSheet"
    - path: frontend/src/components/playlists/PlaylistForm.tsx
      provides: "Form using TwoPanePicker in Shows section"
      contains: "TwoPanePicker"
  key_links:
    - from: frontend/src/components/playlists/TwoPanePicker.tsx
      to: frontend/src/api/playlists.ts
      via: "Available pane click calls useAppendPlaylistRow; remove calls DELETE row"
      pattern: "useAppendPlaylistRow"
    - from: frontend/src/components/playlists/TwoPanePicker.tsx
      to: frontend/src/components/browse/SeriesGrid.tsx
      via: "Reuses SeriesCard/SeriesPoster tile styling for both panes"
      pattern: "SeriesCard"
---

## Phase Goal

**As a** self-host operator, **I want to** manage playlist membership in a visual two-pane tile editor, **so that** I can see what's in vs available without a search box list (D-13–D-18, D-21, PLT-03).

<objective>
Wave 4 frontend — replace SeriesPicker with TwoPanePicker (In playlist | Available to add), RowSettingsSheet for per-row settings, responsive tabs on mobile. Wire to row append/remove/patch API from Plan 06-02. Remove SeriesPicker from PlaylistForm (D-21).

Output: TwoPanePicker, RowSettingsSheet, updated PlaylistForm + PlaylistFormPage, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-CONTEXT.md
@.planning/phases/06-library-playlist-assignment/06-UI-SPEC.md
@.planning/phases/06-library-playlist-assignment/06-RESEARCH.md
@frontend/src/components/playlists/PlaylistForm.tsx
@frontend/src/components/playlists/SeriesPicker.tsx
@frontend/src/pages/PlaylistFormPage.tsx
@frontend/src/components/browse/SeriesGrid.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-04-01 | Tampering | optimistic UI row add | mitigate | On append failure, revert local state + toast error; rely on server 409 for duplicates |
| T-06-04-02 | Denial of Service | infinite scroll in Available pane | accept | Reuse SERIES_PAGE_LIMIT=50 + intersection observer like BrowsePage |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RowSettingsSheet component (D-16)</name>
  <files>frontend/src/components/playlists/RowSettingsSheet.tsx, frontend/src/components/playlists/RowSettingsSheet.test.tsx</files>
  <read_first>frontend/src/components/playlists/SeriesPicker.tsx, frontend/src/components/playlists/PlaylistForm.tsx, .planning/phases/06-library-playlist-assignment/06-UI-SPEC.md</read_first>
  <action>
Create `RowSettingsSheet.tsx` using shadcn Sheet with `side="bottom"`:
- Props: `open`, `onOpenChange`, `row: SeriesRow`, `seriesTitle`, `onSave(updatedRow)`, `onRemove()`
- Content: title "{seriesTitle} — row settings" (UI-SPEC)
- Controls: Ordered/Random toggle (maps to mode ordered/disordered), completion policy select (remove/restart/disordered — reuse options from SeriesPicker), Remove button with confirm dialog
- Defaults match SeriesPicker: ordered + remove

Add `RowSettingsSheet.test.tsx`: render with open=true; assert toggle and select present; assert onSave called with updated mode.

Add shadcn Sheet if missing: check `frontend/src/components/ui/sheet.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/playlists/RowSettingsSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function RowSettingsSheet' frontend/src/components/playlists/RowSettingsSheet.tsx` equals 1
    - `grep -c 'side="bottom"' frontend/src/components/playlists/RowSettingsSheet.tsx` equals 1
    - `grep -c 'row settings' frontend/src/components/playlists/RowSettingsSheet.tsx` equals 1
    - `cd frontend && npm run test -- --run src/components/playlists/RowSettingsSheet.test.tsx` exits 0
  </acceptance_criteria>
  <done>Bottom sheet for In-pane row settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TwoPanePicker component (D-13–D-17)</name>
  <files>frontend/src/components/playlists/TwoPanePicker.tsx, frontend/src/components/playlists/TwoPanePicker.test.tsx, frontend/src/api/playlists.ts</files>
  <read_first>frontend/src/components/playlists/SeriesPicker.tsx, frontend/src/components/browse/SeriesGrid.tsx, frontend/src/pages/BrowsePage.tsx, frontend/src/hooks/useSeriesInfiniteQuery.ts</read_first>
  <action>
Create `TwoPanePicker.tsx`:
- Props: `rows: SeriesRow[]`, `onRowsChange`, `playlistId?: string` (when editing — use API; when creating — local state only until save)
- **Search bar** above Available pane: debounced 300ms, filters catalog via `useSeriesInfiniteQuery`
- **In playlist pane:** grid of member series (poster tiles from row data + thumb from catalog lookup); click tile → open RowSettingsSheet; show count in header "In playlist (N)"
- **Available pane:** reuse SeriesGrid pattern; click tile → add to In (append API if playlistId set, else local push); hide or dim series already in In pane
- **Responsive (D-17):** `grid grid-cols-1 md:grid-cols-2 gap-8` for md+; below md use shadcn Tabs: "In playlist (N)" | "Add shows"
- Export `SeriesRow` type from here (or re-export from SeriesPicker until removal)

Add hooks to playlists.ts if missing: `removePlaylistRow`, `patchPlaylistRow`, `useRemovePlaylistRow`, `usePatchPlaylistRow`

Add `TwoPanePicker.test.tsx`: mock viewport; assert Tabs render on narrow; assert grid columns class on wide (use matchMedia mock or data-testid on layout wrapper).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/playlists/TwoPanePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function TwoPanePicker' frontend/src/components/playlists/TwoPanePicker.tsx` equals 1
    - `grep -c 'In playlist' frontend/src/components/playlists/TwoPanePicker.tsx` is at least 1
    - `grep -c 'Available to add' frontend/src/components/playlists/TwoPanePicker.tsx` is at least 1
    - `grep -c 'md:grid-cols-2' frontend/src/components/playlists/TwoPanePicker.tsx` equals 1
    - `grep -c 'Tabs' frontend/src/components/playlists/TwoPanePicker.tsx` is at least 1
    - `cd frontend && npm run test -- --run src/components/playlists/TwoPanePicker.test.tsx` exits 0
  </acceptance_criteria>
  <done>Two-pane tile picker with responsive layout.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate into PlaylistForm; remove SeriesPicker (D-18, D-21, D-08)</name>
  <files>frontend/src/components/playlists/PlaylistForm.tsx, frontend/src/pages/PlaylistFormPage.tsx</files>
  <read_first>frontend/src/components/playlists/PlaylistForm.tsx, frontend/src/components/playlists/SeriesPicker.tsx, frontend/src/pages/PlaylistFormPage.tsx</read_first>
  <action>
**PlaylistForm.tsx:**
- Replace `<SeriesPicker .../>` in Shows section with `<TwoPanePicker rows={rows} onRowsChange={setRows} playlistId={existingPlaylistId} />`
- Keep Basics and Schedule sections unchanged above Shows (D-18)
- Import SeriesRow from TwoPanePicker (not SeriesPicker)

**PlaylistFormPage.tsx:**
- Read `seriesId` from URL search param `?seriesId=` on create route
- If present, pre-populate rows state with one SeriesRow `{ series_id, mode: ordered, completion_policy: remove }` (D-08 Advanced path)

**Remove SeriesPicker.tsx** (D-21): delete file after confirming no imports remain (`grep -r SeriesPicker frontend/src`)

Update any tests importing SeriesPicker to use TwoPanePicker mocks.
  </action>
  <verify>
    <automated>cd frontend && npm run build && ! grep -r 'SeriesPicker' frontend/src --include='*.tsx' --include='*.ts' | grep -v node_modules</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TwoPanePicker' frontend/src/components/playlists/PlaylistForm.tsx` equals 1
    - `grep -c 'SeriesPicker' frontend/src/components/playlists/PlaylistForm.tsx` equals 0
    - `test ! -f frontend/src/components/playlists/SeriesPicker.tsx` — file deleted (D-21)
    - `grep -c 'seriesId' frontend/src/pages/PlaylistFormPage.tsx` is at least 1
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>PlaylistForm uses two-pane picker; SeriesPicker removed; pre-select from query param works.</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run src/components/playlists/TwoPanePicker.test.tsx src/components/playlists/RowSettingsSheet.test.tsx && npm run build
</verification>

<success_criteria>
- D-13–D-17: Two-pane tile UI with sheet row settings and responsive tabs
- D-18: Settings sections above picker unchanged
- D-21: SeriesPicker removed
- D-08: Advanced create path pre-selects series
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-04-SUMMARY.md` when done.
</output>
