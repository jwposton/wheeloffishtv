---
phase: 06-library-playlist-assignment
plan: 05
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-03"
files_modified:
  - frontend/src/pages/SeriesDetailPage.tsx
  - frontend/src/components/series/SeriesMetadataHero.tsx
  - frontend/src/api/types.ts
  - frontend/src/pages/SeriesDetailPage.test.tsx
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PLT-03
  - WEB-01
tags:
  - phase-6
  - series-detail
  - docs
must_haves:
  truths:
    - "Series detail renders IMDb-like hero: poster, title, year, summary, genre chips, content rating badge, studio (D-10)"
    - "Missing metadata fields omitted gracefully — no N/A placeholders (D-10)"
    - "Primary Add to playlist button + shared AddToPlaylistMenu on detail page (D-09)"
    - "ResumePreview block preserved below/beside hero (D-12)"
    - "Back link reads Back to Library (D-03)"
    - "PROJECT.md, REQUIREMENTS.md reflect ADM-01/02 cancelled and Phase 6 scope (D-01, D-02)"
  artifacts:
    - path: frontend/src/components/series/SeriesMetadataHero.tsx
      provides: "Enriched metadata hero layout"
      contains: "export function SeriesMetadataHero"
    - path: frontend/src/pages/SeriesDetailPage.tsx
      provides: "Detail page with hero + AddToPlaylist + ResumePreview"
      contains: "AddToPlaylistMenu"
    - path: frontend/src/pages/SeriesDetailPage.test.tsx
      provides: "Metadata rendering tests"
      contains: "test_renders_metadata"
  key_links:
    - from: frontend/src/pages/SeriesDetailPage.tsx
      to: frontend/src/components/series/SeriesMetadataHero.tsx
      via: "reads series.provider_metadata.summary/genres/contentRating/studio"
      pattern: "provider_metadata"
    - from: frontend/src/pages/SeriesDetailPage.tsx
      to: frontend/src/components/playlists/AddToPlaylistMenu.tsx
      via: "primary Add to playlist button wraps AddToPlaylistMenu"
      pattern: "AddToPlaylistMenu"
---

## Phase Goal

**As a** self-host operator, **I want to** see rich series metadata and add shows to playlists from the detail page, **so that** Library assignment works from both tiles and detail views (D-09, D-10, D-12, D-01, D-02).

<objective>
Wave 5 closing slice — enrich SeriesDetailPage with metadata hero (using provider_metadata from Plan 06-01), primary Add to playlist button reusing AddToPlaylistMenu from Plan 06-03, update planning docs for cancelled WheelOfFish scope (D-01, D-02).

Output: SeriesMetadataHero component, updated SeriesDetailPage, types extension if needed, docs updates, unit test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-library-playlist-assignment/06-CONTEXT.md
@.planning/phases/06-library-playlist-assignment/06-UI-SPEC.md
@frontend/src/pages/SeriesDetailPage.tsx
@frontend/src/api/types.ts
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-05-01 | Tampering | summary text rendering | mitigate | React JSX auto-escapes; metadata from trusted Plex cache only — never dangerouslySetInnerHTML |
| T-06-05-02 | Information Disclosure | provider_metadata in API | accept | Same scope as existing series detail endpoint; user-scoped CachedSeries |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeriesMetadataHero + detail page enrichment (D-09, D-10, D-12)</name>
  <files>frontend/src/components/series/SeriesMetadataHero.tsx, frontend/src/pages/SeriesDetailPage.tsx, frontend/src/api/types.ts, frontend/src/pages/SeriesDetailPage.test.tsx</files>
  <read_first>frontend/src/pages/SeriesDetailPage.tsx, frontend/src/api/types.ts, .planning/phases/06-library-playlist-assignment/06-UI-SPEC.md, frontend/src/components/playlists/AddToPlaylistMenu.tsx</read_first>
  <action>
**types.ts:** Extend `Series.provider_metadata` type (or add helper) with optional `summary?: string | null`, `genres?: string[]`, `contentRating?: string | null`, `studio?: string | null` alongside existing `ratingKey`.

**SeriesMetadataHero.tsx:**
- Props: `series: Series`
- Layout: md+ flex row (poster left w-40, content right); stacked on mobile
- Render: title (text-2xl), year, content rating Badge (outline), genre chips (Badge secondary), studio line (text-sm muted), summary (text-sm, line-clamp-4 or full)
- Omit any field when null/empty — no "N/A" (D-10)

**SeriesDetailPage.tsx:**
- Replace minimal title/poster block with `SeriesMetadataHero`
- Add primary Button "Add to playlist" wrapping `AddToPlaylistMenu` with `seriesId={series.id}` (D-09)
- Change back link text to "Back to Library" (D-03)
- Keep `ResumePreview` below hero section unchanged (D-12)

**SeriesDetailPage.test.tsx:**
- Mock series with provider_metadata `{ summary: "A spy show", genres: ["Action"], contentRating: "TV-MA", studio: "HBO" }`
- Assert summary text and genre chip visible
- Assert "Add to playlist" button present
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/pages/SeriesDetailPage.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function SeriesMetadataHero' frontend/src/components/series/SeriesMetadataHero.tsx` equals 1
    - `grep -c 'AddToPlaylistMenu' frontend/src/pages/SeriesDetailPage.tsx` equals 1
    - `grep -c 'Add to playlist' frontend/src/pages/SeriesDetailPage.tsx` equals 1
    - `grep -c 'Back to Library' frontend/src/pages/SeriesDetailPage.tsx` equals 1
    - `grep -c 'ResumePreview' frontend/src/pages/SeriesDetailPage.tsx` equals 1
    - `grep -c 'summary' frontend/src/api/types.ts` is at least 1
    - `cd frontend && npm run test -- --run src/pages/SeriesDetailPage.test.tsx` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Series detail shows enriched metadata hero + Add to playlist + ResumePreview.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs for cancelled WheelOfFish (D-01, D-02)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/06-library-playlist-assignment/06-CONTEXT.md</read_first>
  <action>
Verify and update if any stale WheelOfFish / ADM references remain:

**PROJECT.md:** Confirm no promise of household global WheelOfFish playlist; Library-centric playlist UX listed as Phase 6 deliverable (D-02). Update "Last updated" date if changed.

**REQUIREMENTS.md:** Confirm ADM-01/ADM-02 marked cancelled; PLT-03 UX traceability points to Phase 6. Update traceability table if PLT-03 UX still shows Pending — mark complete after this phase ships (note in SUMMARY; for now ensure Phase 6 mapping is correct).

**ROADMAP.md:** Phase 6 section already repurposed — verify no duplicate "Admin WheelOfFish" phase entry remains.

Do NOT change code in this task — docs only.
  </action>
  <verify>
    <automated>! grep -i 'wheeloffish.*admin\|ADM-01\|ADM-02' .planning/PROJECT.md | grep -v -i 'cancel\|removed\|scrapped' || true; grep -c 'Library' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'wheeloffish' .planning/PROJECT.md` equals 0 OR all matches are product name "Wheel of Fish TV" not global admin playlist feature
    - `grep -c 'ADM-01' .planning/REQUIREMENTS.md` is at least 1 with "Cancelled" nearby (within 3 lines)
    - `grep -c 'Library' .planning/ROADMAP.md` is at least 1 in Phase 6 section
    - No new WheelOfFish admin playlist promises introduced in any of the three files
  </acceptance_criteria>
  <done>Planning docs aligned with D-01/D-02 scope cancellation.</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run src/pages/SeriesDetailPage.test.tsx && npm run build
</verification>

<success_criteria>
- D-09, D-10, D-12: Enriched detail page with AddToPlaylist + ResumePreview preserved
- D-01, D-02: Docs reflect cancelled admin WheelOfFish
- Phase 6 vertical slice complete end-to-end
</success_criteria>

<output>
Create `.planning/phases/06-library-playlist-assignment/06-05-SUMMARY.md` when done.
</output>
