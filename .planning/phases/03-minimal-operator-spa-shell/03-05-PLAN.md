---
phase: 03-minimal-operator-spa-shell
plan: 05
type: execute
wave: 3
depends_on:
  - "03-04"
files_modified:
  - frontend/src/pages/AdminLibrarySetupPage.tsx
  - frontend/src/pages/HoldingPage.tsx
  - frontend/src/pages/SettingsLibrariesPage.tsx
  - frontend/src/components/admin/LibraryScopeForm.tsx
  - frontend/src/hooks/useLibraryScope.ts
  - frontend/src/routes/LibraryScopeGuard.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Admin sees checkbox list of all TV libraries and saves scope via PUT library-scope (D-10)"
    - "First admin login after admin env set forces Pick libraries step before browse (D-11)"
    - "Non-admin users see holding page when no libraries scoped (D-12)"
    - "Settings → Libraries allows permanent scope edits (D-11)"
  artifacts:
    - path: frontend/src/components/admin/LibraryScopeForm.tsx
      provides: "Checkbox library scope UI"
      contains: "LibraryScopeForm"
    - path: frontend/src/pages/HoldingPage.tsx
      provides: "Non-admin holding state"
      contains: "Admin hasn't finished setup"
    - path: frontend/src/routes/LibraryScopeGuard.tsx
      provides: "Route guard for scope gating"
      contains: "libraries_scoped"
  key_links:
    - from: frontend/src/components/admin/LibraryScopeForm.tsx
      to: PUT /api/v1/admin/connections/{id}/library-scope
      via: "useLibraryScope mutation"
      pattern: "library-scope"
    - from: frontend/src/routes/LibraryScopeGuard.tsx
      to: frontend/src/hooks/useAuth.ts
      via: "libraries_scoped and is_admin checks"
      pattern: "libraries_scoped"
---

## Phase Goal

**As a** admin operator, **I want to** select which TV libraries are in scope via checkboxes, **so that** household users can browse the right catalog (D-10, D-11, D-12).

<objective>
Deliver library scope admin vertical slice: first-run checklist, Settings → Libraries, non-admin holding page, route guards.

Purpose: Library scope is the one UI-managed policy exception to env-only config (D-10).
Output: LibraryScopeForm, AdminLibrarySetupPage, HoldingPage, LibraryScopeGuard.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@backend/src/wheeloffish/api/routes/catalog.py
@frontend/src/hooks/useAuth.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-05-01 | Elevation | LibraryScopeForm | mitigate | Only render for is_admin && !setup_mode; backend require_admin on PUT |
| T-03-05-02 | Tampering | useLibraryScope | mitigate | Send native_id list only; server validates against cached libraries |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: useLibraryScope hook and LibraryScopeForm</name>
  <files>frontend/src/hooks/useLibraryScope.ts, frontend/src/components/admin/LibraryScopeForm.tsx</files>
  <read_first>backend/src/wheeloffish/api/routes/catalog.py, backend/src/wheeloffish/domain/dto.py</read_first>
  <action>
Implement `useLibraryScope(connectionId)`:
- query GET /admin/connections/{connectionId}/libraries — returns Library[] with in_scope boolean
- mutation PUT /admin/connections/{connectionId}/library-scope body `{in_scope_library_native_ids: string[]}`
- on success invalidate auth/me (libraries_scoped may flip) and libraries query

`LibraryScopeForm`:
- shadcn Checkbox list per library title
- Save button triggers mutation
- toast on success/error
- utilitarian copy explaining scope affects browse catalog (D-19)
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - useLibraryScope fetches admin libraries endpoint with in_scope flags
    - PUT mutation sends in_scope_library_native_ids array
  </acceptance_criteria>
  <done>Reusable library scope form wired to admin API</done>
</task>

<task type="auto">
  <name>Task 2: First-run admin checklist and Settings → Libraries</name>
  <files>frontend/src/pages/AdminLibrarySetupPage.tsx, frontend/src/pages/SettingsLibrariesPage.tsx, frontend/src/App.tsx</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <action>
`AdminLibrarySetupPage` at `/setup/libraries` (D-11):
- AdminRoute guard: is_admin && !setup_mode
- When auth.me.libraries_scoped === false, redirect admin from /browse to this page
- Full-page checklist UI embedding LibraryScopeForm
- Continue to Browse button enabled after successful save with ≥1 library selected

`SettingsLibrariesPage` at `/settings/libraries`:
- Same LibraryScopeForm for ongoing edits
- Link from SettingsPage

Update `App.tsx` routes accordingly.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Admin with libraries_scoped=false cannot reach /browse without completing setup
    - Settings exposes Libraries section for permanent edits
  </acceptance_criteria>
  <done>Admin first-run and settings library scope flows live</done>
</task>

<task type="auto">
  <name>Task 3: Non-admin holding page and LibraryScopeGuard</name>
  <files>frontend/src/pages/HoldingPage.tsx, frontend/src/routes/LibraryScopeGuard.tsx, frontend/src/App.tsx</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <action>
`HoldingPage` (D-12):
- Message: "Admin hasn't finished setup" with utilitarian styling
- Shown when !is_admin && libraries_scoped === false

`LibraryScopeGuard` wraps /browse route:
- if setup_mode && is_admin → allow navigate to AdminSetupPage for env copy (already handled)
- if !libraries_scoped && !is_admin → render HoldingPage
- if !libraries_scoped && is_admin → redirect /setup/libraries
- else render children

Ensure setup_mode non-admin can still browse when libraries_scoped true (D-04 allows browse without admin actions).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Non-admin with libraries_scoped=false sees HoldingPage not empty grid
    - Admin without scope redirected to /setup/libraries
  </acceptance_criteria>
  <done>Library scope gating complete for all user roles</done>
</task>

</tasks>

<verification>
cd frontend && npm run build
</verification>

<success_criteria>
- Admin can scope libraries via checkbox UI (D-10)
- First-run forced pick + permanent settings path (D-11)
- Non-admins blocked with holding page until scoped (D-12)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-05-SUMMARY.md` when done
</output>
