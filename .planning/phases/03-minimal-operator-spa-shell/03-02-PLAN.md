---
phase: 03-minimal-operator-spa-shell
plan: 02
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - backend/src/wheeloffish/api/routes/oauth_plex.py
  - backend/src/wheeloffish/api/routes/oauth_jellyfin.py
  - backend/src/wheeloffish/api/routes/connections.py
  - backend/src/wheeloffish/api/routes/catalog.py
  - backend/src/wheeloffish/api/schemas/oauth.py
  - backend/src/wheeloffish/core/connections.py
  - backend/src/wheeloffish/core/auth.py
  - backend/src/wheeloffish/domain/dto.py
  - backend/src/wheeloffish/core/catalog_sync.py
  - backend/tests/api/test_auth_routes.py
  - backend/tests/api/test_catalog_routes.py
  - backend/tests/api/test_connections_routes.py
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Plex OAuth start/callback links user to env-synced connection — no client-supplied base_url (D-01, D-06)"
    - "Jellyfin auth accepts username/password only; server URL from env connection row (D-01)"
    - "OAuth callback sets session app_user_id cookie"
    - "POST /connections returns 403 for operator flow — env is sole config source (D-06)"
    - "Setup mode allows series browse but blocks admin library-scope PUT (D-04)"
  artifacts:
    - path: backend/src/wheeloffish/api/routes/oauth_plex.py
      provides: "Env-bound Plex PIN OAuth"
      contains: "sync_connection_from_env"
    - path: backend/src/wheeloffish/core/connections.py
      provides: "link_media_user helper"
      contains: "link_media_user"
  key_links:
    - from: backend/src/wheeloffish/api/routes/oauth_plex.py
      to: backend/src/wheeloffish/core/connections.py
      via: "callback upserts user_media_link against boot connection"
      pattern: "link_media_user"
    - from: backend/src/wheeloffish/api/routes/catalog.py
      to: backend/src/wheeloffish/api/deps.py
      via: "get_current_user on browse routes"
      pattern: "get_current_user"
---

## Phase Goal

**As a** operator user, **I want to** sign in with my Plex or Jellyfin account against the pre-configured server, **so that** I get a session without pasting server URLs in the UI (D-01, D-09).

<objective>
Refactor Phase 2 OAuth flows to bind media accounts to the env-synced connection row; set session on success; gate legacy connection CRUD; add admin libraries listing with in_scope flags.

Purpose: Completes media-server-only login path (D-01, D-02) and prevents config drift (D-06).
Output: Refactored oauth routes, `link_media_user`, session cookie on callback, setup_mode admin gate tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md
@backend/src/wheeloffish/api/routes/oauth_plex.py
@backend/src/wheeloffish/api/routes/oauth_jellyfin.py
@backend/src/wheeloffish/core/connections.py
@backend/src/wheeloffish/core/boot.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-02-01 | Information Disclosure | oauth_jellyfin | mitigate | Never log Jellyfin request bodies containing password |
| T-03-02-02 | Elevation | setup_mode admin PUT | mitigate | require_admin returns 403 when WOF_ADMIN_PROVIDER_USER_ID unset (D-04) |
| T-03-02-03 | Tampering | POST /connections | mitigate | Return 403 with code `env_config_only` — connection edits via .env only (D-06) |
| T-03-02-04 | Spoofing | OAuth callback | mitigate | Validate PIN state app_user_id matches session before linking token |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Plex OAuth to env connection</name>
  <files>backend/src/wheeloffish/api/routes/oauth_plex.py, backend/src/wheeloffish/api/schemas/oauth.py, backend/src/wheeloffish/core/connections.py, backend/src/wheeloffish/core/auth.py</files>
  <read_first>backend/src/wheeloffish/api/routes/oauth_plex.py, backend/src/wheeloffish/integrations/plex/auth.py, .planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <behavior>
    - Test: POST /connections/plex/oauth/start without base_url in body succeeds when boot connection exists
    - Test: callback with valid PIN sets request.session app_user_id and creates user_media_link
    - Test: start returns 422 when WOF_PROVIDER != plex
  </behavior>
  <action>
Add `link_media_user(db, vault, connection, app_user, provider_user_id, token)` in `core/connections.py`:
- upsert `UserMediaLink` for `(connection_id, app_user_id)`
- store token in vault namespace for media user

Refactor `PlexOAuthStartRequest` — remove `base_url`, `display_name`, `verify_ssl` fields; empty body or optional `{}` only.

`plex_oauth_start`:
- require `settings.WOF_PROVIDER == "plex"`
- load connection row via `sync_connection_from_env` or query by provider_type
- store pin state with connection_id + app_user_id from `get_current_user` (session required — LoginPage calls `POST /auth/bootstrap-session` on mount per Plan 01 before invoking /start)
- pin state uses env connection `base_url`/`verify_ssl` — not client input

`plex_oauth_callback`:
- exchange token, call `upsert_app_user` with Plex provider user id/username
- call `link_media_user`
- set `request.session["app_user_id"]`
- redirect to `{WOF_OAUTH_CALLBACK_BASE}/` or `/setup` when setup_mode
- trigger_sync in background as today

Update tests in `test_auth_routes.py` with respx mocks for PIN flow (`-k session_cookie`).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_auth_routes.py -k session_cookie -x</automated>
  </verify>
  <acceptance_criteria>
    - PlexOAuthStartRequest no longer accepts base_url
    - Successful callback response includes Set-Cookie for session
    - user_media_link row created against env connection id
  </acceptance_criteria>
  <done>Plex OAuth binds to env connection and establishes session</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Jellyfin auth and gate connection CRUD</name>
  <files>backend/src/wheeloffish/api/routes/oauth_jellyfin.py, backend/src/wheeloffish/api/routes/connections.py, backend/tests/api/test_connections_routes.py</files>
  <read_first>backend/src/wheeloffish/api/routes/oauth_jellyfin.py, backend/src/wheeloffish/api/routes/connections.py</read_first>
  <action>
Refactor Jellyfin auth route per D-01/D-02:
- Request body `{username, password}` only — no base_url/display_name
- Read connection from env-synced row where provider_type=jellyfin
- On success: upsert_app_user, link_media_user, set session, return JSON or redirect matching Plex pattern

Gate operator connection creation:
- `POST /api/v1/connections` → 403 `{"code":"env_config_only","message":"Configure connection in .env and restart"}` (D-06)
- Keep GET connection read endpoints for SPA settings display (D-08)

Replace `settings.enabled_providers_set` checks with `settings.WOF_PROVIDER` match in both oauth modules (D-02).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_connections_routes.py -k "post_connection" -x</automated>
  </verify>
  <acceptance_criteria>
    - POST /connections returns 403 env_config_only
    - Jellyfin auth does not accept base_url in request schema
  </acceptance_criteria>
  <done>Jellyfin login env-bound; connection CRUD gated</done>
</task>

<task type="auto">
  <name>Task 3: Session-protect catalog routes, admin libraries endpoint, setup_mode tests</name>
  <files>backend/src/wheeloffish/api/routes/catalog.py, backend/src/wheeloffish/domain/dto.py, backend/src/wheeloffish/core/catalog_sync.py, backend/tests/api/test_catalog_routes.py</files>
  <read_first>backend/src/wheeloffish/api/routes/catalog.py, backend/src/wheeloffish/domain/dto.py, .planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <action>
Add `in_scope: bool` field to `domain/dto.py` `Library` model.

Update `cached_library_to_dto` to populate `in_scope` from ORM row.

Add admin route `GET /api/v1/admin/connections/{connection_id}/libraries`:
- requires get_current_user + require_admin (blocked in setup_mode per D-04)
- returns ALL cached libraries with `in_scope` flag (not filtered list) for checkbox UI prep (D-10)

Change public `GET /connections/{id}/libraries` to require get_current_user; keep returning in-scope only for browse consumers.

Add get_current_user dependency to series browse, resume, sync routes (401 when unauthenticated).

Extend `test_catalog_routes.py`:
- setup_mode: authenticated user can GET series; PUT library-scope → 403
- admin (non-setup): PUT library-scope succeeds

Fix any Phase 2 tests broken by auth requirement using authenticated_client fixture from 03-01.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_catalog_routes.py -k setup_mode -x</automated>
  </verify>
  <acceptance_criteria>
    - Library DTO includes in_scope boolean
    - Admin GET libraries returns both scoped and unscoped libraries
    - setup_mode blocks admin PUT library-scope
  </acceptance_criteria>
  <done>Catalog API session-ready with admin library listing</done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/api/test_auth_routes.py tests/api/test_catalog_routes.py tests/api/test_connections_routes.py -q
</verification>

<success_criteria>
- Media-server OAuth/Jellyfin auth is the only login path (D-01)
- Single WOF_PROVIDER enforced on oauth routes (D-02)
- No UI/API path creates connection rows except boot sync (D-06, D-07)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-02-SUMMARY.md` when done
</output>
