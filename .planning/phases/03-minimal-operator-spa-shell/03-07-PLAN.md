---
phase: 03-minimal-operator-spa-shell
plan: 07
type: execute
wave: 5
depends_on:
  - "03-06"
files_modified:
  - frontend/src/pages/SeriesDetailPage.tsx
  - frontend/src/hooks/useSeriesResume.ts
  - frontend/src/components/browse/ResumePreview.tsx
  - frontend/src/App.tsx
  - backend/src/wheeloffish/api/routes/meta.py
  - backend/src/wheeloffish/api/schemas/meta.py
  - README.md
  - .env.example
  - .planning/phases/03-minimal-operator-spa-shell/03-UAT-CHECKLIST.md
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Series detail shows metadata and up-next/resume preview from resume API (D-16)"
    - "Detail view is read-only — no playlist actions"
    - "Keyboard: series cards focusable; Enter opens detail (ROADMAP a11y baseline)"
    - "Phase 3 docs updated; Storybook not added (D-20)"
    - "meta/providers single-provider shape verified (implemented in Plan 01; re-run test_meta_routes.py in gate)"
  artifacts:
    - path: frontend/src/pages/SeriesDetailPage.tsx
      provides: "Series detail with resume preview"
      contains: "SeriesDetailPage"
    - path: frontend/src/components/browse/ResumePreview.tsx
      provides: "Up-next episode display"
      contains: "ResumePreview"
    - path: .planning/phases/03-minimal-operator-spa-shell/03-UAT-CHECKLIST.md
      provides: "Manual keyboard and OAuth UAT steps"
      contains: "keyboard"
  key_links:
    - from: frontend/src/hooks/useSeriesResume.ts
      to: GET /api/v1/connections/{id}/series/{series_id}/resume
      via: "TanStack Query"
      pattern: "series/.*/resume"
    - from: frontend/src/pages/SeriesDetailPage.tsx
      to: frontend/src/components/browse/SeriesCard.tsx
      via: "route /series/:seriesId"
      pattern: "/series/"
---

## Phase Goal

**As a** signed-in user, **I want to** open a series and see my up-next episode, **so that** I can confirm resume semantics before playlist authoring (D-16).

<objective>
Complete Phase 3 with series detail + resume preview, meta API alignment, operator docs, and UAT checklist. Explicitly skip Storybook (D-20).

Purpose: Closes WEB-01 Phase 3 partial scope — auth, env config, library scope, browse + detail.
Output: SeriesDetailPage, ResumePreview, README/.env.example updates, 03-UAT-CHECKLIST.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@backend/src/wheeloffish/api/routes/catalog.py
@backend/src/wheeloffish/api/schemas/resume.py
@README.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-07-01 | Information Disclosure | resume API | accept | Resume data only for authenticated user with media link |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Series detail route with resume preview</name>
  <files>frontend/src/pages/SeriesDetailPage.tsx, frontend/src/hooks/useSeriesResume.ts, frontend/src/components/browse/ResumePreview.tsx, frontend/src/App.tsx</files>
  <read_first>backend/src/wheeloffish/api/schemas/resume.py, backend/src/wheeloffish/core/resume.py, .planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <action>
Implement routed detail page at `/series/:seriesId` (D-16 discretion: routed page over drawer for deep links/a11y).

`useSeriesResume(connectionId, seriesId)`:
- GET /connections/{connectionId}/series/{seriesId}/resume
- TanStack Query with staleTime 60s

`ResumePreview`:
- Display episode title, season/episode numbers, watch state label
- Show "Up next" / "Resume" heading based on cursor semantics from API
- Read-only — no action buttons for playlists

`SeriesDetailPage`:
- Parse seriesId composite key from route param
- Find series metadata from browse cache or optional lightweight title from resume response
- Back link to /browse
- shadcn Sheet NOT required if using full page — ensure focus management on mount

Wire route in App.tsx inside ProtectedRoute + LibraryScopeGuard.

Ensure SeriesCard keyboard: Enter key activates navigation (ROADMAP keyboard nav baseline).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Detail route loads resume preview from API
    - No add-to-playlist or rebuild controls rendered
    - seriesId uses composite ID from catalog items
  </acceptance_criteria>
  <done>Series detail vertical slice with resume preview complete</done>
</task>

<task type="auto">
  <name>Task 2: Phase 3 documentation, UAT checklist, full suite gate</name>
  <files>README.md, .env.example, .planning/phases/03-minimal-operator-spa-shell/03-UAT-CHECKLIST.md</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <action>
Update README Phase 3 section:
- Operator configures .env (WOF_PROVIDER, WOF_MEDIA_SERVER_URL, WOF_ADMIN_PROVIDER_USER_ID) before compose up (D-06, D-09)
- Admin discovery flow: copy provider_user_id from /setup/admin (D-04)
- Connection changes require .env edit + restart (D-08)
- Frontend dev: cd frontend && npm run dev with backend on :8000
- Explicit note: Storybook deferred to Phase 7 (D-20)

Update `.env.example` with all Phase 3 vars; deprecate WOF_ENABLED_PROVIDERS comment in favor of WOF_PROVIDER.

Create `03-UAT-CHECKLIST.md`:
- Plex OAuth E2E steps
- Jellyfin credential login steps
- Admin library scope checkbox persistence
- Keyboard: Tab through browse grid; Enter opens detail
- Sync banner visible during catalog sync
- Light/dark theme toggle

Run full verification command from 03-VALIDATION.md as plan gate.
  </action>
  <verify>
    <automated>cd backend && uv run ruff check . && uv run pytest && cd ../frontend && npm run test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - README documents env-only connection config and admin discovery
    - 03-UAT-CHECKLIST.md includes keyboard nav and OAuth manual steps
    - Full backend + frontend test suites green
    - No Storybook files added
  </acceptance_criteria>
  <done>Phase 3 closure docs and verification gate complete</done>
</task>

</tasks>

<verification>
cd backend && uv run ruff check . && uv run pytest && cd ../frontend && npm run test -- --run && npm run build
</verification>

<success_criteria>
- User can browse, search, open detail, see resume pointer (D-16, WEB-01 partial)
- ROADMAP keyboard nav baseline addressable via UAT checklist
- All CONTEXT decisions D-01–D-19 implemented; D-20 explicitly deferred
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-07-SUMMARY.md` when done
</output>
