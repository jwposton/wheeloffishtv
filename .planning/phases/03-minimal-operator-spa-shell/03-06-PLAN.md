---
phase: 03-minimal-operator-spa-shell
plan: 06
type: execute
wave: 4
depends_on:
  - "03-05"
files_modified:
  - frontend/src/pages/BrowsePage.tsx
  - frontend/src/hooks/useDebouncedValue.ts
  - frontend/src/hooks/useSeriesInfiniteQuery.ts
  - frontend/src/hooks/useBrowseLayout.ts
  - frontend/src/components/browse/SeriesGrid.tsx
  - frontend/src/components/browse/SeriesList.tsx
  - frontend/src/components/browse/SeriesCard.tsx
  - frontend/src/components/layout/SyncBanner.tsx
  - frontend/src/components/browse/BrowseToolbar.tsx
  - frontend/src/hooks/useDebouncedValue.test.ts
  - frontend/src/hooks/useSeriesInfiniteQuery.test.ts
  - frontend/src/hooks/useBrowseLayout.test.ts
  - frontend/src/components/layout/SyncBanner.test.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Series browse loads paginated catalog with infinite scroll (D-14)"
    - "Debounced search filters via ?q= substring match (D-14)"
    - "Grid default with list toggle persisted in localStorage (D-13)"
    - "Sync running shows non-blocking top banner while stale series remain visible (D-15)"
  artifacts:
    - path: frontend/src/hooks/useSeriesInfiniteQuery.ts
      provides: "TanStack infinite query for series"
      contains: "useInfiniteQuery"
    - path: frontend/src/components/layout/SyncBanner.tsx
      provides: "Updating library banner"
      contains: "Updating library"
    - path: frontend/src/pages/BrowsePage.tsx
      provides: "Read-only series browser"
      contains: "BrowsePage"
  key_links:
    - from: frontend/src/hooks/useSeriesInfiniteQuery.ts
      to: GET /api/v1/connections/{id}/series
      via: "page limit q params"
      pattern: "connections/.*/series"
    - from: frontend/src/components/layout/SyncBanner.tsx
      to: SeriesBrowseResponse.sync
      via: "sync.status === running"
      pattern: "sync\\.status"
---

## Phase Goal

**As a** signed-in user, **I want to** search and scroll through my scoped TV series with sync status visible, **so that** I can explore the catalog before building playlists (D-13, D-14, D-15).

<objective>
Ship read-only series browser vertical slice: infinite scroll, debounced search, grid/list layout toggle, sync banner.

Purpose: Core browse experience wired to Phase 2 catalog API shapes (WEB-01 partial).
Output: BrowsePage, infinite query hooks, SyncBanner, unit/component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@backend/src/wheeloffish/api/routes/catalog.py
@backend/src/wheeloffish/api/schemas/catalog.py
@frontend/src/routes/LibraryScopeGuard.tsx
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-06-01 | Denial of Service | search input | mitigate | Debounce 300ms; rely on backend page limit max 200 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Debounced search and infinite query hooks</name>
  <files>frontend/src/hooks/useDebouncedValue.ts, frontend/src/hooks/useSeriesInfiniteQuery.ts, frontend/src/hooks/useDebouncedValue.test.ts, frontend/src/hooks/useSeriesInfiniteQuery.test.ts, frontend/src/api/types.ts</files>
  <read_first>backend/src/wheeloffish/api/schemas/catalog.py, .planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <behavior>
    - Test: useDebouncedValue delays value update by 300ms
    - Test: useSeriesInfiniteQuery getNextPageParam returns page+1 while page*limit less than total
    - Test: changing debounced q resets to page 1 via queryKey change
  </behavior>
  <action>
Add TypeScript types for SeriesBrowseResponse, Series, SyncStatusEmbed matching backend schemas.

`useDebouncedValue(value, 300)` — standard debounce hook (D-14 discretion).

`useSeriesInfiniteQuery(connectionId, debouncedQ)`:
- queryKey ['series', connectionId, debouncedQ]
- GET /connections/{connectionId}/series?page={pageParam}&limit=50&q={debouncedQ}
- initialPageParam 1
- getNextPageParam: last.page * last.limit less than last.total ? last.page + 1 : undefined
- refetchInterval: 3000 while any page sync.status === 'running' (D-15 polling)

Write unit tests with mock fetch/responses.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/hooks/useDebouncedValue.test.ts src/hooks/useSeriesInfiniteQuery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Infinite query uses limit 50 default matching WOF_CATALOG_PAGE_DEFAULT
    - Debounce interval 300ms in hook tests
  </acceptance_criteria>
  <done>Pagination and search hooks tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Browse layout toggle, grid/list components, SyncBanner</name>
  <files>frontend/src/hooks/useBrowseLayout.ts, frontend/src/components/browse/SeriesGrid.tsx, frontend/src/components/browse/SeriesList.tsx, frontend/src/components/browse/SeriesCard.tsx, frontend/src/components/browse/BrowseToolbar.tsx, frontend/src/components/layout/SyncBanner.tsx, frontend/src/hooks/useBrowseLayout.test.ts, frontend/src/components/layout/SyncBanner.test.tsx</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <behavior>
    - Test: useBrowseLayout persists 'grid'|'list' to localStorage key wof.browse.layout
    - Test: SyncBanner visible when sync.status=running
  </behavior>
  <action>
`useBrowseLayout` (D-13):
- localStorage key `wof.browse.layout`
- default 'grid'
- toggle function

`SeriesCard`:
- poster thumb, title
- render as button element for keyboard focus (a11y baseline)
- onClick navigates to `/series/{composite_id}`

`SeriesGrid` / `SeriesList` — map items from flattened infinite pages

`BrowseToolbar`:
- search Input debounced
- grid/list ToggleGroup or icon buttons

`SyncBanner` (D-15):
- fixed top banner "Updating library…" when sync.status === 'running'
- does not block interaction with list below

Component test SyncBanner with mock sync embed.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/hooks/useBrowseLayout.test.ts src/components/layout/SyncBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Grid is default layout mode
    - SyncBanner renders when status running
    - SeriesCard uses button or tabIndex=0 with visible focus ring
  </acceptance_criteria>
  <done>Browse UI components with layout preference and sync UX</done>
</task>

<task type="auto">
  <name>Task 3: BrowsePage with infinite scroll intersection</name>
  <files>frontend/src/pages/BrowsePage.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/routes/LibraryScopeGuard.tsx, frontend/src/hooks/useAuth.ts</read_first>
  <action>
Implement `BrowsePage` at `/browse`:
- Wrap with LibraryScopeGuard
- Read connectionId from useAuth().connection.id
- Wire useSeriesInfiniteQuery + useBrowseLayout
- IntersectionObserver sentinel at list bottom calls fetchNextPage when inView
- Flatten pages into items array; show skeleton rows while fetching
- Empty state when total=0 and not loading
- SyncBanner reads sync from first page's sync embed
- Read-only — no add-to-playlist actions

Register route in App.tsx inside ProtectedRoute + AppShell.

Utilitarian spacing/typography (D-19); theme-aware poster placeholders.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - BrowsePage renders grid by default with infinite scroll sentinel
    - Search input debounces API calls
    - Stale series visible during sync.running
  </acceptance_criteria>
  <done>End-to-end browse slice functional for scoped libraries</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run && npm run build
</verification>

<success_criteria>
- Infinite scroll + substring search work against catalog API (D-14)
- Grid/list preference persists locally (D-13)
- Non-blocking sync banner during background sync (D-15)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-06-SUMMARY.md` when done
</output>
