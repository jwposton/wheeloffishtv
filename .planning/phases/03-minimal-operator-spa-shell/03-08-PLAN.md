# Phase 3 Plan 08: UAT Gap Fixes

**Mode:** gap_closure  
**Source:** `.planning/phases/03-minimal-operator-spa-shell/03-UAT.md`

## Objective

Fix Phase 3 UAT gaps: non-admin poster images, resume preview failures, and clarify holding-page behavior.

## Tasks

### Task 1: Fix artwork proxy for home/managed Plex users

**Files:** `backend/src/wheeloffish/api/routes/catalog.py`, `backend/src/wheeloffish/core/auth.py`, `backend/tests/api/test_catalog_routes.py`

**Action:**
- In `get_connection_artwork`, when requesting user's token returns `ProviderUnauthorized` or `not_found`, retry with admin user's vault token (resolve admin `AppUser` via `WOF_ADMIN_PROVIDER_USER_ID` / username match).
- Add test: non-admin session requests artwork path cached from admin sync → 200 image bytes.

**Verify:** `uv run pytest tests/api/test_catalog_routes.py -k artwork -q`

### Task 2: Optimize and harden resume preview API

**Files:** `backend/src/wheeloffish/integrations/plex/client.py`, `backend/src/wheeloffish/api/routes/catalog.py`, `backend/src/wheeloffish/db/models/cached_series.py`, `backend/tests/api/test_catalog_routes.py`

**Action:**
- When serving resume, load `CachedSeries.provider_metadata["ratingKey"]` if present to skip `resolve_guid_to_rating_key`.
- Refactor PlexProvider to accept optional pre-resolved rating key; resolve once per resume request.
- Run `list_episodes` and `get_on_deck_episode` concurrently via `asyncio.gather`.
- Apply same admin-token fallback as artwork when user token fails guid/metadata access.
- Add API test covering resume for non-admin linked user.

**Verify:** `uv run pytest tests/api/test_catalog_routes.py -k resume -q`

### Task 3: Holding page re-verification (no code change unless test fails)

**Files:** `frontend/src/routes/LibraryScopeGuard.tsx` (read-only unless bug found)

**Action:**
- Document in UAT notes: test 8 requires `libraries_scoped=false` (clear all in_scope flags in Settings → Libraries or DB).
- If holding page still missing with zero scoped libraries, fix guard to check `libraries_scoped` before allowing `/browse` in setup_mode for non-admins.

**Verify:** Manual — non-admin + zero scoped libraries → HoldingPage.

## Success Criteria

- Non-admin browse shows poster images (same as admin)
- Series detail resume preview loads within reasonable time for series with watch history
- Holding page confirmed when no libraries scoped (or documented UX gap closed)
