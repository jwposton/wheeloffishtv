---
phase: 03-minimal-operator-spa-shell
plan: 04
type: execute
wave: 2
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - frontend/src/hooks/useAuth.ts
  - frontend/src/api/types.ts
  - frontend/src/routes/ProtectedRoute.tsx
  - frontend/src/routes/AdminRoute.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/AdminSetupPage.tsx
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/components/auth/PlexLoginButton.tsx
  - frontend/src/components/auth/JellyfinLoginForm.tsx
  - frontend/src/components/auth/AdminSetupPanel.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/App.tsx
  - frontend/src/routes/ProtectedRoute.test.tsx
  - frontend/src/test/setup.ts
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Unauthenticated users see login wall only — no server setup wizard (D-09)"
    - "Plex installs show Sign in with Plex; Jellyfin shows username/password form (D-01)"
    - "Setup mode shows provider user ID copy panel for operator (D-04)"
    - "Settings shows read-only Connected to {url} (from configuration) (D-08)"
  artifacts:
    - path: frontend/src/pages/LoginPage.tsx
      provides: "OAuth-only login wall"
      contains: "LoginPage"
    - path: frontend/src/pages/AdminSetupPage.tsx
      provides: "Admin discovery copy screen"
      contains: "provider_user_id"
    - path: frontend/src/hooks/useAuth.ts
      provides: "TanStack Query /auth/me"
      contains: "useAuth"
  key_links:
    - from: frontend/src/routes/ProtectedRoute.tsx
      to: /api/v1/auth/me
      via: "useAuth redirect when unauthenticated"
      pattern: "auth/me"
    - from: frontend/src/pages/AdminSetupPage.tsx
      to: frontend/src/hooks/useAuth.ts
      via: "setup_mode gate"
      pattern: "setup_mode"
---

## Phase Goal

**As a** household user, **I want to** sign in through my media server and see admin setup guidance when needed, **so that** I can access the app without configuring server URLs in the UI (D-01, D-04, D-09).

<objective>
Wire auth vertical slice: login wall, Plex OAuth button / Jellyfin form, protected routing, admin discovery screen, read-only connection settings.

Purpose: First end-to-end user path from `/` → login → authenticated shell (MVP slice).
Output: LoginPage, AdminSetupPage, ProtectedRoute, useAuth hook, component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md
@backend/src/wheeloffish/api/routes/auth.py
@backend/src/wheeloffish/api/routes/oauth_plex.py
@backend/src/wheeloffish/api/routes/oauth_jellyfin.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-04-01 | Spoofing | LoginPage | mitigate | No local password auth UI — media OAuth/creds only (D-01) |
| T-03-04-02 | Information Disclosure | AdminSetupPanel | accept | Displays provider_user_id intentionally for operator copy (D-04) |
| T-03-04-03 | Tampering | JellyfinLoginForm | mitigate | POST over HTTPS; clear password field on submit; no console.log |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAuth hook and ProtectedRoute with failing test first</name>
  <files>frontend/src/hooks/useAuth.ts, frontend/src/api/types.ts, frontend/src/routes/ProtectedRoute.tsx, frontend/src/routes/ProtectedRoute.test.tsx, frontend/src/test/setup.ts</files>
  <read_first>backend/src/wheeloffish/api/schemas/auth.py, frontend/src/api/client.ts</read_first>
  <behavior>
    - Test: ProtectedRoute redirects to /login when useAuth returns isLoading=false and user=null
    - Test: ProtectedRoute renders children when user present
  </behavior>
  <action>
Define `AuthMeResponse` TypeScript interface matching backend fields: app_user_id, provider_user_id, provider_username, is_admin, setup_mode, connection, has_media_link, libraries_scoped.

Implement `useAuth()` with TanStack Query:
- queryKey ['auth','me']
- queryFn GET /auth/me
- retry false on 401

Implement `ProtectedRoute`:
- while loading show skeleton
- if error/401 navigate to `/login` with return URL
- else render Outlet/children

Add vitest + MSW or fetch mock in `ProtectedRoute.test.tsx`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/routes/ProtectedRoute.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AuthMeResponse type includes setup_mode and is_admin
    - ProtectedRoute test passes redirect behavior
  </acceptance_criteria>
  <done>Auth gate enforced client-side</done>
</task>

<task type="auto">
  <name>Task 2: LoginPage with Plex OAuth and Jellyfin credential form</name>
  <files>frontend/src/pages/LoginPage.tsx, frontend/src/components/auth/PlexLoginButton.tsx, frontend/src/components/auth/JellyfinLoginForm.tsx, frontend/src/App.tsx</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md, backend/src/wheeloffish/api/routes/oauth_plex.py</read_first>
  <action>
Create `LoginPage` per D-09 — single purpose sign-in; NO server URL fields.

`PlexLoginButton`:
- POST /connections/plex/oauth/start → open auth_url same window (or popup + poll /status/{pin_id} per discretion)
- on success invalidate auth/me query

`JellyfinLoginForm`:
- username/password fields
- POST /connections/jellyfin/auth (match actual route path from backend)
- display utilitarian copy explaining credential login (D-19)

Read provider from GET /meta/providers or auth/me connection.provider — show only matching provider UI for WOF_PROVIDER install (D-02).

Wire routes in `App.tsx`:
- `/login` public
- `/` protected placeholder home
- No connection wizard routes (D-09)
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - LoginPage has no base_url or provider selector inputs
    - Plex button calls oauth start endpoint
    - Jellyfin form posts credentials to backend auth route
  </acceptance_criteria>
  <done>Users can initiate media-server login from SPA</done>
</task>

<task type="auto">
  <name>Task 3: Admin setup screen and read-only Settings</name>
  <files>frontend/src/pages/AdminSetupPage.tsx, frontend/src/pages/SettingsPage.tsx, frontend/src/components/auth/AdminSetupPanel.tsx, frontend/src/components/layout/AppShell.tsx, frontend/src/routes/AdminRoute.tsx</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md</read_first>
  <action>
`AdminSetupPanel` (D-04):
- When auth.me.setup_mode === true, route authenticated users to `/setup/admin`
- Display provider_user_id prominently with copy-to-clipboard button
- Show provider_username/email if present
- Instructions: add WOF_ADMIN_PROVIDER_USER_ID to .env and restart container
- Non-blocking — user can navigate away to browse if libraries_scoped (link to /browse)

`SettingsPage` (D-08):
- Section "Connection" showing read-only: Connected to {connection.base_url} ({connection.display_name}) — from configuration
- No edit controls; helper text: edit .env and restart

`AppShell`:
- header nav: Browse, Settings, theme toggle, logout (POST /auth/logout)
- utilitarian layout (D-19)

`AdminRoute` wrapper — requires is_admin && !setup_mode for admin-only pages later.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run src/routes/ProtectedRoute.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AdminSetupPage renders provider_user_id copy UI when setup_mode
    - SettingsPage shows connection URL without edit inputs
    - Logout clears session via POST /auth/logout
  </acceptance_criteria>
  <done>Auth UI vertical slice complete through settings shell</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run && npm run build
</verification>

<success_criteria>
- Login wall is the only pre-auth experience (D-09)
- Admin discovery guides operator to set WOF_ADMIN_PROVIDER_USER_ID (D-04)
- Connection display is read-only (D-08)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-04-SUMMARY.md` when done
</output>
