---
phase: 03-minimal-operator-spa-shell
plan: 03
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/components.json
  - frontend/src/components/ui/
  - frontend/src/components/layout/ThemeProvider.tsx
  - frontend/src/api/client.ts
  - backend/src/wheeloffish/api/spa.py
  - backend/src/wheeloffish/core/config.py
  - backend/src/wheeloffish/main.py
  - backend/Dockerfile
  - backend/tests/api/test_spa_routes.py
  - compose.yml
  - frontend/README.md
autonomous: false
requirements:
  - WEB-01
user_setup: []
must_haves:
  truths:
    - "GET / serves Vite-built index.html with 200"
    - "GET /browse returns index.html fallback for client routing"
    - "Frontend dev server proxies /api to backend on port 8000"
    - "Docker image includes frontend dist at SPA_DIST_DIR"
    - "Light and dark theme toggle works with system default (D-18)"
  artifacts:
    - path: frontend/vite.config.ts
      provides: "Vite + Tailwind + /api proxy"
      contains: "proxy"
    - path: backend/src/wheeloffish/api/spa.py
      provides: "SPAStaticFiles 404→index.html"
      contains: "SPAStaticFiles"
    - path: frontend/src/components/layout/ThemeProvider.tsx
      provides: "next-themes wrapper"
      contains: "ThemeProvider"
  key_links:
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/api/spa.py
      via: "mount SPA after API routers"
      pattern: "SPAStaticFiles"
    - from: frontend/src/api/client.ts
      to: /api/v1
      via: "fetch credentials include"
      pattern: "credentials.*include"
---

## Phase Goal

**As a** operator, **I want to** open the app URL and see the React shell with working API proxy and theme support, **so that** subsequent auth and browse slices deploy on a real frontend stack (D-17, D-18, D-19).

<objective>
Bootstrap Vite+React+TS+Tailwind+shadcn+TanStack Query frontend; mount SPA from FastAPI; multi-stage Docker build. Storybook explicitly skipped per D-20.

Purpose: Delivers thin vertical slice — browser loads SPA at `/` from same container port (Phase 1 D-06).
Output: `frontend/` scaffold, `SPAStaticFiles`, Docker build stage, vitest config, theme tokens.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md
@.planning/phases/03-minimal-operator-spa-shell/03-VALIDATION.md
@backend/src/wheeloffish/main.py
@backend/Dockerfile
@frontend/README.md
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-03-01 | Tampering | npm packages | mitigate | Human checkpoint verifies [ASSUMED] packages on npmjs.com before install |
| T-03-03-02 | Information Disclosure | api/client.ts | mitigate | credentials: 'include' only — no token storage in localStorage |
| T-03-03-SC | Tampering | npm install | mitigate | blocking checkpoint for [ASSUMED] packages per Package Legitimacy Gate |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <files>frontend/package.json</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <action>
BLOCKING gate before any npm install (T-03-03-SC). Human verifies [ASSUMED] packages from RESEARCH Package Legitimacy Audit:
- vite, react, typescript, tailwindcss, @tanstack/react-query, react-router-dom, next-themes, lucide-react, vitest, @testing-library/react
Confirm each exists on npmjs.com with expected publisher; no typosquat package names in install commands.
Resume signal: reply "approved" to proceed to Task 2 scaffold.
  </action>
  <verify>
    <automated>test -f frontend/package.json || echo "awaiting human approval before npm install"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirmed package names on npmjs.com
    - No [SLOP] packages in install list
  </acceptance_criteria>
  <done>Human approved npm package list; executor may run npm install</done>
  <what-built>Package list from RESEARCH standard_stack marked [ASSUMED]</what-built>
  <how-to-verify>
    1. Open npmjs.com and confirm packages exist with expected publishers
    2. Confirm no typosquat names in install commands
    3. Reply "approved" to proceed with scaffold
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold frontend with Vite, shadcn, TanStack Query, theme</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/vitest.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/components.json, frontend/src/components/ui/button.tsx, frontend/src/components/layout/ThemeProvider.tsx, frontend/src/api/client.ts, frontend/README.md</files>
  <read_first>.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md, .planning/research/SUMMARY.md</read_first>
  <action>
Initialize greenfield frontend per D-17:
- `npm create vite@latest . -- --template react-ts` in frontend/
- Install: tailwindcss @tailwindcss/vite @tanstack/react-query react-router-dom next-themes lucide-react
- Run `npx shadcn@latest init` — neutral/utilitarian CSS variables per D-19 (not cinematic)
- Add shadcn components: button, input, card, sheet, checkbox, label, separator, skeleton, badge, dropdown-menu, toast

Create `src/api/client.ts`:
- base path `/api/v1`
- `fetchJson(path, init)` with `credentials: 'include'`

Create `src/main.tsx`:
- QueryClientProvider, BrowserRouter, ThemeProvider (next-themes, defaultTheme="system", D-18)
- Minimal `App.tsx` placeholder: "Wheel of Fish TV" heading + ThemeToggle

Configure `vite.config.ts`:
- `@` alias to `src/`
- `base: "/"`
- dev server proxy `/api` → `http://localhost:8000`

Add `vitest.config.ts` + `@testing-library/react` + jsdom; npm script `"test": "vitest"`.

Update frontend/README.md with dev commands (`npm run dev`, `npm run build`, `npm run test`).

Do NOT add Storybook — deferred D-20.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run 2>/dev/null || test -f vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json includes react, vite, @tanstack/react-query, tailwindcss, next-themes
    - vite.config.ts proxies /api to port 8000
    - ThemeProvider wraps app with system default
    - No storybook dependency added
  </acceptance_criteria>
  <done>Frontend scaffold builds; vitest configured</done>
</task>

<task type="auto">
  <name>Task 3: Mount SPAStaticFiles, Docker multi-stage build, spa route tests</name>
  <files>backend/src/wheeloffish/api/spa.py, backend/src/wheeloffish/core/config.py, backend/src/wheeloffish/main.py, backend/Dockerfile, backend/tests/api/test_spa_routes.py, compose.yml</files>
  <read_first>backend/src/wheeloffish/main.py, backend/Dockerfile, .planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <action>
Add `Settings.SPA_DIST_DIR: str = "/app/static/spa"` (or relative path for local dev).

Implement `api/spa.py` class `SPAStaticFiles(StaticFiles)` — on 404 return `index.html` for client routing.

Update `main.py`:
- Remove JSON `@app.get("/")` root handler
- Register all API routers FIRST
- `app.mount("/", SPAStaticFiles(directory=settings.SPA_DIST_DIR, html=True), name="spa")` LAST

Update `backend/Dockerfile`:
- Add stage `frontend-build` (node:22-alpine): COPY frontend/, npm ci, npm run build
- COPY --from=frontend-build /frontend/dist → /app/static/spa in runtime stage

Add `backend/tests/api/test_spa_routes.py`:
- when SPA_DIST_DIR points to temp dir with index.html, GET / returns 200 text/html
- GET /browse returns 200 index.html fallback (`-k fallback`)

For local dev without Docker: document copying `frontend/dist` or optional env `SPA_DIST_DIR=../frontend/dist` in README snippet.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_spa_routes.py -k "index or fallback" -x</automated>
  </verify>
  <acceptance_criteria>
    - API routes registered before SPA mount
    - test_spa_routes confirms / and /browse serve index.html
    - Dockerfile multi-stage includes frontend build
  </acceptance_criteria>
  <done>Production container serves React SPA on port 8000</done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/api/test_spa_routes.py -q && cd ../frontend && npm run build
</verification>

<success_criteria>
- Operator hits http://localhost:8000/ and receives React shell
- Deep link /browse refreshes without 404
- Utilitarian theme tokens in place for Phase 7 polish (D-19)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-03-SUMMARY.md` when done
</output>
