---
phase: 03-minimal-operator-spa-shell
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/alembic/versions/003_app_users.py
  - backend/src/wheeloffish/db/models/app_user.py
  - backend/src/wheeloffish/db/models/__init__.py
  - backend/src/wheeloffish/core/config.py
  - backend/src/wheeloffish/core/boot.py
  - backend/src/wheeloffish/core/auth.py
  - backend/src/wheeloffish/api/deps.py
  - backend/src/wheeloffish/api/routes/auth.py
  - backend/src/wheeloffish/api/routes/meta.py
  - backend/src/wheeloffish/api/schemas/auth.py
  - backend/src/wheeloffish/api/schemas/meta.py
  - backend/src/wheeloffish/main.py
  - backend/tests/api/test_meta_routes.py
  - backend/tests/unit/test_boot_sync.py
  - backend/tests/api/test_auth_routes.py
  - backend/tests/conftest.py
  - .env.example
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Container boot upserts exactly one connections row from env (WOF_PROVIDER, WOF_MEDIA_SERVER_URL)"
    - "Unauthenticated GET /api/v1/auth/me returns 401"
    - "require_admin returns 403 for non-admin session users"
    - "Session cookie max_age honors WOF_SESSION_DAYS when set; unset yields long-lived cookie per D-05"
    - "POST /api/v1/auth/bootstrap-session creates session before OAuth start (unauthenticated login wall)"
    - "GET /api/v1/meta/providers returns single WOF_PROVIDER plus oauth_callback_base (D-02)"
  artifacts:
    - path: backend/src/wheeloffish/core/boot.py
      provides: "sync_connection_from_env lifespan hook"
      contains: "sync_connection_from_env"
    - path: backend/src/wheeloffish/api/routes/auth.py
      provides: "GET /auth/me, POST /auth/logout"
      exports: ["router"]
    - path: backend/alembic/versions/003_app_users.py
      provides: "app_users table migration"
      contains: "app_users"
  key_links:
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/core/boot.py
      via: "lifespan calls sync_connection_from_env"
      pattern: "sync_connection_from_env"
    - from: backend/src/wheeloffish/api/deps.py
      to: backend/src/wheeloffish/db/models/app_user.py
      via: "get_current_user session lookup"
      pattern: "request\\.session\\.get\\(\"app_user_id\"\\)"
---

## Phase Goal

**As a** self-host operator, **I want to** run the app with env-driven connection config and real session identity, **so that** OAuth login can bind users to a single pre-configured media server (D-06, D-07).

<objective>
Ship backend session foundation: `app_users` migration, env→DB boot sync, SessionMiddleware, `/auth/me` with admin/setup flags, and auth test scaffold.

Purpose: Wave 0 replaces Phase 2 stubs (`STUB_APP_USER_ID`, no-op `require_admin`) with production session auth (D-01 foundation, D-03, D-04, D-05, D-02 config). Boot sync upserts env→DB connection row on every startup (D-07).
Output: `003_app_users` migration, `core/boot.py`, `core/auth.py`, `api/routes/auth.py`, green unit/integration auth tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md
@.planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md
@.planning/phases/03-minimal-operator-spa-shell/03-VALIDATION.md
@.planning/ROADMAP.md
@backend/src/wheeloffish/api/deps.py
@backend/src/wheeloffish/core/config.py
@backend/src/wheeloffish/main.py
@backend/src/wheeloffish/db/models/connection.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01-01 | Spoofing | SessionMiddleware | mitigate | httpOnly signed cookie; `app_user_id` only in session — never store media tokens in cookie (D-01) |
| T-03-01-02 | Elevation | require_admin | mitigate | Match `AppUser.provider_user_id` to `WOF_ADMIN_PROVIDER_USER_ID`; optional `WOF_ADMIN_USERNAME` secondary match only (D-03) |
| T-03-01-03 | Tampering | core/boot.py | mitigate | Idempotent upsert by `provider_type`; reject boot if `WOF_PROVIDER` not in `{plex,jellyfin}` (D-02) |
| T-03-01-SC | Tampering | pip/uv deps | accept | No new runtime deps; existing Starlette SessionMiddleware only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add app_users migration and env config fields</name>
  <files>backend/alembic/versions/003_app_users.py, backend/src/wheeloffish/db/models/app_user.py, backend/src/wheeloffish/db/models/__init__.py, backend/src/wheeloffish/core/config.py, .env.example</files>
  <read_first>backend/alembic/versions/002_connections_catalog.py, backend/src/wheeloffish/db/models/user_media_link.py, .planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <behavior>
    - Test: sync_connection_from_env creates one Connection row when DB empty and env has WOF_PROVIDER=plex
    - Test: second boot call updates base_url without creating duplicate provider row
    - Test: invalid WOF_PROVIDER raises at settings validation
  </behavior>
  <action>
Create Alembic migration `003_app_users.py` (`down_revision='002_connections_catalog'`) with table `app_users`:
- columns: `id` (UUID PK string), `provider_user_id` (string, unique), `provider_username` (nullable string), `provider_email` (nullable string), `created_at`, `updated_at`
- index UNIQUE on `provider_user_id`

Add SQLAlchemy model `AppUser` in `db/models/app_user.py`; export from `db/models/__init__.py`.

Extend `Settings` in `core/config.py` per D-02/D-05/D-06:
- `WOF_PROVIDER: Literal["plex","jellyfin"]` (required — replaces dual-provider UX gate; keep `WOF_ENABLED_PROVIDERS` deprecated or derive enabled set from single provider for backward compat in tests)
- `WOF_MEDIA_SERVER_URL: str`
- `WOF_MEDIA_SERVER_DISPLAY_NAME: str = "Media Server"`
- `WOF_VERIFY_SSL: bool = True`
- `WOF_ADMIN_PROVIDER_USER_ID: str = ""`
- `WOF_ADMIN_USERNAME: str = ""`
- `WOF_SESSION_DAYS: int | None = None`
- computed `session_max_age_seconds: int | None` — None when `WOF_SESSION_DAYS` unset (long-lived per D-05)

Implement `core/boot.py` function `sync_connection_from_env(db: Session, settings: Settings) -> Connection`:
- upsert single row where `Connection.provider_type == settings.WOF_PROVIDER`
- set `base_url`, `display_name`, `verify_ssl` from env
- call from lifespan in `main.py` after DB session available

Update `.env.example` documenting Phase 3 vars; note `WOF_ENABLED_PROVIDERS` superseded by `WOF_PROVIDER` for operator installs (D-02).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_boot_sync.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Migration creates `app_users` with UNIQUE(provider_user_id)
    - `sync_connection_from_env` is idempotent across two calls
    - `.env.example` documents WOF_PROVIDER, WOF_MEDIA_SERVER_URL, WOF_ADMIN_PROVIDER_USER_ID, WOF_SESSION_DAYS
  </acceptance_criteria>
  <done>Boot sync unit tests green; config fields validated</done>
</task>

<task type="auto">
  <name>Task 2: Wire SessionMiddleware, auth routes, and deps</name>
  <files>backend/src/wheeloffish/core/auth.py, backend/src/wheeloffish/api/deps.py, backend/src/wheeloffish/api/routes/auth.py, backend/src/wheeloffish/api/schemas/auth.py, backend/src/wheeloffish/main.py</files>
  <read_first>backend/src/wheeloffish/api/deps.py, .planning/phases/03-minimal-operator-spa-shell/03-CONTEXT.md, .planning/phases/03-minimal-operator-spa-shell/03-RESEARCH.md</read_first>
  <action>
Add `starlette.middleware.sessions.SessionMiddleware` to `main.py` BEFORE route registration:
- `secret_key=settings.WOF_SECRET_KEY` (64 hex bytes — existing vault key)
- `max_age=settings.session_max_age_seconds`
- `same_site="lax"`, `https_only=(settings.ENVIRONMENT == "production")`

Implement `core/auth.py`:
- `is_admin(app_user: AppUser, settings: Settings) -> bool` — primary match `provider_user_id == WOF_ADMIN_PROVIDER_USER_ID`; secondary optional username/email match (D-03)
- `is_setup_mode(settings: Settings) -> bool` — True when `WOF_ADMIN_PROVIDER_USER_ID` empty (D-04)
- `upsert_app_user(db, provider_user_id, provider_username, provider_email) -> AppUser`
- `libraries_scoped(db, connection_id) -> bool` — any `CachedLibrary.in_scope` True

Replace stubs in `api/deps.py`:
- `get_current_user(request, db) -> AppUser` — read `request.session["app_user_id"]`; 401 `{"code":"unauthenticated"}` if missing
- `require_admin` — 403 if not admin OR setup_mode active (D-04 blocks admin actions)
- Remove `STUB_APP_USER_ID` usage from deps (keep constant only in tests if needed)

Add `api/routes/auth.py`:
- `GET /api/v1/auth/me` → `AuthMeResponse` with fields: `app_user_id`, `provider_user_id`, `provider_username`, `is_admin`, `setup_mode`, `connection` summary (id, provider, display_name, base_url read-only per D-08), `has_media_link`, `libraries_scoped`
- `POST /api/v1/auth/bootstrap-session` — **no auth required**; if session already has valid `app_user_id`, return 200; else create provisional `AppUser` with `provider_user_id=f"__pending__:{uuid4()}"` (unique), set `request.session["app_user_id"]`, return 200. Enables Plex OAuth `/start` from unauthenticated LoginPage (Plan 04 calls this on mount).
- `POST /api/v1/auth/logout` — clear session

Update `api/routes/meta.py` per D-02 (required before Plan 04 LoginPage):
- `GET /api/v1/meta/providers` returns `{provider: settings.WOF_PROVIDER, oauth_callback_base: settings.WOF_OAUTH_CALLBACK_BASE}` — single provider, not `enabled[]` array
- Update `ProvidersMetaResponse` schema and `test_meta_routes.py` expectations

Register auth router in `main.py` under `/api/v1`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/api/test_auth_routes.py -k "unauthenticated or require_admin or bootstrap" tests/api/test_meta_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /auth/me without session cookie returns 401
    - POST /auth/bootstrap-session returns 200 and sets session cookie with app_user_id
    - Authenticated non-admin calling admin-only dependency returns 403
    - AuthMeResponse includes setup_mode=true when WOF_ADMIN_PROVIDER_USER_ID unset
    - GET /meta/providers returns provider matching WOF_PROVIDER and oauth_callback_base string
  </acceptance_criteria>
  <done>Session auth replaces stubs; /auth/me exposes admin and setup flags</done>
</task>

<task type="auto">
  <name>Task 3: Extend conftest for session auth tests</name>
  <files>backend/tests/conftest.py, backend/tests/api/test_auth_routes.py</files>
  <read_first>backend/tests/conftest.py, backend/tests/api/test_catalog_routes.py</read_first>
  <action>
Extend `conftest.py`:
- `phase3_settings` fixture setting WOF_PROVIDER, WOF_MEDIA_SERVER_URL, WOF_ADMIN_PROVIDER_USER_ID
- `authenticated_client` helper — sets session cookie via TestClient session API after creating AppUser row
- `admin_client` and `non_admin_client` fixtures

Ensure `test_auth_routes.py` covers:
- unauthenticated /auth/me → 401
- authenticated non-admin + require_admin route → 403
- admin user passes require_admin when WOF_ADMIN_PROVIDER_USER_ID matches provider_user_id

Update existing catalog tests to use session auth fixture instead of stub user where they start failing — minimal changes only to restore green suite.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_boot_sync.py tests/api/test_auth_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - conftest exposes authenticated_client with session cookie
    - Auth route tests pass without STUB_APP_USER_ID in production deps path
  </acceptance_criteria>
  <done>Wave 0 auth test infra complete</done>
</task>

</tasks>

<verification>
cd backend && uv run ruff check . && uv run pytest tests/unit/test_boot_sync.py tests/api/test_auth_routes.py -q
</verification>

<success_criteria>
- Boot sync upserts connection from env on every startup (D-06, D-07)
- Session middleware active with configurable TTL (D-05)
- /auth/me returns admin/setup_mode flags for frontend gating (D-03, D-04)
</success_criteria>

<output>
Create `.planning/phases/03-minimal-operator-spa-shell/03-01-SUMMARY.md` when done
</output>
