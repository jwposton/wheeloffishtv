---
phase: 09-series-detail-watch-state-from-playlists-library-view-edit-p
plan: 08
type: execute
wave: 5
depends_on:
  - "09-05"
files_modified:
  - frontend/src/components/playlists/TwoPanePicker.tsx
  - frontend/src/components/playlists/PlaylistMembersPanel.tsx
  - frontend/src/components/playlists/PlaylistMemberTile.tsx
  - frontend/src/pages/PlaylistDetailPage.test.tsx
  - frontend/src/pages/SeriesDetailPage.watch-state.test.tsx
autonomous: false
gap_closure: true
requirements:
  - WEB-01
tags:
  - phase-9
  - gap-closure
  - uat-0
must_haves:
  truths:
    - "Playlist membership edits in playlist edit mode are staged locally and do not persist until Save is clicked."
    - "Cancel discards all unsaved membership edits and restores original server-backed playlist membership state."
    - "Row-level playlist membership affordances reflect pending versus saved state clearly during the edit session."
  artifacts:
    - path: frontend/src/components/playlists/TwoPanePicker.tsx
      provides: "Session-scoped pending add/remove state with explicit commit/discard hooks"
    - path: frontend/src/components/playlists/PlaylistMembersPanel.tsx
      provides: "Save/Cancel wiring that commits staged membership changes only on Save"
    - path: frontend/src/pages/PlaylistDetailPage.test.tsx
      provides: "Integration coverage for Save commit and Cancel rollback semantics"
  key_links:
    - from: frontend/src/components/playlists/PlaylistMembersPanel.tsx
      to: frontend/src/components/playlists/TwoPanePicker.tsx
      via: "edit-session callbacks for pending changes and reset"
      pattern: "onSave|onCancel|pending"
    - from: frontend/src/pages/PlaylistDetailPage.test.tsx
      to: frontend/src/components/playlists/PlaylistMembersPanel.tsx
      via: "user-flow assertions for add/remove then Save/Cancel"
      pattern: "Save|Cancel|playlist members"
---

<objective>
Close the remaining UAT gap where playlist membership edits currently persist immediately instead of honoring explicit Save/Cancel transaction semantics.

Purpose: Align playlist edit behavior with user expectation that edits are tentative until Save and fully revertible via Cancel.
Output: Transactional membership edit flow plus automated regression coverage and explicit UAT rerun checklist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-UAT.md
@frontend/src/components/playlists/TwoPanePicker.tsx
@frontend/src/components/playlists/PlaylistMembersPanel.tsx
@frontend/src/components/playlists/PlaylistMemberTile.tsx
@frontend/src/pages/PlaylistDetailPage.test.tsx
</context>

<gap_reference>
- test: 0
  truth: "Playlist edit changes to show membership and row-level actions apply only when the user clicks Save, and Cancel reverts unsaved changes."
  reason: "User reported immediate persistence of playlist show changes."
  missing:
    - "Define and enforce explicit Save/Cancel transaction semantics for membership edits"
    - "Prevent immediate persistence of add/remove changes until Save"
    - "Ensure Cancel restores original membership/row state"
</gap_reference>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add pending membership transaction model in edit session</name>
  <files>frontend/src/components/playlists/TwoPanePicker.tsx, frontend/src/components/playlists/PlaylistMemberTile.tsx, frontend/src/pages/SeriesDetailPage.watch-state.test.tsx</files>
  <behavior>
    - Test 1: Adding/removing membership in edit mode updates local pending state only and does not call persistence mutations before Save.
    - Test 2: In-pane row visuals distinguish pending add/remove state from persisted membership during the same session.
  </behavior>
  <action>Refactor playlist edit selection state so TwoPanePicker tracks pending membership deltas per D-11 style deterministic state rules, and only emits transactional payloads to parent Save handlers. Update row/tile rendering to reflect pending adds/removes without mutating server-backed membership immediately.</action>
  <verify>
    <automated>cd frontend && npm test -- SeriesDetailPage.watch-state --run</automated>
  </verify>
  <done>Membership edits stay local and visible as pending until an explicit Save event.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Save commit and Cancel rollback semantics</name>
  <files>frontend/src/components/playlists/PlaylistMembersPanel.tsx, frontend/src/components/playlists/TwoPanePicker.tsx, frontend/src/pages/PlaylistDetailPage.test.tsx</files>
  <behavior>
    - Test 1: Clicking Save applies all pending membership additions/removals in one commit path and then clears pending state.
    - Test 2: Clicking Cancel discards pending changes and restores original membership and row-level state.
    - Test 3: Reopening edit mode after Cancel shows server-backed baseline, not discarded pending deltas.
  </behavior>
  <action>Implement explicit transaction controls in PlaylistMembersPanel so Save is the only path that executes membership persistence mutations and Cancel resets editor state from the pre-edit snapshot (per UAT gap truth D-11 parity expectations). Keep transactional semantics deterministic when both adds and removes are queued in one session.</action>
  <verify>
    <automated>cd frontend && npm test -- PlaylistDetailPage --run</automated>
  </verify>
  <done>Save persists staged membership changes, while Cancel reverts fully with no accidental persistence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run UAT test 0 for transactional edit semantics</name>
  <files>.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-UAT.md</files>
  <what-built>Transactional Save/Cancel membership editing with staged pending state and deterministic commit/rollback behavior</what-built>
  <action>Execute targeted frontend suites first, then re-run the failing UAT scenario in the app to confirm immediate persistence is gone and Save/Cancel now govern membership writes.</action>
  <verify>
    <automated>cd frontend && npm test -- PlaylistDetailPage SeriesDetailPage.watch-state --run</automated>
  </verify>
  <how-to-verify>
    1. Enter playlist edit mode and add one show plus remove one existing show; confirm the UI shows pending state but no persisted result outside the editor yet.
    2. Click Cancel and confirm playlist membership returns to the original baseline with both edits discarded.
    3. Repeat add/remove edits, click Save, and confirm membership persists only after Save completes.
    4. Re-open edit mode and verify state matches the saved baseline.
    5. Mark UAT test 0 as pass in `09-UAT.md` if all expectations hold.
  </how-to-verify>
  <resume-signal>Type "approved" when UAT test 0 passes, or provide failing step details.</resume-signal>
  <done>Remaining UAT gap is closed with explicit evidence that Save/Cancel controls playlist membership persistence.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-08-01 | Tampering | client membership edit state | mitigate | Keep pending edits isolated from persisted state until explicit Save path runs |
| T-09-08-02 | Repudiation | Save/Cancel action outcomes | mitigate | Use deterministic commit/rollback flow with tests asserting no pre-save persistence |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `cd frontend && npm test -- PlaylistDetailPage --run` passes for Save/Cancel commit-rollback behavior.
- `cd frontend && npm test -- SeriesDetailPage.watch-state --run` remains green for nearby playlist-related edit interactions.
- UAT rerun for test 0 confirms membership changes persist only after Save and Cancel fully reverts unsaved edits.
</verification>

<success_criteria>
- Playlist edit membership changes are transactional and staged until Save.
- Cancel always restores pre-edit membership state with no persisted side effects.
- UAT test 0 converts from failed to passed with recorded rerun evidence.
</success_criteria>

<output>
Create `.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-08-SUMMARY.md` when done
</output>
