---
phase: 09-series-detail-watch-state-from-playlists-library-view-edit-p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/wheeloffish/integrations/base.py
  - backend/src/wheeloffish/integrations/plex/client.py
  - backend/src/wheeloffish/integrations/jellyfin/client.py
  - backend/tests/unit/test_watch_writeback_services.py
autonomous: true
requirements:
  - INT-01
  - INT-02
must_haves:
  truths:
    - "Provider adapters expose explicit watched/unwatched mutation operations for episode, season, and series scopes (D-08)."
    - "Plex write operations resolve ratingKey correctly from composite ids before scrobble/unscrobble calls (D-09)."
    - "Jellyfin write operations call UserPlayedItems with episode/season/series item ids directly (D-05, D-06, D-10)."
  artifacts:
    - path: "backend/src/wheeloffish/integrations/base.py"
      provides: "Provider interface methods for watch-state mutation scope and action"
    - path: "backend/src/wheeloffish/integrations/plex/client.py"
      provides: "Plex watched/unwatched implementation via scrobble/unscrobble with key resolution"
    - path: "backend/src/wheeloffish/integrations/jellyfin/client.py"
      provides: "Jellyfin played/unplayed implementation via UserPlayedItems endpoints"
    - path: "backend/tests/unit/test_watch_writeback_services.py"
      provides: "Provider dispatch and failure semantics coverage"
  key_links:
    - from: "backend/src/wheeloffish/integrations/plex/client.py"
      to: "backend/src/wheeloffish/integrations/plex/playlists.py"
      via: "reuse of GUID->ratingKey resolution helpers"
      pattern: "resolve_episode_rating_key|resolve_guid_to_rating_key"
    - from: "backend/src/wheeloffish/integrations/base.py"
      to: "backend/src/wheeloffish/api/routes/catalog.py"
      via: "typed provider mutation contract consumed by API layer"
      pattern: "mark_(watched|unwatched)"
---

## Phase Goal

**As a** self-host operator, **I want to** update watched status through the same provider-backed model across Plex and Jellyfin, **so that** all watch-state actions in later UI tasks remain consistent with provider truth.

<objective>
Define provider-level watch-state mutation contracts and concrete adapter implementations for Plex and Jellyfin per D-01 through D-10.

Purpose: Establish deterministic, test-backed backend primitives before API and UI wiring.
Output: Extended provider protocol plus tested Plex/Jellyfin mutation implementations.
</objective>

## Grey Area Decisions (Locked)

- **Plex bulk semantics (A=1)**: implement season/series bulk actions and **fallback gracefully** if Plex rejects the scope (return `partial|failed` via the fixed response envelope; UI will surface error and refetch rather than claiming success). UAT `T-09-01` still required to confirm expected bulk behavior.

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/9-CONTEXT.md
@.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-RESEARCH.md
@backend/src/wheeloffish/integrations/base.py
@backend/src/wheeloffish/integrations/plex/playlists.py
@backend/src/wheeloffish/integrations/plex/client.py
@backend/src/wheeloffish/integrations/jellyfin/client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed provider watch-state mutation contract</name>
  <files>backend/src/wheeloffish/integrations/base.py, backend/tests/unit/test_watch_writeback_services.py</files>
  <behavior>
    - Test 1: Provider abstraction accepts episode/season/series scope and watched/unwatched actions.
    - Test 2: Invalid scope/action combinations are rejected by contract validation.
  </behavior>
  <action>Introduce provider-facing methods and enums/dataclasses for watch-state mutations in `base.py` per D-08. Add or update unit tests to lock method signatures and expected dispatch payloads so downstream route handlers can call one uniform contract (per D-11 follow-on needs).</action>
  <verify>
    <automated>cd backend && python3 -m pytest backend/tests/unit/test_watch_writeback_services.py -q</automated>
  </verify>
  <done>Provider protocol exposes stable watch mutation API with passing unit tests that fail on contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Plex/Jellyfin watch-state adapter methods</name>
  <files>backend/src/wheeloffish/integrations/plex/client.py, backend/src/wheeloffish/integrations/jellyfin/client.py, backend/tests/unit/test_watch_writeback_services.py</files>
  <behavior>
    - Test 1: Plex watched/unwatched calls hit scrobble/unscrobble endpoints with resolved rating keys.
    - Test 2: Jellyfin watched/unwatched calls use POST/DELETE UserPlayedItems for episode/season/series ids.
    - Test 3: Adapter exceptions map to provider error classes without false-success responses.
  </behavior>
  <action>Implement adapter methods for all scopes using locked provider APIs: Plex `:/scrobble` and `:/unscrobble` (D-01, D-02, D-09) and Jellyfin `POST/DELETE /UserPlayedItems/{itemId}` (D-05, D-06, D-10). Reuse existing GUID->ratingKey helpers and preserve explicit error semantics for downstream 401/403/partial handling per D-13 and D-14.</action>
  <verify>
    <automated>cd backend && python3 -m pytest backend/tests/unit/test_watch_writeback_services.py -q</automated>
  </verify>
  <done>Both provider adapters support episode, season, and series watched/unwatched mutations with deterministic request construction and tested failure paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API->Provider adapters | Authenticated app requests become provider API mutations |
| Provider adapter->External APIs | App forwards scoped mutation intents to Plex/Jellyfin endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Plex item id translation | mitigate | Validate/parse composite ids and require resolved ratingKey before scrobble calls |
| T-09-02 | Elevation | Provider mutation API | mitigate | Enforce scope enum/action enum validation in provider contract and route consumers |
| T-09-03 | Denial | Provider API failures | mitigate | Return typed errors for 401/403/network failures and forbid false-success responses |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; keep install gate blocked for [ASSUMED]/[SUS] if introduced later |
</threat_model>

<verification>
- Unit tests validate provider contract + adapter request semantics and error mapping.
- No new dependency installs introduced.
</verification>

<success_criteria>
- Provider abstraction includes explicit watch-state mutation API used by Plex and Jellyfin clients.
- Plex calls resolve episode identity to ratingKey before watched/unwatched requests.
- Jellyfin uses native item ids for episode/season/series watched/unwatched updates.
</success_criteria>

<output>
Create `.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-01-SUMMARY.md` when done
</output>
