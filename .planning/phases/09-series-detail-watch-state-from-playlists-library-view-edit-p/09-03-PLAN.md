---
phase: 09-series-detail-watch-state-from-playlists-library-view-edit-p
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/playlists/PlaylistRowMenuItems.tsx
  - frontend/src/components/playlists/TwoPanePicker.tsx
  - frontend/src/components/playlists/PlaylistRowMenuItems.view-series.test.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "In-playlist row context menu exposes View series in edit flow parity with Library detail access."
    - "Available pane tile click continues to append playlist membership unchanged."
    - "Rows added in current edit session are surfaced first in In playlist pane with New affordance."
  artifacts:
    - path: "frontend/src/components/playlists/PlaylistRowMenuItems.tsx"
      provides: "View series action from in-playlist context menu"
    - path: "frontend/src/components/playlists/TwoPanePicker.tsx"
      provides: "session-added prioritization in In pane while preserving existing add behavior"
    - path: "frontend/src/components/playlists/PlaylistRowMenuItems.view-series.test.tsx"
      provides: "behavior tests for new menu action and click path"
  key_links:
    - from: "frontend/src/components/playlists/PlaylistRowMenuItems.tsx"
      to: "frontend/src/pages/SeriesDetailPage.tsx"
      via: "navigation payload preserving return origin"
      pattern: "View series|navigate"
    - from: "frontend/src/components/playlists/TwoPanePicker.tsx"
      to: "frontend/src/api/playlists.ts"
      via: "append mutation with optimistic update retained"
      pattern: "appendMutation|mutateAsync"
---

## Phase Goal

**As a** self-host operator, **I want to** open the same series detail flow from playlist edit interactions and quickly identify newly-added rows, **so that** playlist editing stays clear and consistent with Library behavior.

<objective>
Deliver playlist-edit parity UX elements before detailed watch-state page wiring.

Purpose: Complete route-entry and two-pane session clarity work that is independent of backend watch mutation endpoints.
Output: Updated playlist menu and picker behavior with focused component tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/9-CONTEXT.md
@.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-UI-SPEC.md
@frontend/src/components/playlists/PlaylistRowMenuItems.tsx
@frontend/src/components/playlists/TwoPanePicker.tsx
@frontend/src/pages/SeriesDetailPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add View series action to in-playlist row menus</name>
  <files>frontend/src/components/playlists/PlaylistRowMenuItems.tsx, frontend/src/components/playlists/PlaylistRowMenuItems.view-series.test.tsx</files>
  <behavior>
    - Test 1: Context menu renders View series action for in-playlist row tiles.
    - Test 2: Selecting View series navigates to shared detail route with origin context for back affordance.
  </behavior>
  <action>Extend playlist row menu items to include View series for in-playlist tiles per UI spec copy contract and D-08 parity goals. Preserve existing menu ordering semantics and add focused tests for render + navigation payload behavior.</action>
  <verify>
    <automated>cd frontend && npm test -- PlaylistRowMenuItems.view-series --run</automated>
  </verify>
  <done>Users can open shared series detail directly from playlist edit row context menu and tests lock behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prioritize session-new rows in In playlist pane</name>
  <files>frontend/src/components/playlists/TwoPanePicker.tsx, frontend/src/components/playlists/PlaylistRowMenuItems.view-series.test.tsx</files>
  <behavior>
    - Test 1: Available-pane click-to-add still appends membership successfully.
    - Test 2: Newly added rows in current session sort to top in In pane with New marker.
  </behavior>
  <action>Track newly-added memberships in TwoPanePicker session state and sort/render these rows first in the In pane with transient New affordance, while preserving existing append mutation behavior and rollback paths.</action>
  <verify>
    <automated>cd frontend && npm test -- TwoPanePicker PlaylistRowMenuItems.view-series --run</automated>
  </verify>
  <done>Edit flow highlights new additions without regressing available-pane append behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User interactions->SPA state | Menu selections and tile clicks mutate client state and navigation context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | Navigation context payload | mitigate | Restrict payload to known route params and avoid arbitrary URL injection |
| T-09-08 | Repudiation | Session-new markers | accept | Pure UI-local transient state; no security-sensitive persistence |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; keep install gate blocked for [ASSUMED]/[SUS] if introduced later |
</threat_model>

<verification>
- Focused frontend tests for menu action and two-pane behavior pass.
- Existing playlist interaction tests remain green.
</verification>

<success_criteria>
- Edit playlist in-pane context menu includes View series and navigates correctly.
- Available pane continues click-to-add behavior unchanged.
- Session-new memberships are visually prioritized in In playlist pane.
</success_criteria>

<output>
Create `.planning/phases/09-series-detail-watch-state-from-playlists-library-view-edit-p/09-03-SUMMARY.md` when done
</output>
