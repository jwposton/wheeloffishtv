---
phase: 04-playlist-mathematics
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/wheeloffish/domain/playlist.py
  - backend/src/wheeloffish/core/playlist/__init__.py
  - backend/tests/unit/fixtures/playlist_vectors.py
  - backend/tests/unit/test_playlist_models.py
autonomous: true
requirements:
  - PLT-01
  - PLT-02
  - PLT-03
  - PLT-04
must_haves:
  truths:
    - "Playlist domain model accepts id, name, episode_count N≥1, and rows list"
    - "Each row declares series_id, RowMode ordered|disordered, completion policy and event"
    - "SeriesRebuildInput bundles episodes + optional on_deck per series for builder input contract"
    - "Test fixtures provide episode factory with multipart fields matching test_resume_service pattern"
  artifacts:
    - path: backend/src/wheeloffish/domain/playlist.py
      provides: "Playlist config + build result Pydantic models"
      contains: "class Playlist"
    - path: backend/tests/unit/fixtures/playlist_vectors.py
      provides: "Shared golden-vector episode factories"
      contains: "def episode"
    - path: backend/src/wheeloffish/core/playlist/__init__.py
      provides: "Package scaffold for playlist submodules"
  key_links:
    - from: backend/src/wheeloffish/domain/playlist.py
      to: backend/src/wheeloffish/domain/dto.py
      via: "Episode import in SeriesRebuildInput"
      pattern: "from wheeloffish.domain.dto import Episode"
---

## Phase Goal

**As a** self-host operator, **I want to** configure named playlists with per-series ordered/disordered modes and episode count N, **so that** the nightly builder can generate deterministic mixed-TV output without DB or API coupling (Phase 5 orchestrates persistence).

<objective>
Establish Phase 4 domain contracts and shared test fixtures — pure Pydantic models for playlist config/build results plus episode factories for golden vectors.

Purpose: Wave 0 locks the builder input/output API consumed by Waves 1–5; no algorithm logic yet.
Output: `domain/playlist.py`, `core/playlist/` package scaffold, `fixtures/playlist_vectors.py`, model validation tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-playlist-mathematics/04-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/src/wheeloffish/domain/dto.py
@backend/tests/unit/test_resume_service.py
</context>

<locked_assumptions>
Research assumptions A1–A8 apply as defaults (no 04-CONTEXT.md yet):
- A1: Uniform slot allocation across active rows (implemented in 04-06)
- A2: Disordered pool = all episodes; no replacement within refresh (04-05)
- A3: Multipart block includes all group members when anchor selected (04-02)
- A4: Default completion event = series_complete (04-03)
- A5: Short playlist when rows exhaust — slots_filled &lt; slots_requested (04-06)
- A6: SCH-02 adjacency only for ordered rows (04-02, 04-04)
- A7: day_key = ISO date string YYYY-MM-DD (04-05, 04-06)
- A8: Hypothesis optional — stdlib + pytest only unless Wave 6 added later
</locked_assumptions>

<out_of_scope>
No DB migrations, Alembic, REST `/api/v1/playlists` routes, SPA screens, APScheduler, WheelOfFish admin, or MediaProvider HTTP inside builder. Phase 4 is pure domain + unit tests only.
</out_of_scope>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-01-01 | Tampering | domain/playlist.py | mitigate | Pydantic Field(ge=1) on episode_count; reject empty rows at builder (04-06) |
| T-04-01-SC | Tampering | pip/uv deps | accept | No new runtime deps in Wave 0 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create domain/playlist.py models and enums</name>
  <files>backend/src/wheeloffish/domain/playlist.py</files>
  <read_first>backend/src/wheeloffish/domain/dto.py, .planning/phases/04-playlist-mathematics/04-RESEARCH.md</read_first>
  <behavior>
    - Test: Playlist rejects episode_count=0 via ValidationError
    - Test: PlaylistSeriesRow defaults mode=ORDERED, completion_policy=REMOVE, completion_event=SERIES_COMPLETE (A4)
    - Test: SeriesRebuildInput accepts episodes list and optional on_deck Episode
    - Test: PlaylistBuildResult exposes slots_requested, slots_filled, day_key, row_outcomes fields (A5 scaffold)
  </behavior>
  <action>
Create backend/src/wheeloffish/domain/playlist.py with StrEnum types and Pydantic v2 models matching research Pattern 1:

Enums (StrEnum):
- RowMode: ORDERED = "ordered", DISORDERED = "disordered"
- CompletionPolicy: REMOVE = "remove", RESTART = "restart", DISORDERED = "disordered"
- CompletionEvent: SERIES_COMPLETE = "series_complete", SEASON_COMPLETE = "season_complete"

Models:
- PlaylistSeriesRow(series_id: str, mode: RowMode = ORDERED, completion_policy: CompletionPolicy = REMOVE, completion_event: CompletionEvent = SERIES_COMPLETE)
- Playlist(id: str, name: str, episode_count: int = Field(ge=1), rows: list[PlaylistSeriesRow]) — PLT-01 named playlist, PLT-02 N, PLT-03 rows membership
- SeriesRebuildInput(series_id: str, episodes: list[Episode], on_deck: Episode | None = None) — import Episode from wheeloffish.domain.dto
- BuiltEpisode(episode: Episode, series_id: str, row_mode: RowMode, slot_index: int)
- RowBuildOutcome(series_id: str, effective_mode: RowMode, excluded: bool = False, policy_applied: CompletionPolicy | None = None)
- PlaylistBuildResult(episodes: list[BuiltEpisode], row_outcomes: list[RowBuildOutcome], day_key: str, slots_requested: int, slots_filled: int)

Do not add SQLAlchemy models or FastAPI schemas — persistence deferred Phase 5 per research phase boundary.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_playlist_models.py -x</automated>
  </verify>
  <done>All enums and models import cleanly; validation tests green</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold core/playlist package and test fixtures</name>
  <files>backend/src/wheeloffish/core/playlist/__init__.py, backend/tests/unit/fixtures/playlist_vectors.py, backend/tests/unit/fixtures/__init__.py</files>
  <read_first>backend/tests/unit/test_resume_service.py, .planning/phases/04-playlist-mathematics/04-RESEARCH.md</read_first>
  <action>
Create backend/src/wheeloffish/core/playlist/__init__.py — empty package docstring noting Phase 4 pure playlist math; export nothing yet (submodules added in later waves).

Create backend/tests/unit/fixtures/__init__.py if missing.

Create backend/tests/unit/fixtures/playlist_vectors.py mirroring test_resume_service._episode factory per research Code Examples:

- def episode(episode_id, season, index, *, percent=0.0, played=False, part_index=None, multipart_group_id=None, is_special=False, special_for_season=None) -> Episode
- def multipart_group(group_id: str, parts: list[tuple[str, int, int, int | None]]) -> list[Episode] — helper building N parts sharing multipart_group_id with ascending part_index
- def fresh_series(series_id_prefix: str, season: int, count: int) -> list[Episode] — S{season}E1..E{count} unwatched
- def playlist_single_row(playlist_id: str, series_id: str, *, episode_count: int = 3, mode: RowMode = RowMode.ORDERED) -> Playlist — convenience for later golden vectors

Import RowMode and Playlist from domain.playlist. Keep factories pure — no HTTP, no DB.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from tests.unit.fixtures.playlist_vectors import episode, multipart_group; assert len(multipart_group('g1', [('p1',1,1,1),('p2',1,1,2)]))==2"</automated>
  </verify>
  <done>Fixtures importable from tests; multipart_group helper produces grouped episodes</done>
</task>

<task type="auto">
  <name>Task 3: Add model validation test module</name>
  <files>backend/tests/unit/test_playlist_models.py</files>
  <action>
Create tests/unit/test_playlist_models.py covering Task 1 behavior block:
- test_playlist_episode_count_minimum
- test_row_defaults_per_a4
- test_series_rebuild_input_shape
- test_playlist_build_result_fields

Use pytest.raises(ValidationError) for invalid episode_count. Use fixtures.playlist_vectors.episode where needed.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_playlist_models.py -q</automated>
  </verify>
  <done>Wave 0 model tests pass; domain contract frozen for downstream waves</done>
</task>

</tasks>

<verification>
cd backend && uv run ruff check src/wheeloffish/domain/playlist.py src/wheeloffish/core/playlist/ tests/unit/fixtures/playlist_vectors.py tests/unit/test_playlist_models.py && uv run pytest tests/unit/test_playlist_models.py -q
</verification>

<success_criteria>
- PLT-01: Playlist id + name model exists
- PLT-02: episode_count N with ge=1 validation
- PLT-03: rows list with series_id membership on PlaylistSeriesRow
- PLT-04: RowMode enum on each row
- Fixtures ready for Waves 1–5 golden vectors
</success_criteria>

<output>
Create `.planning/phases/04-playlist-mathematics/04-01-SUMMARY.md` when done
</output>
