---
phase: 02-media-ingestion-catalogs
plan: 04
type: execute
wave: 2
depends_on:
  - "02-03"
files_modified:
  - backend/src/wheeloffish/integrations/jellyfin/auth.py
  - backend/src/wheeloffish/integrations/jellyfin/client.py
  - backend/src/wheeloffish/integrations/jellyfin/mappers.py
  - backend/src/wheeloffish/integrations/jellyfin/__init__.py
  - backend/src/wheeloffish/api/routes/oauth_jellyfin.py
  - backend/src/wheeloffish/core/connections.py
  - backend/src/wheeloffish/main.py
  - backend/tests/integrations/test_jellyfin_client.py
  - backend/tests/api/test_connections_routes.py
autonomous: true
requirements:
  - INT-01
  - INT-02
must_haves:
  truths:
    - "Jellyfin AuthenticateByName stores per-user AccessToken in vault keyed by app_user_id"
    - "Jellyfin provider lists TV libraries and series with identical DTO shapes as Plex"
    - "provider_disabled returned when Jellyfin not in WOF_ENABLED_PROVIDERS"
    - "Admin API keys rejected for user linking flows"
  artifacts:
    - path: backend/src/wheeloffish/integrations/jellyfin/client.py
      provides: "JellyfinProvider implementing MediaProvider"
      contains: "class JellyfinProvider"
    - path: backend/src/wheeloffish/api/routes/oauth_jellyfin.py
      provides: "Jellyfin auth route"
      contains: "AuthenticateByName"
    - path: backend/tests/integrations/test_jellyfin_client.py
      provides: "Jellyfin respx tests"
      contains: "list_libraries"
  key_links:
    - from: backend/src/wheeloffish/integrations/jellyfin/client.py
      to: backend/src/wheeloffish/domain/dto.py
      via: "identical Library/Series/Episode shapes"
      pattern: "Library|Series|Episode"
    - from: backend/src/wheeloffish/core/secrets.py
      to: backend/src/wheeloffish/integrations/jellyfin/auth.py
      via: "per-user token storage"
      pattern: "store_media_user_token"
---

<objective>
Deliver Jellyfin username/password auth and JellyfinProvider with full MediaProvider parity — identical DTOs to Plex.

Purpose: Complete dual-provider foundation per D-01/D-02; ROADMAP criterion #3 (Jellyfin identical DTOs). Per D-08/D-13: Phase 2 user linking uses `AuthenticateByName` only — admin API keys are out of scope for per-user watch state (server bootstrap may use admin creds in connection create only, not as user tokens).
Output: `POST /api/v1/connections/jellyfin/auth`, `JellyfinProvider`, respx tests, libraries listing for Jellyfin connections.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@backend/src/wheeloffish/integrations/plex/client.py
@backend/src/wheeloffish/domain/dto.py
@backend/tests/fixtures/jellyfin/
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-04-01 | Information Disclosure | Jellyfin auth route | High | mitigate | Password accepted in POST body only; never logged, stored, or returned; only AccessToken in vault |
| T-02-04-02 | Elevation | Admin API key | High | mitigate | Reject API-key-only auth for user linking; require AuthenticateByName per-user token (D-13) |
| T-02-04-03 | Spoofing | Jellyfin server | Medium | mitigate | test-then-save GET /Users/Me after auth; verify_ssl honored on httpx client |
| T-02-04-04 | Information Disclosure | Shared watch state | High | mitigate | Per-user tokens in vault; jellyfin_user_id stored on UserMediaLink |

**Blocker threshold:** High — T-02-04-01, T-02-04-02, T-02-04-04 must be mitigated before plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement Jellyfin auth module</name>
  <files>backend/src/wheeloffish/integrations/jellyfin/auth.py, backend/src/wheeloffish/api/schemas/oauth.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md, backend/tests/fixtures/jellyfin/authenticate.json</read_first>
  <action>
Create `integrations/jellyfin/auth.py`:
- `authenticate(base_url, username, password, verify_ssl) -> tuple[str, str, str]` — POST `{base_url}/Users/AuthenticateByName` with MediaBrowser Authorization header (Client="WheelOfFishTV", DeviceId=uuid, Version="0.1.0"); returns access_token, user_id, username
- `validate_token(base_url, token, verify_ssl) -> dict` — GET `/Users/Me`
- Reject requests where username looks like API key-only admin flow — document unsupported

Authorization header format per RESEARCH:
`MediaBrowser Client="WheelOfFishTV", Device="Server", DeviceId="{uuid}", Version="0.1.0", Token="{token}"`

Add schema `JellyfinAuthRequest(base_url, username, password, display_name, verify_ssl)`.
  </action>
  <verify>cd backend && uv run pytest tests/integrations/test_jellyfin_client.py -k auth -q</verify>
  <acceptance_criteria>
    - respx test mocks AuthenticateByName and returns AccessToken
    - Password not persisted anywhere after auth completes
    - Auth failure maps to ProviderUnauthorized
  </acceptance_criteria>
  <done>Jellyfin authentication client ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement JellyfinProvider client and mappers</name>
  <files>backend/src/wheeloffish/integrations/jellyfin/client.py, backend/src/wheeloffish/integrations/jellyfin/mappers.py, backend/src/wheeloffish/integrations/jellyfin/__init__.py</files>
  <read_first>backend/src/wheeloffish/integrations/plex/client.py, backend/tests/fixtures/jellyfin/media_folders.json, backend/tests/fixtures/jellyfin/episodes.json</read_first>
  <action>
Create `JellyfinProvider(base_url, token, user_id, connection_id, verify_ssl)` implementing MediaProvider:
- `ping()` — GET `/System/Info` or `/Users/Me`
- `list_libraries()` — GET `/Library/MediaFolders`, filter CollectionType=tvshows
- `list_series(library_native_id, page, limit, q)` — GET `/Items?ParentId=&IncludeItemTypes=Series&StartIndex=&Limit=&SearchTerm=`
- `list_episodes(series_composite_id)` — parse composite id; GET `/Shows/{id}/Episodes?userId=&EnableUserData=true`
- `get_on_deck_episode(series_composite_id)` — GET `/Shows/NextUp?seriesId=&userId=&Limit=1&EnableUserData=true`

Mappers produce same DTO field names as Plex mappers:
- `provider_marked_played = UserData.Played`
- `percent_watched` from PlayedPercentage or ticks/duration fallback
- composite id: `{connection_id}:jellyfin:{item_uuid}`
- `map_episode` populates `part_index` and `multipart_group_id` when Jellyfin item metadata exposes native multipart fields (D-21); leave null otherwise — no heuristics

Integration tests:
- `test_list_libraries` — media_folders.json fixture
- `test_list_series_paged` — series_items.json
- `test_list_episodes_watch_fields` — episodes.json
- `test_dto_shape_matches_plex` — assert Library/Series/Episode model fields identical (same pydantic model classes)
  </action>
  <verify>cd backend && uv run pytest tests/integrations/test_jellyfin_client.py -q</verify>
  <acceptance_criteria>
    - Jellyfin Library/Series/Episode use same `domain/dto.py` classes as Plex
    - Episode IDs use Jellyfin item UUID in composite format
    - `test_dto_shape_matches_plex` passes
  </acceptance_criteria>
  <done>Jellyfin provider parity with Plex DTOs</done>
</task>

<task type="auto">
  <name>Task 3: Wire Jellyfin auth route and extend connection factory</name>
  <files>backend/src/wheeloffish/api/routes/oauth_jellyfin.py, backend/src/wheeloffish/core/connections.py, backend/src/wheeloffish/main.py, backend/tests/api/test_connections_routes.py</files>
  <read_first>backend/src/wheeloffish/api/routes/oauth_plex.py, backend/src/wheeloffish/core/connections.py</read_first>
  <action>
Create `api/routes/oauth_jellyfin.py`:
- `POST /connections/jellyfin/auth` — body JellyfinAuthRequest; authenticate → test-then-save via create_connection with token + jellyfin user_id on UserMediaLink

Update `core/connections.py`:
- `build_provider_for_connection` adds JellyfinProvider branch
- Store `provider_user_id` from auth response on UserMediaLink

Extend `GET /connections/{id}/libraries` to work for Jellyfin connections (same handler, provider-agnostic).

Tests:
- `test_jellyfin_auth_success` — respx auth + Users/Me + System/Info → 201, vault token
- `test_jellyfin_auth_unauthorized` — 401 → 422 unauthorized
- `test_provider_disabled` — WOF_ENABLED_PROVIDERS=jellyfin blocks plex (if not already covered)

Register router in main.py.
  </action>
  <verify>cd backend && uv run pytest tests/api/test_connections_routes.py -k jellyfin -q</verify>
  <acceptance_criteria>
    - POST /api/v1/connections/jellyfin/auth creates connection + vault token
    - GET libraries works for Jellyfin connection using respx mocks
    - Per-user token stored via `store_media_user_token(connection_id, app_user_id, token)`
  </acceptance_criteria>
  <done>Jellyfin auth → list libraries end-to-end via API</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/integrations/test_jellyfin_client.py -q` exits 0
- [ ] `cd backend && uv run pytest tests/api/test_connections_routes.py -k "jellyfin or provider_disabled" -q` exits 0
- [ ] Jellyfin and Plex return identical pydantic DTO types
- [ ] Passwords never appear in logs or DB
</verification>

<success_criteria>
- Jellyfin auth and provider at parity with Plex
- ROADMAP criterion #3 met: identical returned DTOs
- INT-01 complete for both providers
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-04-SUMMARY.md`
</output>
