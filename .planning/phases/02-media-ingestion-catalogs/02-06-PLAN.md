---
phase: 02-media-ingestion-catalogs
plan: 06
type: execute
wave: 3
depends_on:
  - "02-05"
files_modified:
  - backend/src/wheeloffish/core/resume.py
  - backend/src/wheeloffish/api/schemas/resume.py
  - backend/src/wheeloffish/api/routes/catalog.py
  - backend/tests/api/test_catalog_routes.py
  - backend/tests/unit/test_resume_service.py
autonomous: true
requirements:
  - INT-03
must_haves:
  truths:
    - "GET /api/v1/connections/{id}/series/{series_id}/episodes returns live watch snapshots (not DB rows)"
    - "GET /api/v1/connections/{id}/series/{series_id}/resume returns ResumeCursor from ResumeService"
    - "Cached watch percent / viewed flags sufficient to compute resume pointers per series"
    - "Hybrid rule honors on-deck when ahead of earliest unfinished (D-10)"
    - "Per-user tokens yield different resume for different app_user_id (D-13)"
  artifacts:
    - path: backend/src/wheeloffish/core/resume.py
      provides: "ResumeService domain logic"
      contains: "compute_resume"
    - path: backend/src/wheeloffish/api/routes/catalog.py
      provides: "Live episodes and resume endpoints"
      contains: "/episodes"
    - path: backend/src/wheeloffish/api/schemas/resume.py
      provides: "ResumeCursor API schema"
      contains: "ResumeCursor"
  key_links:
    - from: backend/src/wheeloffish/api/routes/catalog.py
      to: backend/src/wheeloffish/core/resume.py
      via: "live episodes + on_deck → compute_resume"
      pattern: "compute_resume"
    - from: backend/src/wheeloffish/api/routes/catalog.py
      to: backend/src/wheeloffish/integrations/
      via: "provider.list_episodes live fetch"
      pattern: "list_episodes"
---

<objective>
Expose live episode fetch and resume preview API — proves INT-03 with reusable ResumeService for Phase 4.

Purpose: Operator can probe episodes + computed resume cursor per series without episode SQLite cache (D-15, D-22).
Output: `GET …/series/{series_id}/episodes`, `GET …/series/{series_id}/resume`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@backend/src/wheeloffish/core/resume.py
@backend/src/wheeloffish/domain/dto.py
@backend/src/wheeloffish/api/routes/catalog.py
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-06-01 | Information Disclosure | live episodes | Medium | mitigate | Require valid connection + app_user token; no cross-user episode fetch |
| T-02-06-02 | Information Disclosure | watch state | Medium | mitigate | Resume keyed per (app_user_id, connection_id, series_id); vault token isolation |
| T-02-06-03 | Spoofing | series_id | Low | mitigate | Validate series composite id belongs to connection_id prefix |

**Blocker threshold:** None — no High severity threats open.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Finalize ResumeService and export for API reuse</name>
  <files>backend/src/wheeloffish/core/resume.py, backend/tests/unit/test_resume_service.py</files>
  <read_first>backend/src/wheeloffish/core/resume.py, .planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md</read_first>
  <action>
Ensure `core/resume.py` exports:
- `class ResumeService` with `compute(series_id: str, episodes: list[Episode], on_deck: Episode | None) -> ResumeCursor`
- `classify_watch`, `order_episodes`, `is_ahead_in_sequence` as module-level functions (used by tests)
- ResumeCursor fields: `series_id`, `episode_id`, `season_index`, `episode_index`, `percent_watched`, `source: Literal["earliest_unfinished","on_deck"]`, `series_complete: bool`

Add golden vector tests if missing from plan 01:
- Skipped ahead: S1E5 unwatched but on_deck S2E1 with S1E1-4 complete → source=on_deck
- On deck behind earliest unfinished → source=earliest_unfinished
- Specials after season finale ordering affects cursor position

Document module as reusable by Phase 4 playlist builder in docstring.
  </action>
  <verify>cd backend && uv run pytest tests/unit/test_resume_service.py tests/unit/test_watch_classification.py -q</verify>
  <acceptance_criteria>
    - All golden vectors in test_resume_service.py pass
    - `ResumeService.compute` returns ResumeCursor pydantic model
    - Specials ordering test passes (D-12)
  </acceptance_criteria>
  <done>ResumeService ready for API and Phase 4 reuse</done>
</task>

<task type="auto">
  <name>Task 2: Add live episodes and resume preview routes</name>
  <files>backend/src/wheeloffish/api/schemas/resume.py, backend/src/wheeloffish/api/routes/catalog.py</files>
  <read_first>backend/src/wheeloffish/api/routes/catalog.py, backend/src/wheeloffish/core/connections.py</read_first>
  <action>
Create `api/schemas/resume.py`:
- `EpisodeResponse` mirroring Episode DTO fields including watch snapshot
- `EpisodesListResponse(episodes: list[EpisodeResponse])`
- `ResumePreviewResponse` mirroring ResumeCursor

Extend `api/routes/catalog.py`:
- `GET /connections/{connection_id}/series/{series_id}/episodes`:
  1. Validate series_id composite prefix matches connection_id
  2. Load provider via build_provider_for_connection + app_user vault token
  3. `episodes = await provider.list_episodes(series_id)` — live fetch, NOT from DB
  4. Return EpisodesListResponse
- `GET /connections/{connection_id}/series/{series_id}/resume`:
  1. Same provider fetch: episodes + on_deck = await provider.get_on_deck_episode(series_id)
  2. `cursor = ResumeService().compute(series_id, episodes, on_deck)`
  3. Return ResumePreviewResponse

No episode persistence — verify handler has no INSERT to any episode table.
  </action>
  <verify>cd backend && uv run pytest tests/api/test_catalog_routes.py -k episodes -q</verify>
  <acceptance_criteria>
    - GET episodes returns list with percent_watched and provider_marked_played populated
    - GET resume returns episode_id and source field
    - Handler does not query cached_series for episode data
  </acceptance_criteria>
  <done>Live episode and resume endpoints callable</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests for episodes and resume preview</name>
  <files>backend/tests/api/test_catalog_routes.py</files>
  <read_first>backend/tests/fixtures/plex/show_leaves.json, backend/tests/fixtures/jellyfin/episodes.json, backend/tests/api/test_catalog_routes.py</read_first>
  <action>
Tests with respx mocked providers:
- `test_episodes_live_fetch` — GET episodes → fields populated from fixture; assert no episode DB table in metadata
- `test_resume_matches_resume_service` — seed fixture episodes; GET resume → assert episode_id and source match direct ResumeService.compute call
- `test_resume_on_deck_ahead` — fixture with on-deck ahead of earliest unfinished → source=on_deck
- `test_per_user_isolation` — two app_user_id fixtures with different mock percent_watched → different resume episode_id

Use composite series_id format `{connection_id}:plex:guid-123` in test URLs.
  </action>
  <verify>cd backend && uv run pytest tests/api/test_catalog_routes.py -k "episodes or resume" -q</verify>
  <acceptance_criteria>
    - `pytest tests/api/test_catalog_routes.py -k resume` passes
    - Resume preview source field matches ResumeService golden expectation
    - Per-user isolation test passes
  </acceptance_criteria>
  <done>INT-03 proven via REST resume preview API</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/unit/test_resume_service.py -q` exits 0
- [ ] `cd backend && uv run pytest tests/api/test_catalog_routes.py -k "episodes or resume" -q` exits 0
- [ ] Episodes endpoint performs live provider fetch only
- [ ] Resume cursor matches domain ResumeService output
</verification>

<success_criteria>
- ROADMAP criterion #2 met: watch metadata sufficient for resume pointers
- Live episodes + resume preview API ready for Phase 3 SPA
- INT-03 complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-06-SUMMARY.md`
</output>
