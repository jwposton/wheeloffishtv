---
phase: 02-media-ingestion-catalogs
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - backend/src/wheeloffish/integrations/plex/auth.py
  - backend/src/wheeloffish/integrations/plex/client.py
  - backend/src/wheeloffish/integrations/plex/mappers.py
  - backend/src/wheeloffish/integrations/plex/__init__.py
  - backend/src/wheeloffish/api/routes/oauth_plex.py
  - backend/src/wheeloffish/api/schemas/oauth.py
  - backend/src/wheeloffish/core/connections.py
  - backend/src/wheeloffish/main.py
  - backend/tests/integrations/test_plex_client.py
  - backend/tests/api/test_connections_routes.py
autonomous: true
requirements:
  - INT-01
  - INT-02
must_haves:
  truths:
    - "Plex PIN OAuth start returns auth_url and pin_id"
    - "Plex OAuth callback completes test-then-save and stores encrypted token in vault"
    - "Authenticated Plex session lists TV libraries from live PMS with stable composite IDs"
    - "Plex provider resolves guid to ratingKey internally; composite IDs use guid not ratingKey"
  artifacts:
    - path: backend/src/wheeloffish/integrations/plex/client.py
      provides: "PlexProvider implementing MediaProvider"
      contains: "class PlexProvider"
    - path: backend/src/wheeloffish/api/routes/oauth_plex.py
      provides: "Plex OAuth PIN flow routes"
      contains: "oauth/start"
    - path: backend/tests/integrations/test_plex_client.py
      provides: "respx fixture tests for Plex"
      contains: "list_libraries"
  key_links:
    - from: backend/src/wheeloffish/api/routes/oauth_plex.py
      to: backend/src/wheeloffish/integrations/plex/auth.py
      via: "PIN create/poll against plex.tv"
      pattern: "api/v2/pins"
    - from: backend/src/wheeloffish/integrations/plex/client.py
      to: backend/src/wheeloffish/domain/dto.py
      via: "mappers to Library/Series/Episode"
      pattern: "format_composite_id"
---

<objective>
Deliver Plex OAuth PIN flow and live PlexProvider — connect via OAuth, list TV libraries, and fetch series pages with stable IDs.

Purpose: First live media provider vertical slice proving INT-01 OAuth and INT-02 library enumeration for Plex (D-08, D-19).
Output: `POST /api/v1/connections/plex/oauth/start`, callback route, `PlexProvider` with respx tests, libraries endpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@backend/src/wheeloffish/core/connections.py
@backend/src/wheeloffish/domain/dto.py
@backend/src/wheeloffish/domain/ids.py
@backend/tests/fixtures/plex/
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-03-01 | Spoofing | Plex OAuth callback | High | mitigate | Validate PIN via plex.tv poll; verify token with GET /api/v2/user before save |
| T-02-03-02 | Information Disclosure | OAuth redirect | Medium | mitigate | auth_url uses official app.plex.tv; tokens never logged or returned in API body |
| T-02-03-03 | Spoofing | PMS base_url mismatch | High | mitigate | After OAuth, discover resources via plex.tv; match operator base_url; test-then-save ping local PMS |
| T-02-03-04 | Denial of Service | PIN polling | Low | accept | Poll with backoff; timeout after configurable max attempts |

**Blocker threshold:** High — T-02-03-01 and T-02-03-03 must be mitigated before plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement Plex OAuth auth module</name>
  <files>backend/src/wheeloffish/integrations/plex/auth.py, backend/src/wheeloffish/api/schemas/oauth.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md, backend/src/wheeloffish/core/config.py</read_first>
  <action>
Create `integrations/plex/auth.py`:
- `create_pin(client_identifier: str, product_name: str) -> tuple[int, str, str]` — POST `https://plex.tv/api/v2/pins?strong=true`; returns pin_id, code, auth_url
- `poll_pin(pin_id: int, client_identifier: str) -> str | None` — GET pins/{id}; return authToken when present
- `validate_token(token: str, client_identifier: str) -> dict` — GET `https://plex.tv/api/v2/user`
- `discover_server(token: str, base_url: str) -> bool` — GET resources; verify configured base_url reachable
- Required headers: `X-Plex-Product`, `X-Plex-Client-Identifier`, `Accept: application/json`

Create schemas: `PlexOAuthStartResponse(pin_id, auth_url)`, `PlexOAuthStatusResponse(status, auth_token_present)`
Store in-memory PIN state dict keyed by pin_id (TTL 15 min) for callback correlation — acceptable for ≤5 users Phase 2.
  </action>
  <verify>cd backend && uv run pytest tests/integrations/test_plex_client.py -k oauth_start -q</verify>
  <acceptance_criteria>
    - `create_pin` calls `https://plex.tv/api/v2/pins`
    - auth_url includes `clientID` and `code` query params
    - respx test mocks pin create and asserts response shape
  </acceptance_criteria>
  <done>Plex OAuth client functions ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement PlexProvider client and mappers</name>
  <files>backend/src/wheeloffish/integrations/plex/client.py, backend/src/wheeloffish/integrations/plex/mappers.py, backend/src/wheeloffish/integrations/plex/__init__.py</files>
  <read_first>backend/tests/fixtures/plex/library_sections.json, backend/tests/fixtures/plex/show_leaves.json, backend/src/wheeloffish/integrations/base.py</read_first>
  <action>
Create `PlexProvider(base_url, token, client_identifier, connection_id, verify_ssl)` implementing MediaProvider:
- `ping()` — GET `{base_url}/library/sections` or `/identity`
- `list_libraries()` — GET sections, filter type=show, map to `Library` with composite id using section uuid/key
- `list_series(library_native_id, page, limit, q)` — GET `/library/sections/{id}/all?type=2` with paging; map guid→composite Series id
- `list_episodes(series_composite_id)` — resolve guid→ratingKey via metadata search; GET allLeaves; map watch fields (viewCount, viewOffset, duration)
- `get_on_deck_episode(series_composite_id)` — GET metadata with includeOnDeck=1

Create `mappers.py`:
- `map_library`, `map_series`, `map_episode` using `format_composite_id(connection_id, "plex", guid)`
- `map_episode` populates `part_index` and `multipart_group_id` when Plex leaf metadata exposes native multipart fields (D-21); leave null otherwise — no heuristics
- `resolve_guid_to_rating_key(client, guid) -> str` internal helper

Integration tests with respx + fixtures:
- `test_list_libraries` — fixture library_sections.json
- `test_list_series_paged`
- `test_list_episodes_watch_fields`
  </action>
  <verify>cd backend && uv run pytest tests/integrations/test_plex_client.py -k libraries -q</verify>
  <acceptance_criteria>
    - Series composite IDs contain `:plex:` and guid segment (not raw ratingKey)
    - Episode mapper sets `provider_marked_played = viewCount > 0`
    - `percent_watched` computed from viewOffset/duration when duration > 0
  </acceptance_criteria>
  <done>Live Plex provider reads libraries and episodes from fixtures</done>
</task>

<task type="auto">
  <name>Task 3: Wire Plex OAuth routes and libraries endpoint</name>
  <files>backend/src/wheeloffish/api/routes/oauth_plex.py, backend/src/wheeloffish/core/connections.py, backend/src/wheeloffish/main.py, backend/tests/api/test_connections_routes.py</files>
  <read_first>backend/src/wheeloffish/core/connections.py, backend/src/wheeloffish/api/routes/connections.py</read_first>
  <action>
Create `api/routes/oauth_plex.py`:
- `POST /connections/plex/oauth/start` body `{ display_name, base_url, verify_ssl }` → creates pending connection stub + PIN; returns pin_id, auth_url
- `GET /connections/plex/oauth/callback?pin_id=` — poll PIN, validate token, discover server, test-then-save via create_connection; redirect or JSON success
- `GET /connections/plex/oauth/status/{pin_id}` — poll status for SPA

Extend `core/connections.py`:
- `build_provider_for_connection(connection, token) -> MediaProvider` — instantiate PlexProvider for plex type
- Wire ephemeral provider stub replaced with real PlexProvider

Add route to connections or catalog:
- `GET /api/v1/connections/{connection_id}/libraries` — live fetch via provider.list_libraries() for Plex connections (cache population in plan 05)

Register oauth_plex router in main.py.

Tests:
- `test_plex_oauth_start` — respx pin create → 200 with auth_url
- `test_plex_oauth_callback_stores_vault_token` — respx pin claimed + user + sections → vault row encrypted
  </action>
  <verify>cd backend && uv run pytest tests/api/test_connections_routes.py -k plex_oauth -q</verify>
  <acceptance_criteria>
    - OAuth callback persists connection + encrypted vault token
    - GET libraries returns TV libraries with composite IDs
    - `pytest tests/integrations/test_plex_client.py -k oauth_start` passes
  </acceptance_criteria>
  <done>Plex OAuth connect → list libraries end-to-end via API</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/integrations/test_plex_client.py -q` exits 0
- [ ] `cd backend && uv run pytest tests/api/test_connections_routes.py -k plex -q` exits 0
- [ ] Plex composite IDs use guid not ratingKey
- [ ] OAuth tokens not present in API response bodies
</verification>

<success_criteria>
- Operator can complete Plex PIN OAuth and list TV libraries via REST
- PlexProvider returns normalized Library/Series/Episode DTOs
- ROADMAP criterion #1 partially met for Plex
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-03-SUMMARY.md`
</output>
