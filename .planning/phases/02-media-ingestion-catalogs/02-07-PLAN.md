---
phase: 02-media-ingestion-catalogs
plan: 07
type: execute
wave: 4
depends_on:
  - "02-06"
files_modified:
  - backend/alembic/versions/002_connections_catalog.py
  - backend/src/wheeloffish/db/models/__init__.py
  - backend/src/wheeloffish/main.py
  - backend/src/wheeloffish/core/config.py
  - .env.example
  - README.md
  - .planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md
  - backend/tests/integrations/test_plex_client.py
  - backend/tests/integrations/test_jellyfin_client.py
  - backend/tests/api/test_connections_routes.py
  - backend/tests/api/test_catalog_routes.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
must_haves:
  truths:
    - "Alembic upgrade head applies 002_connections_catalog on clean database"
    - "Full pytest suite green with ruff clean"
    - "Fixtures contain no real tokens or production URLs"
    - "Manual UAT checklist documents live Plex/Jellyfin verification steps"
    - "Both Plex and Jellyfin providers return identical DTO shapes end-to-end"
  artifacts:
    - path: backend/alembic/versions/002_connections_catalog.py
      provides: "Final consolidated Phase 2 schema"
      contains: "cached_series"
    - path: .planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md
      provides: "Manual live-server UAT steps"
      contains: "Plex OAuth"
    - path: .env.example
      provides: "Phase 2 config documentation"
      contains: "WOF_ENABLED_PROVIDERS"
  key_links:
    - from: backend/docker-entrypoint.sh
      to: backend/alembic/versions/002_connections_catalog.py
      via: "alembic upgrade head on container start"
      pattern: "alembic upgrade head"
    - from: .planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md
      to: GET /api/v1/connections/{id}/series/{series_id}/resume
      via: "manual resume verification"
      pattern: "resume"
---

<objective>
Harden Phase 2 integration: finalize migration, run blocking Alembic upgrade, fixture hygiene, docs, and manual UAT checklist.

Purpose: Phase 2 closure — all INT requirements verified, schema applied, operator docs updated (D-03).
Output: Green CI suite, `02-UAT-CHECKLIST.md`, updated README/.env.example, consolidated migration verified.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@.planning/phases/02-media-ingestion-catalogs/02-VALIDATION.md
@.planning/ROADMAP.md
@backend/alembic/versions/002_connections_catalog.py
@README.md
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-07-01 | Information Disclosure | test fixtures | High | mitigate | CI grep gate: no X-Plex-Token, real IPs, or passwords in fixtures |
| T-02-07-02 | Tampering | migration drift | Medium | mitigate | Blocking alembic upgrade head after all schema edits; models match migration |
| T-02-07-03 | Denial of Service | provider rate limits | Low | accept | Document manual UAT pacing; sync chunk delays already in catalog_sync |

**Blocker threshold:** High — T-02-07-01 fixture hygiene gate must pass before plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate migration 002 and verify model alignment</name>
  <files>backend/alembic/versions/002_connections_catalog.py, backend/src/wheeloffish/db/models/__init__.py</files>
  <read_first>backend/alembic/versions/002_connections_catalog.py, backend/src/wheeloffish/db/models/connection.py, .planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md</read_first>
  <action>
Review and finalize `002_connections_catalog.py` after all plans 02-06 model changes:
- Ensure all tables present: connections, user_media_links, cached_libraries, cached_series, catalog_sync_state
- Verify indexes: UNIQUE(connection.provider_type), UNIQUE(cached_series.connection_id, native_id)
- Portable types only (no SQLite-only syntax breaking Postgres CI)
- `down_revision = '001_foundation'`

Ensure `db/models/__init__.py` imports all models for Alembic autogenerate metadata consistency.
Fix any drift between ORM models and migration columns discovered during review.
  </action>
  <verify>cd backend && uv run python -c "from wheeloffish.db.models import Connection, CachedSeries, CatalogSyncState; print('models ok')"</verify>
  <acceptance_criteria>
    - Migration file contains all five Phase 2 tables
    - ORM column names match migration columns
    - No episode cache table in migration
  </acceptance_criteria>
  <done>Schema file finalized for blocking upgrade</done>
</task>

<task type="auto" autonomous="false">
  <name>Task 2: [BLOCKING] Run Alembic upgrade head after all schema modifications</name>
  <files>backend/alembic/versions/002_connections_catalog.py</files>
  <read_first>backend/alembic/versions/002_connections_catalog.py, backend/alembic/env.py</read_first>
  <action>
After Task 1 schema consolidation completes, run blocking migration verification:

```bash
cd backend && WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef DATABASE_URL=sqlite:///./phase2_verify.db uv run alembic upgrade head
```

Verify tables exist via SQLAlchemy inspect or sqlite3:
- connections, user_media_links, cached_libraries, cached_series, catalog_sync_state
- app_metadata and secrets from 001 still present

If upgrade fails: fix migration/models and re-run until exit 0.
Do NOT mark plan complete until this command succeeds.
  </action>
  <verify>cd backend && uv run alembic upgrade head</verify>
  <acceptance_criteria>
    - `cd backend && uv run alembic upgrade head` exits 0
    - All five Phase 2 tables exist after upgrade
    - `alembic current` shows head revision 002
  </acceptance_criteria>
  <done>Database schema applied and verified</done>
</task>

<task type="auto">
  <name>Task 3: Fixture hygiene gate and full test suite</name>
  <files>backend/tests/fixtures/, backend/tests/integrations/test_plex_client.py, backend/tests/integrations/test_jellyfin_client.py, backend/tests/api/test_connections_routes.py, backend/tests/api/test_catalog_routes.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-VALIDATION.md</read_first>
  <action>
Run fixture hygiene check:
```bash
grep -rE 'X-Plex-Token|192\.168\.|10\.\d+\.|password.*:' backend/tests/fixtures/ && exit 1 || echo "fixtures clean"
```

Run full suite:
```bash
cd backend && uv run ruff check . && uv run pytest
```

Fix any failing tests across:
- tests/unit/test_composite_ids.py
- tests/unit/test_watch_classification.py
- tests/unit/test_resume_service.py
- tests/integrations/test_plex_client.py
- tests/integrations/test_jellyfin_client.py
- tests/api/test_connections_routes.py
- tests/api/test_catalog_routes.py

Ensure main.py registers all routers: meta, connections, oauth_plex, oauth_jellyfin, catalog.
  </action>
  <verify>cd backend && uv run ruff check . && uv run pytest</verify>
  <acceptance_criteria>
    - Fixture grep gate passes (no real tokens)
    - Full pytest suite exits 0
    - ruff check exits 0
  </acceptance_criteria>
  <done>CI-equivalent verification green</done>
</task>

<task type="auto">
  <name>Task 4: Update operator docs and create manual UAT checklist</name>
  <files>.env.example, README.md, .planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md</files>
  <read_first>README.md, .env.example, .planning/phases/02-media-ingestion-catalogs/02-VALIDATION.md</read_first>
  <action>
Update `.env.example` with Phase 2 vars:
- WOF_ENABLED_PROVIDERS=plex,jellyfin
- WOF_PLEX_PRODUCT_NAME=Wheel of Fish TV
- WOF_OAUTH_CALLBACK_BASE=http://localhost:8000
- WOF_CATALOG_SYNC_CHUNK_SIZE=100
- WOF_CATALOG_PAGE_DEFAULT=50
- WOF_SCOPED_LIBRARY_IDS= (optional)

Update README.md Phase 2 section:
- Connection setup via API (Plex OAuth, Jellyfin auth)
- Catalog browse/sync endpoints summary
- Resume preview endpoint for UAT

Create `.planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md`:
1. Plex OAuth E2E — PIN flow against real PMS; GET libraries returns TV sections
2. Jellyfin auth E2E — username/password; GET libraries returns tvshows folders
3. Catalog sync — POST sync; poll status; GET series browse shows new shows
4. Resume preview — compare GET resume to Plex On Deck / Jellyfin Next Up for 3 series:
   - Ordered progression (earliest unfinished)
   - Skipped ahead (on-deck honored)
   - Partial episode (mid-watch resume)
5. Library scope — PUT admin library-scope; verify out-of-scope excluded
6. Provider parity — same response field names from Plex vs Jellyfin episodes endpoint

Mark each item PASS/FAIL with date and operator notes columns.
  </action>
  <verify>test -f .planning/phases/02-media-ingestion-catalogs/02-UAT-CHECKLIST.md</verify>
  <acceptance_criteria>
    - UAT checklist contains all 6 scenarios from 02-VALIDATION.md manual section
    - README documents WOF_ENABLED_PROVIDERS
    - .env.example updated with Phase 2 vars
  </acceptance_criteria>
  <done>Operator documentation and UAT checklist ready</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run alembic upgrade head` exits 0 (BLOCKING)
- [ ] `cd backend && uv run ruff check . && uv run pytest` exits 0
- [ ] Fixture grep gate passes
- [ ] `02-UAT-CHECKLIST.md` exists with Plex, Jellyfin, resume scenarios
- [ ] All three ROADMAP success criteria traceable to implemented endpoints
</verification>

<success_criteria>
- Phase 2 schema applied via Alembic
- Full automated test suite green
- Manual UAT checklist documented for live server verification
- INT-01, INT-02, INT-03 all covered and verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-07-SUMMARY.md`
</output>
