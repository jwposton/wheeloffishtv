---
phase: 02-media-ingestion-catalogs
plan: 02
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - backend/alembic/versions/002_connections_catalog.py
  - backend/src/wheeloffish/db/models/connection.py
  - backend/src/wheeloffish/db/models/user_media_link.py
  - backend/src/wheeloffish/db/models/cached_library.py
  - backend/src/wheeloffish/db/models/cached_series.py
  - backend/src/wheeloffish/db/models/catalog_sync_state.py
  - backend/src/wheeloffish/db/models/__init__.py
  - backend/src/wheeloffish/core/namespaces.py
  - backend/src/wheeloffish/core/secrets.py
  - backend/src/wheeloffish/core/connections.py
  - backend/src/wheeloffish/api/deps.py
  - backend/src/wheeloffish/api/schemas/connections.py
  - backend/src/wheeloffish/api/routes/connections.py
  - backend/src/wheeloffish/main.py
  - backend/tests/conftest.py
  - backend/tests/api/test_connections_routes.py
autonomous: true
requirements:
  - INT-01
must_haves:
  truths:
    - "Alembic revision 002 creates connections, user_media_links, cached_libraries, cached_series, catalog_sync_state tables"
    - "POST /api/v1/connections with valid token pings provider before persisting DB row + vault secret"
    - "Structured 422 errors returned for unreachable, unauthorized, ssl_error, provider_disabled, wrong_type"
    - "At most one connection per provider_type enforced"
  artifacts:
    - path: backend/alembic/versions/002_connections_catalog.py
      provides: "Phase 2 catalog schema migration"
      contains: "connections"
    - path: backend/src/wheeloffish/core/connections.py
      provides: "test-then-save connection service"
      contains: "create_connection"
    - path: backend/src/wheeloffish/api/routes/connections.py
      provides: "Connection CRUD REST routes"
      contains: "POST"
  key_links:
    - from: backend/src/wheeloffish/core/connections.py
      to: backend/src/wheeloffish/core/secrets.py
      via: "single transaction DB + vault write"
      pattern: "store_media_user_token|set_secret"
    - from: backend/src/wheeloffish/api/routes/connections.py
      to: backend/src/wheeloffish/core/connections.py
      via: "test-then-save on create"
      pattern: "create_connection"
---

<objective>
Deliver DB schema for connections/catalog cache and connection CRUD with test-then-save — the INT-01 foundation without OAuth yet.

Purpose: Operator can register a server config, validate connectivity, and persist encrypted tokens (D-06, D-07, D-05).
Output: Migration `002_connections_catalog`, ORM models, `POST/GET/DELETE /api/v1/connections`, `POST /api/v1/connections/{id}/test`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@backend/src/wheeloffish/core/secrets.py
@backend/src/wheeloffish/core/namespaces.py
@backend/src/wheeloffish/integrations/base.py
@backend/src/wheeloffish/integrations/errors.py
@backend/alembic/versions/001_foundation.py
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-02-01 | Information Disclosure | connections API | High | mitigate | Tokens stored only in vault via Fernet; never in `connections` table or API responses |
| T-02-02-02 | Spoofing | test-then-save | High | mitigate | Ping provider before commit; reject bad creds with 422 `unauthorized` |
| T-02-02-03 | Tampering | DB transaction | Medium | mitigate | Single SQLAlchemy session wraps connection insert + vault write; rollback on ping failure |
| T-02-02-04 | Elevation | WOF_ENABLED_PROVIDERS | Medium | mitigate | Check provider type against enabled set; 422 `provider_disabled` |

**Blocker threshold:** High — T-02-02-01 and T-02-02-02 must be mitigated before plan completes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create ORM models and Alembic migration 002_connections_catalog</name>
  <files>backend/src/wheeloffish/db/models/*.py, backend/alembic/versions/002_connections_catalog.py, backend/src/wheeloffish/db/models/__init__.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md, backend/alembic/versions/001_foundation.py, backend/src/wheeloffish/db/models/base.py</read_first>
  <action>
Create SQLAlchemy models per RESEARCH schema:
- `Connection`: id UUID PK, provider_type ('plex'|'jellyfin'), display_name, base_url, verify_ssl, plex_client_identifier nullable, enabled, timestamps; UNIQUE on provider_type
- `UserMediaLink`: id UUID, app_user_id UUID, connection_id FK, provider_user_id, provider_username nullable, linked_at
- `CachedLibrary`: id UUID, connection_id FK, native_id, title, in_scope bool, synced_at
- `CachedSeries`: id str PK (composite), connection_id FK, library_native_id, native_id, title, title_sort, year, thumb_url, provider_metadata JSON, synced_at; UNIQUE(connection_id, native_id)
- `CatalogSyncState`: connection_id PK FK, status, library_native_id, page_cursor, total_estimated, error_message, started_at, updated_at

Create `002_connections_catalog.py`:
- `down_revision = '001_foundation'`
- Portable column types (String, Boolean, DateTime, JSON/Text)
- `upgrade()` creates all five tables with constraints
- `downgrade()` drops in reverse FK order

Import all models in `db/models/__init__.py` for Alembic metadata discovery.
  </action>
  <verify>cd backend && WOF_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef DATABASE_URL=sqlite:///./test_002.db uv run alembic upgrade head</verify>
  <acceptance_criteria>
    - `002_connections_catalog.py` exists with `connections` table creation
    - All five model classes importable from `wheeloffish.db.models`
    - `connections.provider_type` has unique constraint (one per type)
  </acceptance_criteria>
  <done>Schema ready for connection persistence</done>
</task>

<task type="auto">
  <name>Task 2: Extend vault namespace helpers for per-user tokens</name>
  <files>backend/src/wheeloffish/core/namespaces.py, backend/src/wheeloffish/core/secrets.py</files>
  <read_first>backend/src/wheeloffish/core/namespaces.py, backend/src/wheeloffish/core/secrets.py</read_first>
  <action>
Add to `namespaces.py`:
```python
def media_user_token_key(connection_id: str, app_user_id: str) -> str:
    return f"media_server/{connection_id}/users/{app_user_id}/token"
```

Extend `SecretsVault`:
- `store_media_user_token(connection_id, app_user_id, token)`
- `get_media_user_token(connection_id, app_user_id)`
- `delete_media_user_token(connection_id, app_user_id)`
- `delete_all_connection_secrets(connection_id)` — delete legacy + all user-scoped keys for connection

Keep existing `store_media_token` for backward compat.
  </action>
  <verify>cd backend && uv run python -c "from wheeloffish.core.namespaces import media_user_token_key; assert 'users/' in media_user_token_key('c1','u1')"</verify>
  <acceptance_criteria>
    - `media_user_token_key` returns path containing `users/{app_user_id}/token`
    - `SecretsVault.store_media_user_token` method exists
  </acceptance_criteria>
  <done>Per-user token vault keys per D-13</done>
</task>

<task type="auto">
  <name>Task 3: Implement connections service with test-then-save and stub provider ping</name>
  <files>backend/src/wheeloffish/core/connections.py, backend/src/wheeloffish/api/deps.py, backend/src/wheeloffish/api/schemas/connections.py</files>
  <read_first>backend/src/wheeloffish/integrations/errors.py, backend/src/wheeloffish/core/config.py, .planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md</read_first>
  <action>
Create `core/connections.py`:
- `build_ephemeral_provider(connection_config, token) -> MediaProvider` — returns injectable stub for tests; real providers wired in plans 03-04
- `create_connection(db, vault, settings, *, provider_type, display_name, base_url, verify_ssl, token, app_user_id)`:
  1. Check `provider_type in settings.enabled_providers_set` else raise `ProviderDisabled`
  2. Check no existing connection with same provider_type else raise conflict (409)
  3. Build ephemeral provider, `await provider.ping()`
  4. On success: insert Connection + UserMediaLink + vault token in one transaction
  5. Map ProviderError subclasses to HTTPException 422 with `detail={"code": err.code}`
- `test_connection(db, vault, connection_id, app_user_id)` — ping without persisting changes
- `delete_connection(db, vault, connection_id)` — cascade delete links + vault secrets

Create Pydantic schemas: `ConnectionCreate`, `ConnectionResponse` (no token field), `ConnectionTestResponse`
Create `api/deps.py`: `get_db`, `get_vault`, `get_settings_dep`, `get_app_user_id` (returns stub UUID until Phase 3 auth)
  </action>
  <verify>cd backend && uv run python -c "from wheeloffish.core.connections import create_connection; print('ok')"</verify>
  <acceptance_criteria>
    - `create_connection` calls `provider.ping()` before DB commit
    - ProviderDisabled maps to code `provider_disabled`
    - ConnectionResponse schema excludes token fields
  </acceptance_criteria>
  <done>Connection business logic with test-then-save ready</done>
</task>

<task type="auto">
  <name>Task 4: Wire connection REST routes and integration tests</name>
  <files>backend/src/wheeloffish/api/routes/connections.py, backend/src/wheeloffish/main.py, backend/tests/conftest.py, backend/tests/api/test_connections_routes.py</files>
  <read_first>backend/src/wheeloffish/main.py, backend/tests/conftest.py</read_first>
  <action>
Create `api/routes/connections.py`:
- `GET /connections` — list connections (no secrets)
- `POST /connections` — body: provider_type, display_name, base_url, verify_ssl, token (manual token path for testing pre-OAuth); delegates to create_connection
- `POST /connections/{connection_id}/test` — re-validate
- `DELETE /connections/{connection_id}` — remove + vault cleanup

Register router in `main.py` at prefix `/api/v1`.

Update `conftest.py`:
- `db_engine` fixture runs alembic through `002` head
- `connection_factory` helper creating test connection with mocked ping

Tests in `test_connections_routes.py`:
- `test_create_connection_success` — mock ping OK → 201, vault row exists
- `test_create_unauthorized` — mock ping raises ProviderUnauthorized → 422 code `unauthorized`
- `test_create_unreachable` — mock connection refused → 422 `unreachable`
- `test_create_ssl_error` — mock ping raises ProviderSSLError → 422 code `ssl_error` (D-07)
- `test_create_wrong_type` — mock ping raises ProviderWrongType → 422 code `wrong_type` (D-07)
- `test_provider_disabled` — WOF_ENABLED_PROVIDERS=plex, POST jellyfin → 422 `provider_disabled`
- `test_duplicate_provider_type` — second plex → 409
- `test_connection_response_excludes_token` — response JSON has no token key
  </action>
  <verify>cd backend && uv run pytest tests/api/test_connections_routes.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/api/test_connections_routes.py -k unauthorized` passes
    - `pytest tests/api/test_connections_routes.py -k provider_disabled` passes
    - GET /api/v1/connections returns list without token fields
  </acceptance_criteria>
  <done>Connection CRUD API callable end-to-end with test-then-save</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/api/test_connections_routes.py -q` exits 0
- [ ] `cd backend && uv run alembic upgrade head` (temp sqlite) exits 0
- [ ] Connection create rejects bad credentials before DB row persisted
- [ ] `cd backend && uv run ruff check .` exits 0
</verification>

<success_criteria>
- Schema migration 002 applies cleanly
- Operator can register/test/delete connections via REST with encrypted vault storage
- INT-01 structural foundation complete (OAuth added in plans 03-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-02-SUMMARY.md`
</output>
