---
phase: 02-media-ingestion-catalogs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/tests/conftest.py
  - backend/tests/fixtures/plex/pin_create.json
  - backend/tests/fixtures/plex/pin_claimed.json
  - backend/tests/fixtures/plex/library_sections.json
  - backend/tests/fixtures/plex/show_leaves.json
  - backend/tests/fixtures/plex/show_ondeck.json
  - backend/tests/fixtures/jellyfin/authenticate.json
  - backend/tests/fixtures/jellyfin/media_folders.json
  - backend/tests/fixtures/jellyfin/series_items.json
  - backend/tests/fixtures/jellyfin/episodes.json
  - backend/tests/fixtures/jellyfin/next_up.json
  - backend/src/wheeloffish/domain/__init__.py
  - backend/src/wheeloffish/domain/ids.py
  - backend/src/wheeloffish/domain/dto.py
  - backend/src/wheeloffish/integrations/base.py
  - backend/src/wheeloffish/integrations/errors.py
  - backend/src/wheeloffish/api/schemas/meta.py
  - backend/src/wheeloffish/api/routes/meta.py
  - backend/src/wheeloffish/core/config.py
  - backend/src/wheeloffish/main.py
  - backend/tests/unit/test_composite_ids.py
  - backend/tests/unit/test_watch_classification.py
  - backend/tests/unit/test_resume_service.py
  - backend/tests/api/test_meta_routes.py
  - .env.example
autonomous: true
requirements:
  - INT-01
  - INT-03
must_haves:
  truths:
    - "Composite stable IDs round-trip parse/format as {connection_id}:{provider}:{native_id}"
    - "ResumeService golden vectors pass for watch thresholds, specials ordering, and hybrid on-deck rule"
    - "GET /api/v1/meta/providers returns WOF_ENABLED_PROVIDERS from config"
  artifacts:
    - path: backend/src/wheeloffish/domain/ids.py
      provides: "Composite ID parse/format helpers"
      contains: "format_composite_id"
    - path: backend/src/wheeloffish/domain/dto.py
      provides: "Library, Series, Episode, ResumeCursor DTOs"
      contains: "class Episode"
    - path: backend/src/wheeloffish/integrations/base.py
      provides: "MediaProvider protocol skeleton"
      contains: "class MediaProvider"
    - path: backend/tests/fixtures/plex/library_sections.json
      provides: "Sanitized Plex fixture"
      contains: "MediaContainer"
  key_links:
    - from: backend/src/wheeloffish/domain/dto.py
      to: backend/tests/unit/test_resume_service.py
      via: "pure domain ResumeService tests"
      pattern: "ResumeService|compute_resume"
    - from: backend/src/wheeloffish/core/config.py
      to: backend/src/wheeloffish/api/routes/meta.py
      via: "WOF_ENABLED_PROVIDERS settings"
      pattern: "WOF_ENABLED_PROVIDERS"
---

<objective>
Establish Phase 2 test infrastructure, domain DTOs/composite IDs, ResumeService pure logic, and a minimal meta API proving config wiring.

Purpose: Wave 0 shared foundation (domain + test infra) — first end-to-end provider slice completes in plan 02-03 (Plex OAuth → libraries). Every subsequent plan inherits respx fixtures, domain types, and ResumeService golden vectors (D-19, D-20, D-11, D-03).
Output: respx dev dep, sanitized fixtures, `domain/` package, `MediaProvider` protocol stub, unit tests green, `GET /api/v1/meta/providers`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md
@.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md
@.planning/phases/02-media-ingestion-catalogs/02-VALIDATION.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations-packaging/01-CONTEXT.md
@backend/src/wheeloffish/core/config.py
@backend/src/wheeloffish/core/secrets.py
@backend/src/wheeloffish/integrations/plex.py
@backend/src/wheeloffish/integrations/jellyfin.py
</context>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-02-01-01 | Information Disclosure | tests/fixtures/ | High | mitigate | Sanitize all fixtures — no real `X-Plex-Token`, URLs, or credentials; grep check in plan 07 |
| T-02-01-02 | Tampering | domain/dto.py | Medium | mitigate | Pydantic models with strict field types; no eval of provider metadata |
| T-02-01-03 | Denial of Service | ResumeService | Low | accept | Pure in-memory algorithm; no HTTP in unit tests |

**Blocker threshold:** High — T-02-01-01 must be mitigated before plan completes (fixtures contain placeholder tokens only).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add respx dev dependency and extend test fixtures scaffold</name>
  <files>backend/pyproject.toml, backend/uv.lock, backend/tests/conftest.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md, backend/tests/conftest.py, backend/pyproject.toml</read_first>
  <action>
Run `cd backend && uv add --dev respx`.
Extend `backend/tests/conftest.py`:
- `settings` fixture: temp `WOF_SECRET_KEY` (64 hex chars), `DATABASE_URL=sqlite:///:memory:`
- `async_client` fixture: `httpx.AsyncClient` with `ASGITransport(app=wheeloffish.main.app)`
- `vault` fixture: `SecretsVault` over test DB session (stub until plan 02 wires alembic)
- `app_user_id` fixture: constant UUID `"00000000-0000-4000-8000-000000000001"` for per-user token tests
- `load_fixture(name)` helper reading JSON from `tests/fixtures/{plex|jellyfin}/`
  </action>
  <verify>cd backend && uv run pytest tests/test_smoke.py -q</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` dev-dependencies include `respx`
    - `conftest.py` contains `async_client` and `load_fixture`
    - `cd backend && uv run python -c "import respx"` exits 0
  </acceptance_criteria>
  <done>Test harness ready for provider and API tests</done>
</task>

<task type="auto">
  <name>Task 2: Create sanitized Plex/Jellyfin JSON fixtures</name>
  <files>backend/tests/fixtures/plex/*.json, backend/tests/fixtures/jellyfin/*.json</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md, .planning/phases/02-media-ingestion-catalogs/02-VALIDATION.md</read_first>
  <action>
Create sanitized fixture files (no real tokens, hostnames, or account IDs):
- `tests/fixtures/plex/pin_create.json` — PIN create response with `id`, `code`, `authToken: null`
- `tests/fixtures/plex/pin_claimed.json` — PIN with `authToken: "SANITIZED_PLEX_TOKEN"`
- `tests/fixtures/plex/library_sections.json` — TV sections (`type=2`) with `key`, `title`, `uuid`
- `tests/fixtures/plex/show_leaves.json` — episode leaves with `guid`, `viewCount`, `viewOffset`, `duration`, `parentIndex`, `index`
- `tests/fixtures/plex/show_ondeck.json` — show metadata with `OnDeck` child
- `tests/fixtures/jellyfin/authenticate.json` — `AccessToken`, `User.Id`, `User.Name`
- `tests/fixtures/jellyfin/media_folders.json` — folders with `CollectionType: tvshows`
- `tests/fixtures/jellyfin/series_items.json` — paged series with `Id`, `Name`, `ProductionYear`
- `tests/fixtures/jellyfin/episodes.json` — episodes with `UserData.Played`, `UserData.PlayedPercentage`, `ParentIndexNumber`, `IndexNumber`
- `tests/fixtures/jellyfin/next_up.json` — single NextUp item for series
Use fictional GUIDs/UUIDs only.
  </action>
  <verify>grep -rE 'X-Plex-Token|192\.168\.|password' backend/tests/fixtures/ && exit 1 || echo "fixtures clean"</verify>
  <acceptance_criteria>
    - All 10 fixture files exist under `tests/fixtures/plex/` and `tests/fixtures/jellyfin/`
    - No fixture contains real IP addresses or production hostnames
    - Plex fixtures use `guid` attributes (not ratingKey-only IDs)
  </acceptance_criteria>
  <done>CI-safe recorded API fixtures available</done>
</task>

<task type="auto">
  <name>Task 3: Implement domain DTOs, composite IDs, MediaProvider protocol, and ResumeService</name>
  <files>backend/src/wheeloffish/domain/ids.py, backend/src/wheeloffish/domain/dto.py, backend/src/wheeloffish/integrations/base.py, backend/src/wheeloffish/integrations/errors.py, backend/src/wheeloffish/core/resume.py, backend/tests/unit/test_composite_ids.py, backend/tests/unit/test_watch_classification.py, backend/tests/unit/test_resume_service.py</files>
  <read_first>.planning/phases/02-media-ingestion-catalogs/02-CONTEXT.md, .planning/phases/02-media-ingestion-catalogs/02-RESEARCH.md</read_first>
  <action>
Create `domain/ids.py`:
- `format_composite_id(connection_id: str, provider: str, native_id: str) -> str`
- `parse_composite_id(value: str) -> tuple[str, str, str]` raising `ValueError` on bad format
- URL-encode native_id segment per D-19

Create `domain/dto.py` Pydantic models:
- `Library`, `Series`, `PagedSeries`, `Episode`, `WatchSnapshot`, `ResumeCursor`
- Episode fields: `id`, `title`, `season_index`, `episode_index`, `duration_ms`, `percent_watched`, `provider_marked_played`, optional `part_index`, `multipart_group_id`

Create `integrations/base.py` — `MediaProvider` Protocol with:
- `ping()`, `list_libraries()`, `list_series(library_native_id, *, page, limit, q)`, `list_episodes(series_composite_id)`, `get_on_deck_episode(series_composite_id)`

Create `integrations/errors.py`:
- `ProviderError` base; subclasses `ProviderUnreachable`, `ProviderUnauthorized`, `ProviderSSLError`, `ProviderDisabled`, `ProviderWrongType` each with `.code` matching 422 codes

Create `core/resume.py`:
- `WatchState` enum, `classify_watch(ep: Episode)`, `order_episodes(episodes)` per D-12, `compute_resume(episodes, on_deck)` per D-10
- `is_ahead_in_sequence(on_deck, earliest, ordered)` helper

Unit tests:
- `test_composite_ids.py` — round-trip, bad format raises
- `test_watch_classification.py` — 4% unwatched, 50% partial, 96% complete, viewCount/Played override (D-11)
- `test_resume_service.py` — golden vectors: fresh series→E1; partial S1E3; on-deck ahead when skipped; all complete; specials after season finale
  </action>
  <verify>cd backend && uv run pytest tests/unit/test_composite_ids.py tests/unit/test_watch_classification.py tests/unit/test_resume_service.py -q</verify>
  <acceptance_criteria>
    - `format_composite_id("conn-uuid", "plex", "com.plexapp.agents.thetvdb://123")` returns three-part string
    - `test_resume_service.py` passes specials ordering test
    - `MediaProvider` protocol defines all five async methods
  </acceptance_criteria>
  <done>Domain layer and ResumeService proven by golden vectors</done>
</task>

<task type="auto">
  <name>Task 4: Add config fields, meta API route, and smoke test</name>
  <files>backend/src/wheeloffish/core/config.py, backend/src/wheeloffish/api/schemas/meta.py, backend/src/wheeloffish/api/routes/meta.py, backend/src/wheeloffish/main.py, backend/tests/api/test_meta_routes.py, .env.example</files>
  <read_first>backend/src/wheeloffish/core/config.py, backend/src/wheeloffish/main.py</read_first>
  <action>
Extend `Settings` in `core/config.py`:
- `WOF_ENABLED_PROVIDERS: str = "plex,jellyfin"` — property `enabled_providers_set: set[str]`
- `WOF_PLEX_PRODUCT_NAME: str = "Wheel of Fish TV"`
- `WOF_OAUTH_CALLBACK_BASE: str = "http://localhost:8000"`
- `WOF_CATALOG_SYNC_CHUNK_SIZE: int = 100`
- `WOF_CATALOG_PAGE_DEFAULT: int = 50`

Create `api/schemas/meta.py`: `ProvidersMetaResponse(enabled: list[str])`
Create `api/routes/meta.py`: `GET /meta/providers` → `{ "enabled": ["plex", "jellyfin"] }` parsed from settings
Register router in `main.py`: `app.include_router(meta.router, prefix="/api/v1")`
Update `.env.example` with new vars (no secrets)
Create `tests/api/test_meta_routes.py`: assert enabled list respects `WOF_ENABLED_PROVIDERS=plex` monkeypatch
  </action>
  <verify>cd backend && uv run pytest tests/api/test_meta_routes.py -q</verify>
  <acceptance_criteria>
    - `curl` equivalent: `GET /api/v1/meta/providers` returns 200 with `enabled` array
    - `.env.example` documents `WOF_ENABLED_PROVIDERS`
    - `main.py` includes router at `/api/v1`
  </acceptance_criteria>
  <done>First Phase 2 API endpoint callable end-to-end</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv run pytest tests/unit -q` exits 0
- [ ] `cd backend && uv run pytest tests/api/test_meta_routes.py -q` exits 0
- [ ] `cd backend && uv run ruff check .` exits 0
- [ ] Fixtures contain no real credentials (manual grep)
</verification>

<success_criteria>
- Domain primitives and ResumeService golden vectors pass
- Test fixtures and respx scaffold ready for provider plans
- Meta API proves config + router wiring
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-ingestion-catalogs/02-01-SUMMARY.md`
</output>
