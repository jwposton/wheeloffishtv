---
phase: 05-orchestration-scheduling
plan: 05
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified:
  - frontend/src/api/playlists.ts
  - frontend/src/api/types.ts
  - frontend/src/pages/PlaylistsPage.tsx
  - frontend/src/components/playlists/PlaylistCard.tsx
  - frontend/src/components/playlists/StatusBadge.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/PlaylistsPage.test.tsx
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Authenticated user sees /playlists list of owned playlists (D-19, D-20)"
    - "Each card shows name, cadence label, status badge, last_rebuild_at relative time (D-21)"
    - "StatusBadge maps succeeded→green, partial→amber, failed→red, null→muted outline (D-21, UI-SPEC)"
    - "AppShell nav includes Playlists link visible when authenticated"
    - "Empty state shows No playlists yet with New playlist CTA (UI-SPEC copywriting)"
  artifacts:
    - path: frontend/src/pages/PlaylistsPage.tsx
      provides: "/playlists list page"
      contains: "PlaylistsPage"
    - path: frontend/src/components/playlists/StatusBadge.tsx
      provides: "Semantic status badge"
      contains: "StatusBadge"
    - path: frontend/src/api/playlists.ts
      provides: "TanStack Query hooks for list"
      contains: "usePlaylists"
  key_links:
    - from: frontend/src/pages/PlaylistsPage.tsx
      to: /api/v1/playlists
      via: "usePlaylists GET"
      pattern: "/playlists"
    - from: frontend/src/App.tsx
      to: frontend/src/pages/PlaylistsPage.tsx
      via: "Route path /playlists"
      pattern: "PlaylistsPage"
---

## Phase Goal

**As a** self-host operator, **I want to** see all my playlists with rebuild status at a glance, **so that** I know which lineups are healthy without digging into logs (D-19, D-21, WEB-01).

<objective>
SPA vertical slice — playlist list page with API hooks, status badges, nav entry, empty state.

Purpose: First user-visible Phase 5 UI path after API exists.
Output: PlaylistsPage, PlaylistCard, StatusBadge, api/playlists.ts, component test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-orchestration-scheduling/05-UI-SPEC.md
@.planning/phases/05-orchestration-scheduling/05-CONTEXT.md
@frontend/src/pages/BrowsePage.tsx
@frontend/src/components/layout/AppShell.tsx
@frontend/src/api/client.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-05-01 | Information Disclosure | PlaylistsPage | mitigate | ProtectedRoute + API returns only owner playlists (D-18) |
| T-05-05-02 | XSS | error_message display | mitigate | Render error text as plain text nodes, not dangerouslySetInnerHTML |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API types and usePlaylists hook</name>
  <files>frontend/src/api/playlists.ts, frontend/src/api/types.ts</files>
  <read_first>backend/src/wheeloffish/api/schemas/playlists.py, frontend/src/api/client.ts, frontend/src/hooks/useAuth.ts</read_first>
  <action>
Add TypeScript types matching backend list/detail responses:
- PlaylistListItem, RebuildStatus enum: succeeded | partial | failed | running | queued | null
- RefreshCadence: daily | weekly

Implement:
- `fetchPlaylists()` GET /api/v1/playlists
- `usePlaylists()` queryKey ['playlists','list'], staleTime 30_000

Export cadence label helper: `formatCadence(item)` → "Daily" or "Weekly · Saturday" using weekday names array.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `playlists.ts` exports `usePlaylists` and `formatCadence`
    - PlaylistListItem includes last_rebuild_status and last_rebuild_at fields
  </acceptance_criteria>
  <done>Frontend can load playlist list from API</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusBadge component</name>
  <files>frontend/src/components/playlists/StatusBadge.tsx, frontend/src/pages/PlaylistsPage.test.tsx</files>
  <read_first>.planning/phases/05-orchestration-scheduling/05-UI-SPEC.md</read_first>
  <action>
Create StatusBadge accepting status: RebuildStatus | null:
- succeeded: Badge with green indicator / text-green-600
- partial: amber text-amber-600
- failed: destructive variant
- running|queued: secondary with "Rebuilding…"
- null: outline "Never rebuilt"

Add vitest test asserting class names or text for succeeded and failed cases.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/pages/PlaylistsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge renders "Never rebuilt" when status is null
    - Test file includes at least 2 StatusBadge assertions
  </acceptance_criteria>
  <done>D-21 badge semantics implemented</done>
</task>

<task type="auto">
  <name>Task 3: PlaylistsPage + routing + nav</name>
  <files>frontend/src/pages/PlaylistsPage.tsx, frontend/src/components/playlists/PlaylistCard.tsx, frontend/src/App.tsx, frontend/src/components/layout/AppShell.tsx</files>
  <read_first>frontend/src/pages/BrowsePage.tsx, frontend/src/App.tsx, .planning/phases/05-orchestration-scheduling/05-UI-SPEC.md</read_first>
  <action>
PlaylistCard: Link to /playlists/:id, shows name, formatCadence, StatusBadge, relative last_rebuild_at via date-fns formatDistanceToNow or simple ISO fallback.

PlaylistsPage:
- Header "Playlists" + Button Link to /playlists/new "New playlist"
- Grid of PlaylistCard from usePlaylists
- Loading skeletons; error toast via sonner
- Empty state copy from UI-SPEC

App.tsx: add Route path="/playlists" element PlaylistsPage inside ProtectedRoute + AppShell (same level as /browse)

AppShell nav: add Link to /playlists labeled "Playlists" with active state when pathname starts with /playlists
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'path="/playlists"' frontend/src/App.tsx` >= 1
    - AppShell contains link text "Playlists"
    - PlaylistsPage contains string "No playlists yet"
  </acceptance_criteria>
  <done>User can browse playlist list at /playlists (D-20)</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run src/pages/PlaylistsPage.test.tsx && npm run build
</verification>

<success_criteria>
- WEB-01 partial: playlist list + status badges
- D-19/D-20/D-21: minimal operator UI list path with semantic status
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-scheduling/05-05-SUMMARY.md` when done.
</output>
