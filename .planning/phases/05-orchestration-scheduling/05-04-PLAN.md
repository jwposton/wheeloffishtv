---
phase: 05-orchestration-scheduling
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - backend/src/wheeloffish/api/schemas/playlists.py
  - backend/src/wheeloffish/api/routes/playlists.py
  - backend/src/wheeloffish/api/routes/__init__.py
  - backend/src/wheeloffish/main.py
  - backend/tests/integration/test_playlists_api.py
autonomous: true
requirements:
  - PLT-01
  - PLT-02
  - PLT-03
  - SCH-01
must_haves:
  truths:
    - "GET/POST/PUT/DELETE /api/v1/playlists scoped to session app_user_id (D-18)"
    - "POST /api/v1/playlists/{id}/rebuild enqueues immediate rebuild for owner only (D-06, D-22)"
    - "List response includes last_rebuild status, last_rebuild_at, cadence labels for badges (D-21)"
    - "Detail response includes current snapshot episodes + last 3 run summaries (D-16)"
    - "Create defaults refresh_cadence=daily and episode_count=20 (D-04, Phase 4 D-19)"
  artifacts:
    - path: backend/src/wheeloffish/api/routes/playlists.py
      provides: "Playlist CRUD + rebuild REST API"
      contains: "router = APIRouter"
    - path: backend/src/wheeloffish/api/schemas/playlists.py
      provides: "Pydantic request/response models"
      contains: "class PlaylistCreateRequest"
    - path: backend/tests/integration/test_playlists_api.py
      provides: "Ownership + CRUD integration tests"
      contains: "test_create_playlist"
  key_links:
    - from: backend/src/wheeloffish/api/routes/playlists.py
      to: backend/src/wheeloffish/core/orchestrator.py
      via: "POST rebuild calls run_manual_rebuild"
      pattern: "run_manual_rebuild"
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/api/routes/playlists.py
      via: "include_router playlists prefix /api/v1"
      pattern: "playlists.router"
---

## Phase Goal

**As a** self-host operator, **I want to** create and manage playlists via API with secure ownership, **so that** the SPA and scheduler share one persistence layer (PLT-01–03, D-18, D-22).

<objective>
REST API vertical slice: playlist CRUD, rebuild trigger, list/detail with status and snapshot payloads.

Purpose: Expose orchestrator + persistence to authenticated clients.
Output: `routes/playlists.py`, `schemas/playlists.py`, integration tests, router mounted in main.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-orchestration-scheduling/05-CONTEXT.md
@.planning/phases/05-orchestration-scheduling/05-RESEARCH.md
@backend/src/wheeloffish/api/routes/catalog.py
@backend/src/wheeloffish/api/deps.py
@backend/src/wheeloffish/core/orchestrator.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-04-01 | Information Disclosure | GET /playlists/{id} | mitigate | 404 when playlist.app_user_id != session user (D-18) |
| T-05-04-02 | Tampering | POST rebuild | mitigate | Owner-only; reject cross-user playlist_id (D-22) |
| T-05-04-03 | Tampering | PUT body | mitigate | Pydantic validation episode_count ge=1; enum fields for mode/policy |
| T-05-04-04 | Spoofing | unauthenticated CRUD | mitigate | Depends(get_current_user) on all routes |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic schemas</name>
  <files>backend/src/wheeloffish/api/schemas/playlists.py</files>
  <read_first>backend/src/wheeloffish/domain/playlist.py, .planning/phases/05-orchestration-scheduling/05-UI-SPEC.md</read_first>
  <action>
Define schemas:

`PlaylistSeriesRowRequest`: series_id, mode (RowMode), completion_policy, completion_event optional defaults

`PlaylistCreateRequest`: name (min 1), episode_count default 20 ge=1, slot_allocation, default_completion_policy, refresh_cadence (daily|weekly), refresh_day_of_week optional, rows list

`PlaylistUpdateRequest`: same fields optional

`RebuildRunSummary`: id, status, started_at, finished_at, error_message optional, slots_filled, slots_requested

`SnapshotEpisode`: episode_id, title, series_id, series_title optional, slot_index, row_mode

`PlaylistDetailResponse`: id, name, config fields, rows with series titles resolved from cached_series when available, current_snapshot list[SnapshotEpisode], last_rebuild RebuildRunSummary | null, recent_runs list (max 3)

`PlaylistListItem`: id, name, refresh_cadence, refresh_day_of_week, last_rebuild_status, last_rebuild_at

Use StrEnum imports from domain.playlist for validation.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from wheeloffish.api.schemas.playlists import PlaylistCreateRequest; print(PlaylistCreateRequest.model_fields.keys())"</automated>
  </verify>
  <acceptance_criteria>
    - PlaylistCreateRequest validates episode_count ge=1
    - refresh_cadence accepts only daily or weekly strings
    - Schemas import without circular dependency
  </acceptance_criteria>
  <done>API contract matches UI-SPEC and domain enums</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRUD routes with ownership</name>
  <files>backend/src/wheeloffish/api/routes/playlists.py, backend/src/wheeloffish/main.py</files>
  <read_first>backend/src/wheeloffish/api/routes/catalog.py, backend/src/wheeloffish/api/deps.py, backend/src/wheeloffish/db/models/playlist.py</read_first>
  <action>
Create `playlists.py` router tag playlists:

- GET /playlists — filter Playlist.app_user_id == user.id; join latest rebuild_run for status/at
- POST /playlists — create with rows; default refresh_cadence daily (D-04)
- GET /playlists/{id} — detail + snapshot from latest succeeded/partial run + recent_runs query limit 3
- PUT /playlists/{id} — replace rows (delete orphan rows, upsert new); ownership check
- DELETE /playlists/{id} — cascade via ORM; ownership check

Helper `_get_owned_playlist(db, playlist_id, app_user_id)` raises 404 if not found or wrong owner.

Mount in main.py: `app.include_router(playlists.router, prefix="/api/v1")`

Validate weekly cadence requires refresh_day_of_week 0-6 on create/update.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from wheeloffish.main import create_app; create_app()"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'include_router.*playlists' backend/src/wheeloffish/main.py` >= 1
    - Router defines GET and POST on "" path under /playlists prefix (full path /api/v1/playlists)
    - All route handlers depend on get_current_user
  </acceptance_criteria>
  <done>PLT-01–03 CRUD persisted via REST</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild endpoint</name>
  <files>backend/src/wheeloffish/api/routes/playlists.py</files>
  <read_first>backend/src/wheeloffish/core/orchestrator.py, .planning/phases/05-orchestration-scheduling/05-CONTEXT.md</read_first>
  <action>
Add POST /playlists/{id}/rebuild:
- Ownership check via _get_owned_playlist
- If latest run status==running return 409 with code rebuild_in_progress
- await run_manual_rebuild(db, id, user.id)
- Return RebuildRunSummary from completed run

Does not alter next scheduled cron firing (D-06).
  </action>
  <verify>
    <automated>grep -q 'rebuild' backend/src/wheeloffish/api/routes/playlists.py && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Route path includes `/rebuild`
    - Handler calls run_manual_rebuild
    - Returns 404 for other user's playlist id
  </acceptance_criteria>
  <done>D-06/D-22 manual rebuild API ready</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration tests</name>
  <files>backend/tests/integration/test_playlists_api.py</files>
  <read_first>backend/tests/integration/conftest.py, backend/src/wheeloffish/api/routes/playlists.py</read_first>
  <action>
Tests using TestClient + authenticated session fixture (follow existing auth test patterns):

- `test_create_playlist_returns_201` with name + one row
- `test_list_playlists_scoped_to_user` — user B cannot see user A playlist in list
- `test_get_other_users_playlist_404`
- `test_update_playlist_rows` — add second series row
- `test_delete_playlist`
- `test_rebuild_owner_only` — mock run_manual_rebuild or use test provider

If no session fixture exists, create minimal helper setting session cookie with app_user_id.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/integration/test_playlists_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - At least 5 integration tests pass
    - Cross-user access test expects 404 not 403 (avoid leaking existence)
  </acceptance_criteria>
  <done>API ownership and CRUD verified (D-18)</done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/integration/test_playlists_api.py -q && uv run ruff check src/wheeloffish/api/routes/playlists.py src/wheeloffish/api/schemas/playlists.py
</verification>

<success_criteria>
- PLT-01/02/03: Full playlist config CRUD via API
- SCH-01: Rebuild endpoint triggers same pipeline as scheduler
- D-04/D-06/D-16/D-18/D-21/D-22: defaults, manual rebuild, history, ownership, status fields
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-scheduling/05-04-SUMMARY.md` when done.
</output>
