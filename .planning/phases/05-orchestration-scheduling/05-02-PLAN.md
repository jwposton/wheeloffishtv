---
phase: 05-orchestration-scheduling
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/pyproject.toml
  - backend/src/wheeloffish/core/config.py
  - backend/src/wheeloffish/core/scheduler.py
  - backend/src/wheeloffish/core/playlist/cadence.py
  - backend/src/wheeloffish/main.py
  - backend/tests/unit/test_playlist_cadence.py
  - backend/tests/unit/test_scheduler.py
  - .env.example
autonomous: true
requirements:
  - SCH-01
must_haves:
  truths:
    - "APScheduler AsyncIOScheduler starts in FastAPI lifespan and shuts down cleanly (D-07)"
    - "Global cron defaults to 04:00 UTC via WOF_REBUILD_CRON_UTC env (D-01)"
    - "is_due returns true for daily playlists every night and weekly only on matching DOW (D-02, D-03)"
    - "Scheduler job uses max_instances=1 and coalesce=True — no catch-up pile-up (D-05, D-10)"
    - "Startup marks orphaned running rebuild_runs as failed with interrupted message"
  artifacts:
    - path: backend/src/wheeloffish/core/scheduler.py
      provides: "parse_cron_time + create_scheduler factory"
      contains: "AsyncIOScheduler"
    - path: backend/src/wheeloffish/core/playlist/cadence.py
      provides: "is_due(playlist, now_utc) cadence filter"
      contains: "def is_due"
    - path: backend/src/wheeloffish/main.py
      provides: "lifespan starts scheduler"
      contains: "scheduler.start"
  key_links:
    - from: backend/src/wheeloffish/main.py
      to: backend/src/wheeloffish/core/scheduler.py
      via: "lifespan creates scheduler with CronTrigger"
      pattern: "WOF_REBUILD_CRON_UTC"
---

## Phase Goal

**As a** self-host operator, **I want to** have playlists rebuild automatically on a nightly schedule respecting each playlist's cadence, **so that** I get fresh mixed-TV output without manual intervention (SCH-01, D-01–D-05).

<objective>
Install APScheduler, wire global UTC cron into FastAPI lifespan, implement pure cadence evaluation, and recover interrupted jobs on startup.

Purpose: Time-based trigger layer before orchestrator rebuild logic.
Output: `scheduler.py`, `cadence.py`, modified `main.py` + `config.py`, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-orchestration-scheduling/05-CONTEXT.md
@.planning/phases/05-orchestration-scheduling/05-RESEARCH.md
@backend/src/wheeloffish/main.py
@backend/src/wheeloffish/core/config.py
@backend/src/wheeloffish/db/models/rebuild_run.py
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-02-01 | Denial of Service | scheduler job overlap | mitigate | max_instances=1 on nightly job |
| T-05-02-02 | Tampering | WOF_REBUILD_CRON_UTC parse | mitigate | Validate HH:MM format; fallback 04:00 on invalid with warning log |
| T-05-02-SC | Tampering | pip/uv deps | mitigate | Pin apscheduler~=3.11.2 in pyproject.toml |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install APScheduler + config field</name>
  <files>backend/pyproject.toml, backend/src/wheeloffish/core/config.py, .env.example</files>
  <read_first>backend/pyproject.toml, backend/src/wheeloffish/core/config.py</read_first>
  <action>
Run `cd backend && uv add "apscheduler~=3.11.2"`.

Add to Settings class: `WOF_REBUILD_CRON_UTC: str = "04:00"`.

Ensure `.env.example` documents the field (may already exist from 05-01).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import apscheduler; from wheeloffish.core.config import Settings; print(Settings.WOF_REBUILD_CRON_UTC)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` or lockfile lists apscheduler
    - `Settings` model includes field `WOF_REBUILD_CRON_UTC` default `"04:00"`
  </acceptance_criteria>
  <done>APScheduler dependency available</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cadence evaluation pure functions</name>
  <files>backend/src/wheeloffish/core/playlist/cadence.py, backend/tests/unit/test_playlist_cadence.py</files>
  <read_first>.planning/phases/05-orchestration-scheduling/05-CONTEXT.md, backend/src/wheeloffish/db/models/playlist.py</read_first>
  <action>
Create `cadence.py` with:

`def is_due(playlist_orm, now_utc: datetime) -> bool`:
- If refresh_cadence == "daily": return True (D-03 evaluates at nightly trigger only)
- If refresh_cadence == "weekly": return now_utc.weekday() == playlist_orm.refresh_day_of_week (Mon=0 per D-24)
- If weekly but refresh_day_of_week is None: return False and log warning

`def parse_cron_time(cron_str: str) -> tuple[int, int]`:
- Split "HH:MM", return (hour, minute) ints
- On invalid format raise ValueError

Tests in `test_playlist_cadence.py`:
- daily always due
- weekly due only on matching weekday
- weekly with null DOW not due
- parse_cron_time("04:00") == (4, 0)
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_playlist_cadence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_playlist_cadence.py -q` reports at least 4 passed
    - `grep -c 'def is_due' backend/src/wheeloffish/core/playlist/cadence.py` equals 1
  </acceptance_criteria>
  <done>D-02/D-03/D-04 cadence rules unit-tested</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scheduler factory + lifespan wiring</name>
  <files>backend/src/wheeloffish/core/scheduler.py, backend/src/wheeloffish/main.py, backend/tests/unit/test_scheduler.py</files>
  <read_first>backend/src/wheeloffish/main.py, .planning/phases/05-orchestration-scheduling/05-RESEARCH.md</read_first>
  <action>
Create `scheduler.py`:
- `def create_scheduler(settings: Settings, job_callable) -> AsyncIOScheduler`
- Parse WOF_REBUILD_CRON_UTC via parse_cron_time
- Add job id `nightly_rebuilds` with CronTrigger(hour=, minute=, timezone="UTC"), max_instances=1, coalesce=True, misfire_grace_time=None

Modify `main.py` lifespan:
- Import create_scheduler
- Pass placeholder callable `run_nightly_rebuilds` from orchestrator module (stub no-op if orchestrator not yet implemented — import from wheeloffish.core.orchestrator)
- scheduler.start() before yield; scheduler.shutdown(wait=True) after yield

Add `recover_interrupted_rebuilds(db)` call on startup: set rebuild_runs.status from running→failed where status='running', error_message='Interrupted by restart'

Test `test_scheduler.py`:
- create_scheduler registers job id nightly_rebuilds
- parse_cron_time invalid raises
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/unit/test_scheduler.py tests/unit/test_playlist_cadence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scheduler.start' backend/src/wheeloffish/main.py` equals 1
    - `grep -c 'scheduler.shutdown' backend/src/wheeloffish/main.py` equals 1
    - Scheduler tests pass
  </acceptance_criteria>
  <done>SCH-01 cron trigger wired in-process (D-07)</done>
</task>

</tasks>

<verification>
cd backend && uv run pytest tests/unit/test_playlist_cadence.py tests/unit/test_scheduler.py -q && uv run ruff check src/wheeloffish/core/scheduler.py src/wheeloffish/core/playlist/cadence.py
</verification>

<success_criteria>
- SCH-01: Nightly scheduler registered at configurable UTC cron
- D-01/D-05/D-07/D-10: global timer, no catch-up, in-process APScheduler, no same-night retry
- D-02/D-03/D-04: cadence filter implemented and tested
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-scheduling/05-02-SUMMARY.md` when done.
</output>
