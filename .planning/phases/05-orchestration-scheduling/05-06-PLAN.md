---
phase: 05-orchestration-scheduling
plan: 06
type: execute
wave: 6
depends_on:
  - "05-05"
files_modified:
  - frontend/src/api/playlists.ts
  - frontend/src/pages/PlaylistDetailPage.tsx
  - frontend/src/pages/PlaylistFormPage.tsx
  - frontend/src/components/playlists/PlaylistForm.tsx
  - frontend/src/components/playlists/SeriesPicker.tsx
  - frontend/src/components/playlists/RebuildBanner.tsx
  - frontend/src/components/playlists/OutputList.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/PlaylistDetailPage.test.tsx
autonomous: true
requirements:
  - WEB-01
  - PLT-03
must_haves:
  truths:
    - "User can create playlist at /playlists/new with series picker from catalog search (D-25)"
    - "User can edit playlist settings including episode_count, slot_allocation, policies, cadence, rows (D-26)"
    - "Detail page shows current output episode list + status banner with error text (D-20, D-21)"
    - "Rebuild now button triggers POST rebuild and polls until terminal status (D-06, D-22)"
    - "Ordered/disordered toggle and per-row completion override exposed in form"
  artifacts:
    - path: frontend/src/pages/PlaylistDetailPage.tsx
      provides: "/playlists/:id detail"
      contains: "PlaylistDetailPage"
    - path: frontend/src/components/playlists/PlaylistForm.tsx
      provides: "Shared create/edit form"
      contains: "PlaylistForm"
    - path: frontend/src/components/playlists/SeriesPicker.tsx
      provides: "Catalog search multi-select"
      contains: "SeriesPicker"
  key_links:
    - from: frontend/src/pages/PlaylistDetailPage.tsx
      to: /api/v1/playlists/{id}/rebuild
      via: "useMutation POST rebuild"
      pattern: "/rebuild"
    - from: frontend/src/components/playlists/SeriesPicker.tsx
      to: /api/v1/catalog/series
      via: "debounced browse search reuse"
      pattern: "catalog/series"
---

## Phase Goal

**As a** self-host operator, **I want to** create playlists, pick shows from my catalog, tune advanced settings, and trigger rebuilds, **so that** I control my mixed-TV lineup end-to-end (D-25, D-26, D-06, WEB-01).

<objective>
SPA vertical slice — create/edit form with series picker, detail page with output list, rebuild now with polling, delete confirmation.

Purpose: Complete Phase 5 operator UI for playlist lifecycle.
Output: PlaylistFormPage, PlaylistDetailPage, SeriesPicker, RebuildBanner, OutputList, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-orchestration-scheduling/05-UI-SPEC.md
@.planning/phases/05-orchestration-scheduling/05-CONTEXT.md
@frontend/src/pages/BrowsePage.tsx
@frontend/src/hooks/useBrowseSeries.ts
@frontend/src/api/playlists.ts
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-06-01 | Tampering | Rebuild now button | mitigate | Only shown on owned playlist detail; API enforces D-22 |
| T-05-06-02 | Tampering | Delete playlist | mitigate | AlertDialog confirmation with destructive copy from UI-SPEC |
| T-05-06-03 | XSS | RebuildBanner error | mitigate | Plain text error_message only |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API mutations and detail hook</name>
  <files>frontend/src/api/playlists.ts</files>
  <read_first>backend/src/wheeloffish/api/schemas/playlists.py, frontend/src/api/playlists.ts</read_first>
  <action>
Extend playlists.ts:

Types: PlaylistDetailResponse, PlaylistCreatePayload, SnapshotEpisode

Functions:
- fetchPlaylist(id)
- createPlaylist(payload)
- updatePlaylist(id, payload)
- deletePlaylist(id)
- triggerRebuild(id)

Hooks:
- usePlaylist(id) — refetchInterval 5000 when last_rebuild_status is running or queued (UI-SPEC polling)
- useCreatePlaylist, useUpdatePlaylist, useDeletePlaylist, useRebuildPlaylist mutations invalidating ['playlists']

Map slot_allocation API values to labels: wild→Wild, balanced→Balanced, round_robin→Round-robin (D-26).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - exports triggerRebuild and usePlaylist
    - usePlaylist accepts id string parameter
    - PlaylistDetailResponse includes current_snapshot array
  </acceptance_criteria>
  <done>Full CRUD + rebuild client ready</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SeriesPicker component</name>
  <files>frontend/src/components/playlists/SeriesPicker.tsx</files>
  <read_first>frontend/src/pages/BrowsePage.tsx, frontend/src/hooks/useBrowseSeries.ts, .planning/phases/05-orchestration-scheduling/05-CONTEXT.md</read_first>
  <action>
SeriesPicker props: selectedSeriesIds, onAdd(series), onRemove(seriesId), existingRows optional

Reuse catalog browse API with debounced search (300ms) and q= parameter (D-25).

Display results as list with poster thumb + title; clicking adds row unless series_id already selected.

Show selected rows as removable chips/cards with series title resolved from selection cache.

Do not allow free-text series id entry.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SeriesPicker file exports SeriesPicker function component
    - Uses fetch to /api/v1/catalog/series or existing browse hook
    - No input field for raw series_id paste-only
  </acceptance_criteria>
  <done>D-25 catalog picker implemented</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlaylistForm create/edit</name>
  <files>frontend/src/components/playlists/PlaylistForm.tsx, frontend/src/pages/PlaylistFormPage.tsx, frontend/src/App.tsx</files>
  <read_first>.planning/phases/05-orchestration-scheduling/05-UI-SPEC.md, backend/src/wheeloffish/domain/playlist.py</read_first>
  <action>
PlaylistForm sections per UI-SPEC:
1. Basics: name Input, episode_count number default 20, slot_allocation Select (Wild/Balanced/Round-robin), default_completion_policy Select (remove/restart/disordered)
2. Schedule: Radio daily|weekly; weekly shows Select day Mon-Sun mapping 0-6
3. Series rows: SeriesPicker + per-row ordered/disordered toggle + completion_policy override Select

Validation: name required, episode_count>=1, weekly requires DOW, at least one row to submit.

PlaylistFormPage: reads :id param — if "new" create mode else load usePlaylist and edit mode.

Routes: /playlists/new and /playlists/:id/edit both render PlaylistFormPage (or separate new route only).

Submit calls createPlaylist or updatePlaylist; navigate to /playlists/:id on success.
  </action>
  <verify>
    <automated>grep -q 'episode_count' frontend/src/components/playlists/PlaylistForm.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - PlaylistForm contains slot_allocation and default_completion_policy fields
    - App.tsx routes /playlists/new to form page
    - Weekly cadence shows day-of-week select when weekly selected
  </acceptance_criteria>
  <done>D-26 advanced settings exposed in UI</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Detail page output + rebuild + delete</name>
  <files>frontend/src/pages/PlaylistDetailPage.tsx, frontend/src/components/playlists/RebuildBanner.tsx, frontend/src/components/playlists/OutputList.tsx, frontend/src/App.tsx, frontend/src/pages/PlaylistDetailPage.test.tsx</files>
  <read_first>.planning/phases/05-orchestration-scheduling/05-UI-SPEC.md, frontend/src/components/playlists/StatusBadge.tsx</read_first>
  <action>
RebuildBanner: shows StatusBadge, last_rebuild_at, error_message for failed, partial warning copy from UI-SPEC.

OutputList: numbered list of snapshot episodes (slot_index, title, series title).

PlaylistDetailPage:
- usePlaylist(id)
- Header with name, Edit link to /playlists/:id/edit, Delete with AlertDialog
- Rebuild now Button calling useRebuildPlaylist; disabled when status running
- RebuildBanner + OutputList sections
- Empty output message when no snapshot yet

Route /playlists/:id → PlaylistDetailPage (exclude :id matching "new" — order routes so /new comes first)

Test: mock API returns detail; assert Rebuild now button present and OutputList renders episode title.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/pages/PlaylistDetailPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Detail page contains button text "Rebuild now"
    - Delete confirmation includes "cannot be undone"
    - Test passes with at least 1 assertion on output list
  </acceptance_criteria>
  <done>Full playlist lifecycle UI complete (D-06, D-20, D-22)</done>
</task>

</tasks>

<verification>
cd frontend && npm run test -- --run src/pages/PlaylistDetailPage.test.tsx && npm run build && cd ../backend && uv run pytest tests/integration/test_playlists_api.py -q
</verification>

<success_criteria>
- WEB-01: Playlist CRUD UI, status badges, rebuild now, output view
- PLT-03: Add/remove series via catalog picker
- D-06/D-20/D-22/D-25/D-26: manual rebuild, detail route, owner actions, catalog picker, advanced settings
- Phase 5 exit: operator can define playlist, rebuild, inspect output and status
</success_criteria>

<output>
Create `.planning/phases/05-orchestration-scheduling/05-06-SUMMARY.md` when done.
</output>
